# Axiom Synthesis — wake-up note (built overnight 2026-07-22)

Two boss-requested improvements were designed with you, then built and proven while you
slept. Everything that does NOT need your accounts is **done and tested**. Three account
steps remain — only you can do them.

## DONE + PROVEN (offline, no model calls, no accounts)

### 1. Boolean confidence checklist (Point 1)
- `scripts/checklist.py` — the gate engine (veto + percentage → tier).
- `config/checklist.yaml` — the editable knobs (threshold, questions, domain packs).
- Wired into `scripts/orchestrate.py` (stage 4.5, engaged when judge answers are present;
  legacy path untouched when they aren't).
- Rules, as you approved:
  - 4 **veto** questions: any single NO (or missing answer) nullifies the claim.
  - 10 **normal** questions scored to a percentage.
  - Tiers: ≥90% → `verified`, ≥70% → `probable`, below → `unverified`.
  - Single-source cap preserved (needs N1 + N2 for `verified`).
  - Universal core + optional per-topic `domain_packs`.
- Tests: `tests/test_checklist.py` — **21 assertions**. Full suite = **75 assertions, all
  passing.** Includes your 9/10 example and the veto override.

### 2. Four-family fan-out — code + prompts (Point 5)
- New `zai` provider in `.acos/config/providers.yaml` (direct `api.z.ai`, key env
  `ZAI_API_KEY`) — verified it parses.
- Four blind, family-neutral prompts built: `prompts/{elicitor,grader,refuter,synthesizer}.md`.
- ChatGPT browser voice + wake-up steps documented in `SKILL.md`.
- Plan updated: `PLAN.md` §17.

## YOUR THREE STEPS (need your accounts — I could not do these asleep)

```bash
# 1) Gemini — DONE 2026-07-22: key stored in Doppler as GOOGLE_API_KEY;
#    gemini-flash-latest confirmed working. (gemini-2.5-flash is retired -> 404.)
doppler run --project ai-model-api --config dev_personal --silent -- \
  python3 .claude/scripts/run-external-agent.py --agent qa-reviewer \
  --model google:gemini-flash-latest --task "reply OK"

# 2) z.ai / GLM — DONE 2026-07-22: glm-4.7 confirmed working.
#    Two fixes were needed: (a) map the Doppler secret name ZAI_CODING_PLAN_API_KEY
#    to the env var ZAI_API_KEY the code expects; (b) the Coding Plan key is funded
#    only on the CODING endpoint (/api/coding/paas/v4), NOT the general one —
#    providers.yaml now points there.
doppler run --project ai-model-api --config dev_personal --silent -- \
  bash -c 'ZAI_API_KEY="$ZAI_CODING_PLAN_API_KEY" \
    python3 .claude/scripts/run-external-agent.py --agent qa-reviewer \
    --model zai:glm-4.7 --task "reply OK"'

# 3) ChatGPT (REQUIRED 4th family, browser voice) — DONE 2026-07-22.
#    Confirmed working: navigated chatgpt.com in the connected Chrome (Plus,
#    logged in as Ziaul Karim), sent a test prompt, got exact reply "OK".
#    GOTCHA: two Chrome browsers were connected to the account, so the session
#    reported "not connected" until Browser 1 (deviceId 8273510d-...) was
#    selected via list_connected_browsers + select_browser. Not a login problem.
```

## STATUS 2026-07-22: all four families LIVE
Claude (Task) · Gemini (gemini-flash-latest) · GLM (glm-4.7, coding endpoint) ·
ChatGPT (browser voice, Plus). Ready for a live end-to-end fan-out run.

## Verify anytime
```bash
S=.claude/skills/acos-axiom-synthesis
python3 $S/tests/test_substrate.py && python3 $S/tests/test_pipeline.py && python3 $S/tests/test_checklist.py
```

## NOT built (deliberately out of scope — you chose to go deep on 2 of 5)
- `FOLLOW_UP_REQUIRED` claim state (boss point 1). A hook is left for it: claims that fail
  the checklist are the natural input to that state when you want it.
- Large-data quality-at-scale hardening (boss point 4).
- Standalone competency-question tooling beyond the domain-pack mechanism (boss point 3).

## Honest status
The deterministic core is finished and proven. The whole skill is NOT "live-proven"
end-to-end, because the four-model run needs the three account steps above. Nothing here
touched the other session's Autopilot (reverse-cleanroom).

## UPDATE 2026-08-02 — conductor built + LIVE-PROVEN end-to-end
- `conductor.py` + `tests/test_conductor.py` (21) + `RUNBOOK.md` built. Offline suite now
  **96 assertions**, all passing.
- **First live four-family run** executed: session `.acos/axiom/live-2026-08-02/`.
  Question: "What is the capital of Australia, and is it Australia's largest city?"
  All four families elicited LIVE (Claude via Task, Gemini + GLM via run-external-agent,
  ChatGPT via browser); 3 shared claims graded by 2 blind graders each + refuted; engine
  ran → `source-of-truth.md` produced, ledger chain intact, coverage satisfied.
- **Result (epistemically correct):** all claims landed CORROBORATED/`probable`, NOT
  verified — because every source was `origin: model-internal` (models answering from
  memory, no external citation). De-circularization correctly collapses shared-origin
  memory claims to 1 independent vote, so the engine refuses "verified" for uncited
  AI-memory consensus. To reach `verified`, elicitors must supply real external sources
  (distinct origins), which the pipeline then counts as independent.
- **Real gotchas the live run caught (all fixed):**
  1. Models mis-report their own `family` (Gemini/GLM both said "anthropic") →
     conductor now tags family by TRANSPORT (filename), not self-report.
  2. Source `text`=statement made independent families look like verbatim copies →
     fixed to `text=""` (independence via origin/family/context).
  3. zsh arrays are 1-indexed (not 0) — a `for i in 0 1 2` loop skipped C3 and made junk
     files; re-ran with `1 2 3`.
  4. v1 clustering is exact-normalized-text, so paraphrases don't auto-merge — the
     orchestrator did the semantic-merge by hand per RUNBOOK step 2 (raw replies kept in
     `collected/elicitor-raw/`).

## UPDATE 2026-08-02 (2) — cited-source elicitor + `verified` PROVEN live
- Elicitor upgraded (`prompts/elicitor.md`): each claim must carry a real cited source
  with a `source_url` + `source_date`. `origin` now keys de-circularization on the source
  URL (distinct URLs = independent). `source_date` threaded into ledger provenance as
  `as_of` (recorded now for the future recency work). Offline suite now **99 assertions**.
- **Second live run** (session `.acos/axiom/live-sourced-2026-08-02/`), web-enabled:
  Claude (Task+WebSearch) + ChatGPT (browser web search) each fetched REAL dated sources.
  Result: **C1 "The capital of Australia is Canberra" reached ESTABLISHED / `verified`** —
  cited by TWO distinct sources from TWO families (nca.gov.au [anthropic] +
  britannica.com [openai-web]). The other 4 claims (single source each) correctly stayed
  `probable`/CORROBORATED. `source-of-truth.md` shows the verified claim with both URLs.
- **Honest firewall demo:** in a first attempt BOTH models cited the SAME government page
  (nca.gov.au) → de-circularization correctly collapsed them to 1 source → `probable`.
  Only when ChatGPT cited a DIFFERENT source (Britannica) did it reach `verified`.
- **More real bugs caught + fixed:** (5) numeric claim values arrive as strings
  ("5,638,830") and crashed median/mean fusion → conductor now coerces to float, falls
  back to categorical if not a clean number (test added). (6) re-running over an existing
  ledger causes illegal-transition refusals (ESTABLISHED→CONJECTURE) — the engine
  correctly refuses; a clean re-run needs a fresh ledger or resume logic (KNOWN LIMIT).
- **Constraint surfaced:** only Claude (Task) + ChatGPT (browser) can web-search live;
  the external runner (Gemini/GLM) has no web tools. Adding web-grounding to
  `run-external-agent.py` is the follow-up to get all four families citing live sources.

## UPDATE 2026-08-02 (3) — recency discipline BUILT (option a) + 139 tests green
Zee chose option (a): implement the recency-bias fix designed in
`RESEARCH-recency-bias-2026-08-02.md` §6.1, defaults A/B/C confirmed. Built the full
core (mechanisms h+a+g+e+f+c); deferred the optional refinements (b) time-decay math and
(d) `--recency` mode. All offline + deterministic; **139 assertions passing** (substrate
19 + pipeline 35 + checklist 21 + conductor 24 + **recency 40 new**).
- **New:** `scripts/volatility.py` (classifier + `compute_freshness`) and
  `config/volatility.yaml` (freshness windows — durable ∞ / slow 1095d / fast 183d /
  volatile 14d; lexical signals; domain priors; `stale_k`). Classifier = deterministic
  lexical + domain prior, optional blind-judge label reconciled; ADVISORY + asymmetric
  (only ever moves a claim toward MORE volatile; never nullifies). Unknown → `slow`, low
  confidence, flagged-not-capped (Decision A). No-date source → unknown-age, can't be
  "recent" (Decision B).
- **Wired (opt-in, inert on pre-recency fixtures):** `grade_fuse.grade_claim` gains
  `volatility`+`freshness` → a `("stale", severity)` downgrade + a recent-corroboration
  requirement for the top tier (caps `verified`→`probable`, never lower);
  `resolve.resolve_conflict` gains a **guarded supersession** rung for non-durable
  conflicts (newer AND ≥tier AND corroborated → wins; else falls through to the classic
  ladder); `axiom_ledger.render_markdown` surfaces a "⚠ possibly stale" banner + per-source
  `as-of`; `orchestrate.process_fact` classifies + computes freshness + stores
  `volatility`/`observed_at`/`recency` on every claim; `conductor.py` needs no change (it
  already threads `domain` + source `as_of` + `date_str`, so it auto-inherits recency —
  a live run must pass the real `date_str`).
- **TWO deliberate deviations from the design doc, verified against the code (the
  "verify, don't trust the description" lesson):** (1) `N5-NOT-STALE` STAYS a **normal**
  question, not a veto — the actual `checklist.yaml`/`checklist.py` had it as normal, and
  keeping it normal means a stale claim loses a point → tier capped, consistent with
  "cap, never nullify" (a veto would fully nullify, which the guardrails forbid).
  (2) Freshness keys on each source's `as_of` (content date), not `observed_at` (ingest)
  as §6.2 literally says, because `observed_at ≈ run-date` carries NO staleness signal
  within one run; `observed_at` is still recorded per claim for bitemporal audit.
- **Parser gotcha found + worked around:** the skill's minimal stdlib YAML reader
  (`checklist._parse_yaml`) silently dies at the first NESTED block, so `checklist.yaml`
  never actually parses — the run always falls back to the embedded `DEFAULT_CHECKLIST`.
  `config/volatility.yaml` is therefore written FULLY FLAT (top-level `key: value` only)
  so the parser reads it and Zee's edits to the numbers ACTUALLY take effect (Decision A
  has teeth). `load_volatility()` overlays the file onto the embedded `DEFAULT_VOLATILITY`
  (graceful fallback + drift test). NOTE for later: `checklist.yaml`'s own edits still
  don't take effect — a separate pre-existing bug, out of scope for this task.
- **Guardrails preserved (design §6.4):** de-circularization runs first; the
  ≥2-independent-cross-family floor is never lowered; supersession is authority+corroboration
  gated; the classifier only caps/flags; decay/cap never inverts a winner; superseded
  claims stay in the ledger. Working tree still dirty + uncommitted; other sessions still
  touching this repo — any commit must stay scoped to `acos-axiom-synthesis` paths.

## UPDATE 2026-08-02 (4) — line-by-line review found 3 real bugs; all FIXED, 146 tests
Review method: re-read every changed file, then DEMONSTRATED each suspected bug by
running it live — no theoretical findings. Every bug biased toward wrongly capping at
`probable`, never toward a false `verified` (the cap-not-nullify floor held throughout).
- **F1 (volatility.py, FIXED):** two DISAGREEING signals (lexical vs domain) produced
  HIGH confidence for the more-volatile class — "The capital of Minnesota is Saint
  Paul." + domain geography → fast/high, arming the stale cap on a durable fact.
  Now: agreement is the ONLY path to 'high'; disagreement → more-volatile class at LOW
  confidence (cap disarmed) + flagged, mirroring the judge-disagreement rule.
- **F2 (volatility.py, FIXED):** raw substring lexical matching — 'sota' fired inside
  "Minnesota", 'rates' inside "operates", 'live' inside "delivered" (the latter two at
  gate-applying medium confidence). Now whole-word `\b` regex matching.
- **F3 (resolve.py, FIXED):** supersession's upper-rung protection compared the TOP TWO
  claims overall, not NEWEST-vs-INCUMBENT — in a 3-claim conflict a newest
  reliability-D claim superseded a reliability-A incumbent. Now the pair-wise check:
  if any upper rung separates newest from incumbent, supersession abstains and the
  classic ladder decides. (2-claim conflicts were always correct.)
- **F6/F7 nits (FIXED):** `window_fast: true` no longer becomes a 1-day window (bool is
  an int subclass in Python); dead ternary in a test cleaned up.
- **Suite: 139 → 146 assertions** (test_recency 40 → 47: Minnesota/operates/delivered
  word-boundary cases, the disagreement-confidence case, both 3-claim supersession
  cases, the bool-window case). All 146 passing; the three original bug demos re-run
  clean (durable/medium, slow/low/flagged ×2, incumbent A wins at independent_sources).
- **STILL OPEN (flagged in review, NOT fixed):** F4 — the conductor passes no `domain`
  or `volatility_judge` (assemble_fact call at conductor.py:535) and its grader
  `freshness_ok` flag (conductor.py:330) is now dead — live recency stays mostly
  inert-conservative until the grader prompt + build_facts carry volatility signals.
  F5 — a resolved supersession returns loser ids but nothing writes SUPERSEDED ledger
  entries (conflict contenders aren't separate ledger claims today; documented limit).

## UPDATE 2026-08-02 (5) — F4 BUILT: live recency wiring + a real gap found & fixed. 159 tests
F4 closed (F5 remains the only open item). What changed:
- **Grader prompt (`prompts/grader.md`):** the retired `freshness_ok` flag is replaced by
  a blind `volatility` label (durable/slow/fast/volatile) — the judge labels how fast the
  CLAIM's truth decays; freshness itself stays code-computed from source dates.
- **Conductor:** `parse_grader` validates/normalizes the label (legacy `freshness_ok`
  silently ignored — old fixtures parse clean); `combine_graders` takes a unique-max vote
  (tie → None = no judge signal, the conservative direction); `assemble_fact` attaches it
  as `volatility_judge` and drops the dead grading field; `build_facts`/`run_from_collected`
  accept an optional run-level `domain` tag that reaches every fact.
- **Classifier rule (volatility.py):** a judge label with NO deterministic signals is ONE
  real clue, not a disagreement with the unknown-default fallback → judge's class at
  MEDIUM confidence (gate armed). Without this, the judge vote could never arm the
  freshness gate on lexically-quiet claims — the whole F4 wiring would have been moot.
- **REAL GAP found while building (orchestrate.py, fixed):** in checklist mode, a stale
  claim losing only N5-NOT-STALE scores 9/10 = 90%, which still meets verified_min=0.90 —
  so the checklist percentage alone can NEVER block `verified` for a stale claim, and the
  grade_claim recency cap was bypassed (`conf = verdict["tier"]`). Fixed: the checklist
  path now applies the same mechanism-(f) cap — confidently non-durable + no recent
  independent source → `verified` capped to `probable` (`recency_capped` in the result).
- **Proof:** new e2e case — lexically-quiet claim ("The reference figure is 42."),
  undated sources, no domain, two graders voting `volatile` → classified volatile from
  the vote alone → capped at CORROBORATED with `recency_capped=True`. Suite grew
  146 → **159** (test_conductor 24 → 36, test_recency 47 → 48); all passing.
- Docs updated: SKILL.md counts, RUNBOOK (§3 grader label, §5 sample now shows
  `domain=` + real-`date_str` warning, proven-offline note), prompts/README.md contract.

## UPDATE 2026-08-02 (6) — THIRD live run: recency discipline PROVEN on a real stale-vs-fresh fight
Session `.acos/axiom/live-recency-2026-08-02/`. Question: "What is the current target
range for the US federal funds rate, and when did the FOMC last change it?" — chosen
because the answer post-dates every model's training data. **The live world handed us
the exact failure the discipline exists for:** Gemini + GLM (memory-only, no web)
both confidently answered the STALE range 4.25-4.50% / last change 2024-12-18;
Claude (Task+WebSearch) + ChatGPT (browser web) both found the CURRENT range
3.50-3.75%, last change 2025-12-10 (held at 2026-07-29 meeting), with real dated Fed
citations.
- **Result:** C1 (current range) + C2 (last-change date) both ESTABLISHED/verified with
  the FRESH values — conflicts resolved at the **supersession rung** (guarded rung fired
  LIVE: newer as_of + equal tier + authority-corroborated beat the stale side's equal
  accumulated support). C3/C4/C5 correctly capped probable (family/source floors).
  Coverage complete, hash chain intact, evidence mirrored. 4-family fan-out: elicitors
  ×4, blind graders ×2 per claim (Claude WebFetch-verified the Fed pages; Gemini
  text-judged), GLM refuters ×5 (2 substantive objections, 3 clean passes; C2's
  objection — "a page timestamp is not evidence of a policy action" — recorded as a
  live disconfirmer on the STALE side).
- **F4 features exercised live:** grader volatility labels emitted + voted (C1 tie
  fast/volatile → no judge signal — vote tie rule worked); `domain="rates"` threaded;
  real `date_str`; per-source `as-of` stamps render in source-of-truth.md.
- **LIVE FINDINGS (report-only, not yet fixed):**
  1. **Adjacent-class disagreement disarms the gate everywhere:** judges said fast (C1,
     C2) or durable (C3-C5) while the domain prior said volatile → the F1 disagreement
     rule dropped ALL five claims to low confidence → `gate_applies=false` run-wide, so
     the freshness cap never armed. RIGHT outcome for C3-C5 (historical facts protected)
     but C2 sat stale:true-yet-uncapped. Refinement candidate: adjacent-class
     disagreement (fast↔volatile) → medium (gate stays armed); only cross-gap
     disagreement (durable↔volatile) → low.
  2. **F5 seen live:** supersession resolved C1/C2 but the SUPERSESSION LOG in
     source-of-truth.md is empty (loser side is not a separate ledger claim), and the
     winning claim's rendered provenance still lists the STALE sources (cluster
     provenance includes wrong-value candidates). Post-supersession provenance filtering
     + a conflict-level supersession note in render are the natural follow-ups.
  3. Same-elicitor lateral sources correctly collapsed (Fox Business surfaced by the
     same Claude agent as the Fed statement → same context_id → 1 vote — independence
     firewall right again), which is WHY C2's newest independent as_of stayed
     2025-12-10 (stale:true).
- Full audit trail: collected/{elicitor,elicitor-raw,grader,refuter}/ + MERGE-NOTES.md
  (semantic merges, granularity mapping, unit normalization, ChatGPT JSON-quote repair)
  + driver.py (conflict enrichment, documented) + evidence/C*.txt packets.
