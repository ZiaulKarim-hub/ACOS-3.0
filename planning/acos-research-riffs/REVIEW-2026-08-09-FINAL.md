# /acos-research-riffs — FINAL Review (2026-08-09)

Third adversarial pass, after fix pass 1 (REVIEW-2026-07-25), fix pass 2 (REVIEW-2026-07-27),
the recency policy, and the deep-drill chat mode. 13 reader lenses + a fix-audit lens over the
NEWEST code, adversarial verification (default-reject), synthesis. Workflow `wf_0d1680e7-0cc`,
27 agents, 0 errors on the resumed run.

**Findings:** 66 raised -> 56 survived verification -> 8 merged -> 18 must-fix / 30 improvements.

## SHIP VERDICT: **HOLD**

HOLD is driven by two empirically-confirmed HIGH defects in the claim engine plus the reachable claim-engine mediums that compound them. claims.ts:174 (negation parity via count%2, verified at source against a comment promising the opposite) drops a double-negative refutation as a duplicate and merges its refuting tier-1 source into the refuted claim, upgrading a wrong claim to verified on the extremely common 'X is not available and Y is not supported' phrasing — the founding failure class this tool was built to eliminate, reintroduced. claims.ts:796 (verified) scopes conflict detection to the answering hit while pooling corroboration across all strong hits, so a recorded $99-vs-$49 dispute between two supporting hits is delivered 'verified' with conflicting_ids empty. Around them, claims.ts:162 fabricates conflicts between '$99' and '$99.00' (blocking corroboration and telling the user 'corpus disagrees'), claims.ts:540 silently erases recorded disputes on the standard probe-append+re-ingest loop, and claims.ts:922 delivers a zero-source claim as provisional in violation of I1. For a trust-first tool whose sole promise is never to present an unverified or contradicted claim as settled, these are actively misleading in realistic use — the mandate is explicit that three review cycles do not earn a SHIP while something misleading remains. Everything outside the claim engine (recency-delivery seams, two unfenced injection surfaces, room/liveness robustness, doc-drift, tests) is SHIP-WITH-NOTES-grade and does not by itself block; the tool becomes shippable once the negation-count fix, cross-strong-hit conflict surfacing, numeric-value comparison, conflicts_with carry-over, and the zero-source guard land.

## Overall

Third-pass synthesis of /acos-research-riffs at /Users/zee/Documents/Vibe Coding/ACOS 3.0/.claude/skills/acos-research-riffs. Across 13 lenses, 56 findings surfaced; deduping merges 8 into 48 unique defects (2 HIGH, 23 MEDIUM, 23 LOW). The fix pass is genuinely thorough on the happy path — the fix-audit lens correctly certifies all 36 M-items, all 8 contracts, and the deep-drill stamp contract as landed for the primary flow — but adversarial re-probing found that the trust core still leaks: two empirically-confirmed HIGH defects in claims.ts make the engine deliver a wrong or disputed claim under a trustworthy label, one of them reintroducing the founding wrong-claim-as-verified failure class on common two-clause negation phrasing. Below that sit a cluster of recency-policy seams (young-window keyed to capture time, action-field and ledger-label gaps, charter with no sweep executor), two unfenced prompt-injection surfaces the M22 fence skipped, and room/liveness robustness holes; the low tier is almost entirely doc-drift, dead code, and test gaps. The skill is close, and most of what survives is a small pass — but the two HIGHs are the exact thing this tool exists to prevent, so it is not shippable as a trust-first daily tool until they land.

## Fix-audit verdict (did fix pass 2 + contracts + deep-drill hold?)

Substantially landed but NOT complete — the fix-audit lens's "all 36 hold, all 8 contracts verified" is accurate only for the primary path; adversarial lenses found roughly a dozen fixes with surviving edge seams, one of them a genuine reintroduction of the original failure class. Specifically: (1) M2 negation-parity is structurally wrong — the parity-mod-2 implementation (claims.ts:174, verified) drops a double-negative refutation as a duplicate and merges its refuting tier-1 source INTO the refuted claim, exactly the founding wrong-claim-as-verified class the tool exists to stop. (2) CONTRACT-1 / M5-M6 daemon liveness: the beacon-trust path (riff.ts:150) and the room.pid kill path (riff.ts:1181) both use bare kill(pid,0) with no ps identity check, unlike the lock path — recycled-pid false-ready and SIGTERM-of-innocent both reproduced. (3) CONTRACT-7 recency is wired but leaks at four points — young-window keyed to ingest today() so undated/old claims read "too new for corroboration" (claims.ts:897, verified), the ask action string collapses primary-new into plain cited delivery (riff.ts:920), attest bypasses the floor (coverage.ts:137), and the researcher charter ships no sweep executor so the orchestrator must forge a --recency record or deadlock the gate. (4) Deep-drill: renderEntry drops thread/depth so "the report replays each drill" is unfulfillable (SKILL.md:606), and primary-new cannot legally enter the ledger the report compiles from (ledger.ts CONFIDENCES rejects it — compiler-charter.md:37). (5) M22 bundle neutralization skipped Section 2 panel/charters (report.ts:250) and the live FINDINGS block (riff-live.ts:255) — the two untreated injection siblings. (6) M11 thinking-SET landed in daemon and page but the adapter (room.ts:446) severs it. (7) I52 — the fix-audit lens's own catch — is dead code (riff.ts:1066, knownIds guard never wired to its only call site). Doc-drift I66-I68 sit outside the code-verification root and are correctly not reported as code defects.

## Per-group summaries

- **claims** (found 8, kept 8): scripts/lib/claims.ts (1041 lines) is the claim-corpus engine after fix pass 2 + deep-drill: number-and-negation-aware dedup with conflicts_with stamps, best-match merge, namespace-scoped atomic source merging, normalizeTier, the CRAG assess() router with answering-claim corroboration, the primary-new/60-day recency machinery with newer-primary promotion, and the new QuestionRecord deep-drill stamps. The prior reviews' fixes (M1, M15, M16, M17, I6, I7, I19, I20, I21, I22, I23) all landed recognizably and the fail-safe bias is real: the promotion path, the unresolved-conflict provisional cap, and the per-hit figure gate behaved correctly under direct adversarial probing. However, every finding below was empirically confirmed by running the module under Bun (RIFF_ROOT fixture sessions), and three are high: (1) the negation guard uses parity (count % 2), so a passive-voice double-negative refutation is dropped as a duplicate and its tier-1 source merged into the claim it refutes — the founding failure class, reintroduced; (2) conflict surfacing is answer-scoped while answers are composed from all strong hits, so a recorded $99-vs-$49 dispute sitting at hits[1]/hits[2] is delivered under a clean "verified" label with conflicting_ids=[]; (3) the primary-new young-window is keyed to newest(as_of, published) with as_of defaulted to today() at ingest, so undated and old-published single-primary claims all read "too new for independent corroboration yet" (charter line 106 promises the opposite) and the aged-decay branch is dead in-session. Mediums: re-ingest silently retargets/erases recorded conflicts_with edges; the I21 id-shape fix skipped the read path (agent-invented "alpha-pricing" ids are corpus-visible, never deduped, invisible to pendingIngest); sameNumbers string-compares tokens so "$99" vs "$99.00" fabricates a conflict and permanently splits corroboration; and a zero-source claim is still delivered "provisional / single source" in violation of I1 (moderatorPick was fixed, assess was not). One low: source merges discard the duplicate's fresher date, so re-confirmation can never clear staleness. The deep-drill additions themselves (QuestionRecord, recordQuestion depth-0 handling, recentQuestions) are clean.

- **riff-cli** (found 9, kept 8): scripts/riff.ts (1297 lines, skill root /Users/zee/Documents/Vibe Coding/ACOS 3.0/.claude/skills/acos-research-riffs) is the 23-command CLI dispatcher for the research-riffs state engine, re-reviewed after fix pass 2 and the deep-drill build. The newest code is mostly well-made: the CONTRACT-2 single-append-fd spawn logging is correct (one openSync("a") descriptor shared by stdout/stderr, closed in finally after the child dups it), the pre-spawn beacon unlink genuinely fixes the M5 provenance race, numericFlag closes the M23 NaN class for values that are present, checkDimension closes M24's shape hole, the --recency/fast_moving wiring flows correctly through coverage init/add/probe into the gate and eval, and the ask --thread/--depth stamping handles depth=0 and empty/value-less --thread correctly with an append-only questions.jsonl that `riff thread` replays cleanly. The defects that remain cluster exactly where the task predicted: both new pid-based liveness judgments (the CONTRACT-1 beacon trust and the new room.pid kill) use a bare kill(pid,0) probe with no process-identity check — despite the codebase having TWO marker-checking siblings (session.ts pidIsLive, riff-live.ts pidIsLiveResponder) with comments explaining pids get recycled — which I empirically confirmed yields a false "ready" room with no responder, and in the kill path can SIGTERM an innocent same-user process; coverage init accepts duplicate dimension ids that wedge the gate (empirically confirmed); the deep-drill deliver-then-deepen protocol double-counts moderator_streak so the moderator fires mid-drill after a single user turn; and the recency ladder's primary-new label gets the verified action string, dropping the mandatory dated-delivery instruction. Three low-severity residues: value-less numeric flags silently no-op (empirically confirmed for --depth), attest bypasses the CONTRACT-7 recency floor creating a gate/eval disagreement, and the now-landed recency field is still read through a type-erasing transitional cast.

- **riff-live** (found 5, kept 4): /Users/zee/Documents/Vibe Coding/ACOS 3.0/.claude/skills/acos-research-riffs/scripts/riff-live.ts (742 lines) is the warm-pool live responder, re-reviewed after fix pass 2. The new machinery verifiably landed: spawn try/catch + existsSync(CLAUDE_BIN) settle a missing binary as a failed beacon instead of a top-level crash (M9); THINKING is a per-seat Set whose appendTurn clears only its own seat (M11); err:true synthetic turns are excluded from both the transcript replay and spokeBefore (M34); the discussion replay is flattened per turn (turnSafe) and fenced in <<<DISCUSSION markers with rules-win framing (M35); the beacon carries a pid suffix, is unlinked on clean shutdown, kept on failure exits, and left untouched on lock-contention exits; the poll loop re-asserts lock ownership every 5s (M7) and shuts down keepStatus when superseded. Exit-path audit: every exit either unlinks the beacon (SIGINT/SIGTERM), leaves an intentional failed: line (API key, missing binary, warmup timeout, all-dead verdict), touches nothing pre-lock (resolve failure, lock contention), or leaves a dead-pid line the CLI unlinks-and-respawns over — the contract holds EXCEPT that riff.ts trusts a beacon on bare pid-aliveness (no riff-live identity check like the lock path has), so a recycled pid turns a crash leftover into a permanent false "ready". Five findings survive, all in fix-pass-2 seams: err failure turns are stripped by the room.ts adapter so the fail-visibly machinery is invisible at the only surface the chair watches; the thinking seats array is likewise dropped by the adapter (room.html's set renderer is dead code, and riff-live.ts:204's "buildIcState renders the set" comment is false); the beacon recycled-pid trust hole; the YOUR FINDINGS block is the one remaining unflattened/unfenced prompt interpolation (chair and transcript are both fenced; dossier claim text is not); and an all-workers-dead-after-ready daemon keeps a live "ready" beacon that makes DEAD_WORKER_TEXT's "restart the room" advice non-actionable through `riff room`. One prior-review item is noted but NOT re-reported as a finding: review 2's riff-live headline (readLoop/onEvent not fenced by proc identity, so a superseded worker's buffered final result can complete the successor's warmup and deliver the warmup "ok" as a seat's answer) is still unfixed in the current file — every sibling review-2 riff-live finding has a landed fix with a marker comment; this one has neither.

- **report** (found 2, kept 2): scripts/lib/report.ts (879 lines) assembles the single-pass report compile bundle, runs the ~14-check mechanical self-eval (coverage, sourcing, delivered-figures-primary-sourced with the new live-room channel, recency-swept, ledger-integrity id-continuity, citation resolution, verdict freshness), and cross-checks a written report's citations against the corpus. The newest code (fix pass 2 + deep-drill) is in good health: the M22 fences (flat/cell/neutralizeLines) genuinely close the claim/ledger/tree/coverage injection vectors, the M4 live-figure scan reuses citationAudit's exact idsCitedIn matcher, M29 chair counting and the M30 id-continuity check are present and correct, and the recency-swept field fix reads the recency_probes field coverage.ts actually writes. Two residual defects: (1) the M22 fence fix skipped Section 2 (Panel and Charters) — seat title/lane/not_lane and history rationale are interpolated raw and UNFENCED, an agent-authored free-text injection into the same bundle that carries the compiler's instructions; (2) chairMessageCount claims to mirror riff-live's dispatch condition but only excludes level-type messages, so an unknown-typed inbox line carrying a seat/text (which riff-live drops without generating a turn) is counted as conversation. No regressions found in the reviewed additions.

- **room-state** (found 8, kept 5): scripts/lib/room.ts (495 lines) recomputes RoomState from the session directory on every read, fingerprints all watched files for the server's push-on-change loop, and adapts riff state into the IC meeting-state shape for room/room.html. The fix-pass-2 work here largely landed correctly and verifiably: turns_total is genuinely monotonic on every adapter path (room-turns.jsonl is append-only with no truncation site anywhere in the codebase; readJsonl tolerates torn trailing lines without un-counting; the fallback branch reports 0 by construction), err-turn exclusion is consistent across riff-live's spokeBefore/transcript, room.ts, and room.html's _isLiveTurn (contract-pinned by tests at test-riff.ts:2915/2998/3042), the ledger fallback carries the neutral "Session log" speaker + fallback tag, computeGate is consumed purely from coverage.ts (one gate implementation, no write side effect), both c.sources readers are Array.isArray-guarded, the thinking marker is freshness-gated failing toward not-thinking, and the reading-level clamp rounds. What remains are three half-landed seams in the newest code: the adapter drops riff-live's concurrent thinking SET (seats[]) that both the daemon comment and room.html's consumer were built around; research_label is sent and test-asserted but room.html never consumes it, so the header still fabricates "N research agents running in the background" (the I41 fabrication the field was created to cure); and the errors[] diagnostic channel dead-ends — no UI consumer renders it, so a degraded build masquerades as a healthy empty room. Plus low-severity residue: buildIcState re-reads allClaims/surfacedIds outside the section() harness, a stale "writeJson is non-atomic" comment contradicting util.ts's now-atomic writeJson, err-turn failure notices never reaching the chair (30s generic watchdog instead of the daemon's instant actionable text), the retired-seat suffix defeating shortLabel's 18-char cap, and an unused TIERS import.

- **room-page** (found 6, kept 5): scripts/room/room.html (735 lines) is the riffs live committee-room page, derived from acos-investment-committee's meeting_template.html. PARITY: room.html vs meeting_template.html differs at exactly the six enumerated relabel lines (6, 150, 156, 210, 255, 260) and meeting_template.html vs meeting.html differs only at the injected MEETING literal (210) — no unenumerated drift; all fix-pass-2 JS is identical across the three copies, so every finding below must be fixed in all three per the re-derive rule. The fix-pass-2 JS largely landed correctly: turns_total detection is genuinely monotonic and its backfill window arithmetic is consistent with the adapter's .slice(-100) cap (refresh, multi-turn growth, and window-overrun cases all verified); the play-start transcript append (M10) makes turns interruption-proof; fallback notes render dimmed exactly once; SSE onerror/onopen, the loud #Name failure, HTTP-reject postCmd, the pre-panel branch, and the seat-fingerprint arc rebuild all match intent. The surviving defects are seam failures between the page's new consumers and what the daemon/adapter actually send: playEvent clears the single global think-watchdog and every seat's thinking class on ANY turn, so a second in-flight call that fails afterwards dies with zero signal (err turns are contractually excluded from the timeline, so nothing ever tells the chair); the thinking-SET contract is severed in the middle (riff-live writes {seat,seats[],ts} and room.html consumes st.thinking.seats, but buildIcState forwards only {seat}); research_label was added adapter-side but has no consumer in the page, so the header still banners claim-counts as "N research agents running"; appendChairLine's last-element-only dedup duplicates and misorders chair lines whenever two questions are queued before an answer; and the adapter's new errors[] degradation channel is never rendered. One comment (line 623) misdocuments CONTRACT-8 by promising dimmed err-entry rendering that can never occur.

- **session-tree-panel-coverage** (found 7, kept 7): session.ts, tree.ts, panel.ts and coverage.ts are the state-engine core after fix pass 2, and the big-ticket fixes verifiably landed: the manifest identity check (loadManifest refuses a directory whose manifest names another session, making saveManifest's id-routed write safe), the superseded-dir rename with a collision loop and a daemon-ownership refusal gate, resolveSession's newest-wins arbitration, atomic writeJson underneath everything, duplicate-slug rejection in validatePanel, the tree's phantom/root/empty-path guards, and the CONTRACT-7 recency floor whose core math is correct at the edges I probed (0 probes can never saturate or be recency-credited, a lone dry recency probe stays thin, agent-saturated + recency behaves as documented, computeGate is genuinely pure and shared with the room). The surviving defects cluster in the seams of the newest code: the force-refusal liveness check treats EPERM as proof of ownership without the ps marker check (the one path where a genuinely dead session deadlocks init --force behind a reboot-recycled pid), the autofile "never fatal" sanitization only covers string inputs while ingest preserves non-string dimension values verbatim, the I52 knownIds guard was built but never wired to its only caller, emitCharter never got the SLUG_RE bar so the panel-add path still scatters charters/dossiers outside the layout, coverage init never checks dimension-id uniqueness (the exact M18 class fixed for panel slugs), and the recency floor can be waived via attestation or the budget cap. All findings verified against exact source lines; both prior reviews checked to avoid re-reporting.

- **server-util-ledger** (found 1, kept 1): riff-server.ts (259 lines) is the loopback SSE bridge; util.ts (216 lines) holds arg parsing, JSONL I/O, atomic writeJson, and lexical scoring; ledger.ts (217 lines) is the append-only ledger with derived supersession plus the new deep-drill thread/depth fields. All fix-pass-2 focus items verified sound by running the real modules: the host allowlist handles [::1] with and without port and fails closed on every exotic IPv6/hostname form (which are unreachable for legitimate use anyway, since the server binds IPv4 127.0.0.1 only — DNS-rebound Hosts 403 before any route, Origin:null fails closed); the exact content-type essence parse is correct and room.html sends `Content-Type: application/json`, so the legit chair channel passes the new 415 gate; the stopword fix landed (go/am retained, trailing-punctuation strip works, c++/node.js/api.z.ai survive); atomic writeJson is correct — the tmp file is a same-directory sibling (never cross-device for renameSync), tmp names are pid-keyed so concurrent processes cannot collide, and no directory scan (dossierFiles' endsWith filter, allSessions' isDirectory filter, the fingerprint stat list) can ever misread a crash-leftover .tmp file; and addEntry's thread/depth validation is solid, including the explicit depth-0-is-falsy spread guard, with riff.ts passing whole payloads through so the new fields are never silently stripped. One confirmed defect survives fresh adversarial eyes: chains() silently drops any correction that supersedes a LOSING superseder of a recorded conflict — empirically confirmed against the real module (L-0004 superseding loser L-0002 appears in no chain while view() correctly marks L-0002 superseded), contradicting the guarantee stated in the comment directly above the code, and untested (test-riff.ts:733-761 covers the conflict shape but never supersedes a loser — the exact combination REVIEW-2026-07-25 P25 asked to add once the conflict model landed).

- **tests** (found 5, kept 3): scripts/test-riff.ts is now a 3,851-line sequential e2e suite (480 checks) driving the real riff.ts CLI, the SSE room server (including raw-TCP Host-allowlist probes via a hand-rolled rawHttp helper), and a stubbed-claude riff-live daemon against a throwaway mkdtemp root, plus lib-level claims/coverage/panel imports for contracts the CLI does not expose. The fix-pass-2 additions are largely excellent: the prior reviews' test items verifiably landed (SIGINT/SIGTERM reaper, readOr for I57, raced SSE reads, stub argv logging for I3, the I42 grounding and routeToSeat assertions, same-origin accept branch, exact eval-check-id set, running-count decoupling via lib calls), the worker-death tests genuinely exercise riff-live's exit-observer and RIFF_JOB_TIMEOUT_MS watchdog paths (not just the stub — STUB_HANG_NOW keeps the worker alive so only the daemon's watchdog can produce the asserted err turn), the rawHttp helper is leak-safe (deadline-raced, end() on all paths, loopback-only against a verified-up server), and the deep-drill block asserts the exact stamp format in every variant (full, thread-only, flagless, not-in-corpus) plus falsy-zero guards, record-shape absence, ledger round-trip, and id-burn contiguity. Five residual defects survive adversarial reading, concentrated in the newest blocks: a watchdog-timing check whose bound (30s) is looser than its own poll deadline (25s) so it can never fail for the reason it names; zero coverage of riff-live's entire give-up arm (3-fast-exits, the new I14 windowed give-up, the new M9 spawn-throw path, submit-on-dead — DEAD_WORKER_TEXT appears nowhere in the suite); an atomic-writeJson race check that passes vacuously if the hammer child crashes partway (exit code and final value never asserted, reader loop unbounded); no test of the new M11 concurrent thinking-marker shape ({seat, seats:[...]}, own-seat-only clear); and the review-2 M8 regression fixture (in-namespace id with a missing sources key vs riff room --state) that was never written — every raw fixture carries sources: [].

- **skill-md-templates** (found 9, kept 9): Reviewed SKILL.md (1014 lines, post fix-pass-2 + deep-drill rewrite), all six assigned templates, and agents/riff-researcher.md, after reading both prior review summaries and the two design docs. Every prior-review item touching these files verifiably landed (auditor category-sweep/PASS rule, attest signal, citation-id contract, ask-label table rewording, conflicts protocol, descope interim, preflight explicit-id resume, eval-rubric inputs, riff-researcher independence scoping and model-pin removal). I also verified the new Phase 4 drill protocol's mechanical claims against the engine: the stamp format/segments, thread listing with `deepest` and empty-history behavior, --depth 0-2 validation, moderator cadence (MODERATOR_L=2, standard mode only), tier table vs TIERS, fast_moving default-true and the recency saturation floor, the 60-day PRIMARY_NEW window, the versioned-figure newer-primary tiebreak (claims.ts:756), ask output fields (stamp, corroborating_sources, numeric_unprimaried_ids, recency, moderator_due) — all match. The nine surviving findings are seams the parallel edits opened, not engine bugs: the recency-probe accounting credits panel seats with last-90-days sweeps their charter never instructs (so the floor either deadlocks the gate or teaches record forgery); the primary-new label cannot legally enter the ledger the report is compiled from; the label table's "offer to deepen" contradicts the ladder's mandatory auto-escalation for provisional; and the drill-replay promise is unfulfillable because renderEntry drops thread/depth from the compile bundle. Remainder are low-severity internal contradictions and vocabulary gaps.

- **lens-security** (found 2, kept 2): Third-pass SECURITY-lens review of the three newest-touched files: /Users/zee/Documents/Vibe Coding/ACOS 3.0/.claude/skills/acos-research-riffs/scripts/riff-server.ts, /Users/zee/Documents/Vibe Coding/ACOS 3.0/.claude/skills/acos-research-riffs/scripts/riff-live.ts, and /Users/zee/Documents/Vibe Coding/ACOS 3.0/.claude/skills/acos-research-riffs/scripts/lib/report.ts. I read every line, read the SKILL.md Invariants (I1-I12), and both prior reviews (2026-07-25, 2026-07-27) to avoid re-reporting their items.

DEFENSES THAT HELD (tried to defeat, could not): (1) Host allowlist isLoopbackHost() — exact-match set {127.0.0.1, localhost, [::1]} with port/bracket stripping; no non-loopback host can be coerced to match, and rebinding names (evil.com resolving to 127.0.0.1) carry a non-loopback Host and are 403'd. (2) Content-Type gate now parses essence (split ';' + trim + lowercase, strict ===) so a CORS-safelisted simple request can never satisfy application/json — preflight is genuinely forced; OPTIONS is unhandled (404) so cross-origin JSON is blocked. (3) Origin check compares against the fixed allowlist, not Host-derived url.origin (M20 fix good); null origin is rejected. (4) Bundle fence escapes: sections 5/7 are flat()'d (no newline can introduce a line-leading ``` to close the fence), section 3 outline runs neutralizeLines (line-leading #, ---, ```, ~~~ prefixed with '· '), section 4 uses cell() pipe-escaping. No fence-close or column-shift escape found. (5) Beacon/lock protocol: setStatus stamps pid, lock uses wx-atomic acquire with pid-liveness + ps-command verification, in-poll lock re-assertion; no riff-live-side beacon spoof or double-daemon hole found. (6) readJsonl tolerates missing files, so CONTRACT-8's new room-turns/chair-inbox reads don't crash CLI-only sessions.

TWO GENUINE GAPS FOUND — both the same defense (flatten web-derived free text before it lands next to instructions) applied unevenly, and both MISSED by prior reviews: the live-prompt FINDINGS block (riff-live) and report-bundle section 2 panel (report.ts) are the two untreated siblings of the chair/replay/claim-body fixes.

- **lens-coherence** (found 3, kept 2): Coherence-lens third pass over SKILL.md + riff.ts, cross-checked against the newest lib code (claims.ts, coverage.ts, ledger.ts, session.ts, report.ts) and ARCHITECTURE.md notes 20-22. The deep-drill and recency surfaces are mechanically faithful: --thread/--depth are validated and echoed exactly as documented (including the falsy depth-0 path, guarded by `!== undefined` at riff.ts:904/911), the preformatted stamp `[thread T · depth L<n> · <corroborating_sources> sources · <label>]` matches all four worked-example stamps, `riff thread` returns {ts,depth,question,label}+deepest as SKILL claims, --recency gates fast-moving saturation with fast_moving defaulting true, PRIMARY_NEW_WINDOW_DAYS=60 matches the 60-day label, every eval check id SKILL names exists in report.ts, all 10 ledger ENTRY_TYPES (incl. answer/note) are valid, MODERATOR_L=2, and the M23/M19 resume-id guidance is correctly updated. Three low-severity residuals: (1) SKILL's ingest `conflicts` description is figure-only but the code also keeps negation-parity conflicts; (2) the ask-output `action` field collapses primary-new into the verified instruction, dropping the dated-delivery guidance the ask table prescribes; (3) ARCHITECTURE note 21 still states "None of review 2's fixes are applied yet", contradicted by note 22. No high/medium coherence defects found; prior-review items were not re-reported.

- **fix-audit** (found 1, kept 0): Audited the full acos-research-riffs skill against REVIEW-2026-07-27.md (M1-M36, I1-I69), ARCHITECTURE.md note 22 (the 8 pre-defined cross-file contracts CONTRACT-1..8), DEEP-DRILL-DESIGN.md (stamp/thread/depth ladder), and RECENCY-DESIGN-PROPOSAL.md (CONTRACT-7). Read every production file line-by-line: riff.ts, riff-live.ts, riff-server.ts, room/room.html, and all 8 lib/*.ts (claims, coverage, ledger, report, room, panel, tree, session, util), plus SKILL.md, all templates, and agents/riff-researcher.md.

The fix pass is remarkably thorough. All 36 must-fix items landed and hold in the current code (M1 per-claim corroboration excluding disputants, M2 negation parity, M5/M6/M7 daemon liveness + shared append-fd log + init --force daemon guard, M8/M12/M13 room adapter, M14 manifest id-routing, M15/M16/M17 dedup+tier, M18 dup-slug, M19 explicit-id resume in SKILL, M20/M21 CSRF host allowlist + media-type essence, M22 bundle neutralization, M23 numericFlag, M24/M25 coverage shape+force, M26/M27/M28 charters, M29/M30 live-room-aware eval + ledger-integrity, M31/M32 verify-first plumbing, M33/M34/M35/M36 tree/live/util). All 8 contracts verified. The recency policy (CONTRACT-7) is implemented end-to-end: primary-new label with 60-day mechanical decay, fast_moving recency floor blocking saturation, recency-swept eval reading recency_probes, I11, and newer-primary-outranks-count on versioned conflicts. The deep-drill stamp contract is wired (optional thread/depth on ledger and questions.jsonl, ask --thread/--depth echo + preformatted stamp, riff thread with deepest). The documented known-good deviations (M4 strict-cap, questions.jsonl label, recordQuestion-after-assess) were respected. The stamp dropping the design's "N probes" field is documented as-built in note 22 and SKILL.md consistently, so not reported. I69 (tsconfig comment) is fixed. Doc-drift items I66-I68 persist in planning/ARCHITECTURE.md (note-22 Files block still omits scripts/riff-live.ts; line 251 still says 146 assertions vs 320/442/480 elsewhere) but sit OUTSIDE the specified code-verification root (.claude/skills/acos-research-riffs) and are planning-doc drift, not shipped-code defects, so not reported as findings. One genuine in-scope incomplete fix found: I52.

## Must-fix (18)

### M1. `scripts/lib/claims.ts:174` — Negation parity uses count%2, so a double-negative refutation reads as non-negated and is dropped as a duplicate of the positive claim it refutes, merging its tier-1 refuting source into the refuted (wrong) claim.

- **Category:** bug
- **Why:** Verified at source: negationParity returns match.length % 2, and sameNegation compares the parities; the comment at 165-170 promises differing parity is a conflict to keep, but two negations (parity 0) equal zero negations (parity 0). 'X is not available and Y is not supported' vs 'X is available and Y is supported' has identical stopword-stripped tokens and empty number sets, so the M2 guard passes it as a duplicate and the refuted claim is upgraded to tier 1 by the very source refuting it — the founding wrong-claim-as-verified failure class, reintroduced on common two-clause phrasing.
- **Fix:** Compare negation COUNT, not parity: make sameNegation require equal counts (any count difference between near-duplicates is a conflict to keep), and optionally compare the multiset of negated head tokens to catch equal-count swaps.

### M2. `scripts/lib/claims.ts:796` — Conflict surfacing and the unresolved-provisional cap are judged only against the answering claim (hits[0]) while the answer is composed from ALL strong hits, so a recorded figure dispute between two supporting strong hits is delivered under a 'verified' label with conflicting_ids empty and no cap.

- **Category:** bug
- **Why:** Confirmed: conflictingIds (line 796) and unresolved (833-836) both filter inConflict(answer, h) only, but corroboration is pooled across the whole strong-hit set (780-794). An echo-001($99)/echo-002($49) recorded conflict pair, both strong hits not at position 0, returns label 'verified', conflicting_ids [], reason 'N sources corroborate'. The file's own design comment says trust judgments must run over all strong hits because the answer is composed from them, and applies that to figures — but not to conflicts. An orchestrator quoting the pricing part picks $99 or $49 with zero dispute signal.
- **Fix:** Detect recorded conflicts pairwise across the strong set, not just against hits[0]: surface them in the Sufficiency result and reason string, and cap the label at provisional (or emit per-hit quoting gates the way numeric_unprimaried_ids already does).

### M3. `scripts/lib/claims.ts:897` — The primary-new young-window is keyed to newest(as_of, published) while ingest defaults as_of to today(), so an UNDATED claim and a years-old published claim both read 'young' and are delivered primary-new with a fabricated 'too new for corroboration yet' narrative, contradicting the charter and making the aged-decay branch unreachable in-session.

- **Category:** bug
- **Why:** Confirmed: young = answerDate !== null && Date.now() - answerDate.ms <= window, and newestDate takes the newer of as_of/published where as_of defaults to today() at ingest. A dateless claim and a published:'2019-03-14' claim both get primary-new, while researcher-charter.md 106-107 explicitly promise an undated claim cannot get that treatment. Every claim is captured this session, so virtually every single-primary claim qualifies as young; the '1 source, aged -> re-probe' row and 60-day decay are dead code, and 'too new for corroboration' is factually wrong for old facts.
- **Fix:** Judge youth from the fact's age, not capture time: use published ?? researcher-supplied as_of, never letting the ingest today() default qualify (record ingested_at separately); treat dateless answers as not-young so they stay provisional, matching the charter promise.

### M4. `scripts/lib/claims.ts:540` — ingestFile rebuilds each claim without carrying over its existing conflicts_with array, recomputing a single closest-match edge instead — so a routine probe-append + re-ingest silently retargets or erases recorded disputes.

- **Category:** bug
- **Why:** Confirmed empirically: gamma-001($99) stamped conflicts_with [beta-001]($49); after hotel-001($79) joined and a probe appended one line to gamma's dossier, re-ingesting rewrote gamma-001 with conflicts_with [hotel-001] — the $99-vs-$49 edge is gone corpus-wide (beta-001 never carried the reciprocal). inConflict(gamma-001, beta-001) is now false, so the cap and conflicting_ids no longer see the dispute; gamma can read verified while a recorded disagreement was silently un-recorded. Probe-append-then-re-ingest is the standard Phase-4 loop, and the string[] array can never hold more than one edge because both write sites store cands[0] only.
- **Fix:** In ingestFile and addClaims claim construction, union carried-over raw.conflicts_with (filtered to ids still in the corpus) with the recomputed cands[0] edge, and report only NEW edges so re-ingest does not duplicate ledger notes.

### M5. `scripts/lib/claims.ts:84` — The I21 full-id-shape fix (^slug-\d{3,}$) landed only at the two write sites; allClaims (line 84) and pendingIngest (line 104) still use bare startsWith, so an agent-written non-numeric in-namespace id enters the corpus without passing ingest and is invisible to the un-ingested safety net.

- **Category:** fix-incomplete
- **Why:** Confirmed: a dossier line with id 'alpha-pricing' (never ingested) appears in allClaims, pendingIngest returns [] (same startsWith concludes not-pending), and assess answers from it — delivered primary-new with primary_sourced:true. Such claims bypass dedup, conflict stamping, id assignment, and tier normalization entirely; the line-75 comment claims this spoof path is closed but it remains open for the agent's own namespace, and the report's never-ingested safety net is blind to it.
- **Fix:** Apply the same idShape regex (^${escapeRegExp(slug)}-\d{3,}$) in allClaims, and make pendingIngest count rows whose id fails that shape as pending — read and write paths must agree on what a corpus id is.

### M6. `scripts/lib/claims.ts:162` — sameNumbers compares numeric tokens as raw strings, so numerically identical figures in different formats ('$99' vs '$99.00', '0.5' vs '.5', '1.0' vs '1') read as DIFFERENT — fabricating a conflict between agreeing sources and permanently blocking corroboration between them.

- **Category:** bug
- **Why:** Confirmed at line 162 (na.every((t,i)=>t===nb[i])): adding '$99.00' after '$99' returns conflicts instead of a duplicate merge. The assess corroboration gate uses the same sameNumbers, so two agreeing primary sources can never corroborate (verified unreachable for that fact), the unresolved cap can pin the pair at provisional telling the user 'corpus disagrees', and the recency path can narrate 'newer primary wins' between two sources stating the identical price. Decimal-format variance across vendor pages is routine, so this systematically splits corroboration.
- **Fix:** Compare per-position numeric VALUES: parse each token with Number() and treat tokens as equal when both are finite and numerically equal, falling back to string equality otherwise, in numericTokens/sameNumbers.

### M7. `scripts/lib/claims.ts:922` — assess delivers a zero-source claim as 'provisional' with reason 'single source — corroboration missing' — an unprovenanced answer handed to the orchestrator for delivery, violating invariant I1, with a reason that misstates zero sources as one.

- **Category:** contract-violation
- **Why:** Confirmed: a claim with sources:[] falls through 918-923 (corroborating<=1 includes 0) and is returned as the answering hit with label 'provisional', corroborating_sources 0. I1 requires every delivered claim to carry provenance; there is nothing to cite. The prior review fixed exactly this in moderatorPick (sources-length filter) but the ask path was never given the same guard; the charter only excludes sourceless claims from the final REPORT, leaving live Phase-4 delivery as the leak.
- **Fix:** Filter sourceless claims out of the answering set (in search() or at the top of assess), or return not-in-corpus with a probe dispatch when the best hit carries no sources — mirroring the moderatorPick fix; also correct the reason so 0 sources is not labeled 'single source'.

### M8. `scripts/riff.ts:920` — The ask output's action field collapses primary-new into the verified branch ('answer from the corpus with citations'), dropping the CONTRACT-7 requirement that primary-new answers be delivered DATED, never as settled fact. [merged: riff-cli + lens-coherence]

- **Category:** contract-violation
- **Why:** SKILL.md 526 specifies the primary-new action as 'Answer WITH the date — per <vendor>'s release notes of <date>; too new for independent corroboration yet', and the engine's principle is that machine-emitted fields, not narration, drive delivery. An orchestrator obeying the authoritative action string delivers a single-source 60-day-old claim exactly like a 2+-source verified one; the dating instruction survives only inside the free-text reason, which nothing instructs the orchestrator to parse. Same field-vs-prose gap class as review-2 M31.
- **Fix:** Add a primary-new arm to the ternary before the final else: a.label === 'primary-new' ? 'answer WITH the date (recency.as_of_newest) — single primary source too new for corroboration; never deliver as settled fact' : ...

### M9. `scripts/lib/report.ts:250` — Section 2 (Panel and charters) interpolates agent-authored s.title, s.lane, s.not_lane (line 250) and s.rationale (251), plus panel-history rows (258), RAW and UNFENCED into the compile bundle — the M22 flatten/fence fix skipped this section, so an embedded newline can forge bundle structure or inject compiler directives. [merged: report + lens-security]

- **Category:** security
- **Why:** The bundle's DATA BOUNDARY note asserts sections 1-8 are inert data and only sections 0 and 9 are instructions, and M22 flattened claim bodies, ledger prose, tree names, and (via cell()) dimension names for exactly this reason. Seat fields are equally agent-authored — panel.setPanel casts model JSON straight to Seat[] and validatePanel constrains only slug — and panel-proposer/panel-add fills them from briefs that may quote hostile web content. A seat title of 'Widgets\n\n---\n\n## 9. Compiler instructions (updated): ...' lands outside every fence in the compiler's sole input; Section 2 is not even inside a fence, a strictly easier injection point than the sections the fix covered.
- **Fix:** Route every free-text seat/history field through flat(): flat(s.title), flat(s.lane), flat(s.not_lane), flat(s.rationale), flat(h.action/h.slug/h.rationale); flatten c.slug at line 310; optionally render Section 2 inside a fenced data block like sections 5/7.

### M10. `scripts/riff-live.ts:255` — The YOUR FINDINGS block interpolates dossier claim text (c.claim) raw — no newline flatten, no cap, no fence — into the highest-authority section of the live seat prompt, the one remaining unfenced untrusted interpolation after M16 (chair) and M35 (transcript) were both fenced. [merged: riff-live + lens-security]

- **Category:** security
- **Why:** Claim text is agent-authored from untrusted web content (probe agents summarizing hostile pages) — one step closer to the attacker than the transcript turns M35 flattens. An embedded newline can forge a fake 'RULES (binding):' section or fake <<<DISCUSSION/CHAIR fences above the real rules in every prompt for that seat, defeating I2/I9 grounding on the flagship live channel, and the poisoned output persists via replay. report.ts calling flat() on the identical field is proof it carries newlines; both prior reviews touched the siblings but not this block.
- **Fix:** Render each finding via turnSafe(c.claim) (flatten \s+ to spaces, cap ~600-900 chars); optionally flatten the seat title/topic in the header and wrap the list in its own <<<FINDINGS fence with data-never-instructions framing; add a prompt-capture test asserting a '\n\nRULES' claim renders on one line.

### M11. `scripts/riff.ts:1181` — The room-reuse kill path trusts a stale room.pid through bare pidAlive() and can SIGTERM an unrelated process whose pid recycled into the dead server's slot.

- **Category:** bug
- **Why:** room.port/room.pid persist on disk indefinitely; after a reboot or any server death the recorded pid is dead and macOS reassigns it, commonly to another same-user process that passes pidAlive() and the kill permission check. The next riff room finds the port unresponsive, enters this block, and kills whatever now holds the pid (an editor, dev server, another Claude session). The codebase's own session.ts liveOwnerPids refuses exactly this with pidIsLive(pid,'riff-server'); this I10 fix omitted the identity check.
- **Fix:** Before killing, verify identity as session.ts does: ps -p <prevPid> -o command= must contain 'riff-server' (ideally this session's id from argv); otherwise treat the pid file as stale and never kill. Extract and share a pidIsLive(pid, marker) helper.

### M12. `scripts/riff.ts:150` — CONTRACT-1 beacon trust keys on bare pid liveness (ensureLiveResponder), so a crash-relic 'ready pid=N' beacon whose pid recycled into any live same-user process makes the CLI report 'ready' and skip the spawn — a live-looking room with no responder. [merged: riff-cli + riff-live]

- **Category:** fix-incomplete
- **Why:** Empirically reproduced: a beacon naming a live sleep pid yields output 'live: ready' with no room-live.lock and no daemon spawned; clicks land in chair-inbox unanswered with zero diagnostics, persistently until the beacon is hand-deleted. Only clean shutdown unlinks the beacon, so SIGKILL/crash/reboot leave it; after reboot pid recycling is near-certain. This reintroduces the M5/M19 misleading-liveness class the CONTRACT-1 rewrite exists to close; the comment's 'a beacon whose pid is alive belongs to the daemon' is false under pid reuse. The suite only tests a DEAD relic pid. Note riff.ts pidAlive also inverts EPERM (treats as dead) vs both siblings (alive).
- **Fix:** Gate beacon trust on identity: require ps -p <pid> -o command= to contain 'riff-live' (mirroring pidIsLiveResponder/pidIsLive), EPERM treated as alive-but-unverifiable; on mismatch unlink the relic and spawn. Add a recycled-pid ('ready pid=1' = launchd) fixture asserting respawn.

### M13. `scripts/riff.ts:520` — coverage init (riff.ts:520 checkDimension loop) and initCoverage (coverage.ts:114) validate each dimension's shape but never cross-check ids, so a duplicated dimension id persists twice and permanently wedges the gate with only destructive --force recovery. [merged: riff-cli + session]

- **Category:** robustness
- **Why:** Empirically confirmed: [{id:'dup-dim',name:'A'},{id:'dup-dim',name:'B'}] initializes 2 dimensions; every verb resolves with find() (first match wins) so the second copy can never be probed/attested/saturated, and after A saturates the gate stays closed with the self-contradicting reason '1 dimension(s) still open: dup-dim' while coverage show lists it saturated. Duplicate ids are a plausible LLM authoring slip — the exact class M18 closed for panel slugs, left open on the payload that drives the gate; the only escape (--force) discards the whole probe record.
- **Fix:** After the shape loop in initCoverage, collect ids and throw naming any duplicates; mirror the same check in riff.ts's coverage init handler so the error carries the CLI's expected-shape hint.

### M14. `scripts/riff-live.ts:393` — When the I14 slow-death breaker settles EVERY worker dead after warmup succeeded (e.g. OAuth expiring mid-session), giveUp keeps the daemon running with the beacon still 'ready pid=<live>', so riff room keeps reporting ready and early-returns — making DEAD_WORKER_TEXT's own 'restart the room' advice non-actionable without a manual kill.

- **Category:** robustness
- **Why:** The all-dead verdict is only checked pre-ready (waitReady's I11 early-false); post-ready giveUp neither updates the beacon nor exits. Every subsequent click burns a dispatch into an err turn that (per the room.ts:411 exclusion) never displays, so the flagship UI degrades to clicks-do-nothing while the CLI actively claims 'live room: click a seat and it answers'. The exitTimes path is new fix-pass-2 code, and long-lived chair sessions are the design case.
- **Fix:** In giveUp, when every worker in the pool is dead, setStatus(`failed:${reason}`) and shutdown({ keepStatus: true }); the beacon contract then lets the next riff room see the dead-pid failed line, unlink it, and respawn.

### M15. `scripts/lib/tree.ts:150` — The M33 autofile sanitization only survives STRING dimension values: ingest preserves raw.dimension with no typeof check, so a truthy non-string (an array like ['pricing','latency'] — a plausible LLM shape for a claim spanning two dimensions — or a number) reaches this line and throws 'raw.split is not a function', aborting autofile half-filed.

- **Category:** fix-incomplete
- **Why:** The comment above promises 'Never fatal', and the M33 failure mode (a bad value persisting in the canonically-rewritten dossier so the crash recurs every run) is fully reproduced for the non-string case: the value survives ingest's rewrite, allClaims returns it, and every riff tree autofile run dies mid-loop with a TypeError naming no claim, leaving the tree half-filed before the report compile.
- **Fix:** Harden both ends: here, const s = typeof raw === 'string' ? raw : '' (count a present non-string in defaulted, or join string arrays with '/'); in claims.ts ingest/addClaims only preserve dimension when typeof raw.dimension === 'string'. Add a fixture with dimension:['a'] asserting autofile completes and reports it defaulted.

### M16. `scripts/lib/ledger.ts:193` — chains() pushes a conflict's losing superseder into the chain but never follows the loser's OWN supersession branch, so a correction targeting that loser appears in NO chain at all — an active, ledgered reversal silently vanishes from the delivered supersession record.

- **Category:** bug
- **Why:** Empirically confirmed against the real module: L-0001 superseded by L-0002 and L-0003, then L-0004 superseding L-0002, yields the single chain [L-0001,L-0002,L-0003] with active L-0004 absent, while view() correctly marks L-0002 superseded_by L-0004. The comment two lines above promises 'no recorded reversal disappears from decisions and reversals', and chains() feeds exactly that report bundle section (report.ts:152) plus riff ledger chains — an I3 evidence-trail integrity hole. The suite asserts the conflict shape but never supersedes a loser, the exact combination REVIEW-2026-07-25 P25 asked to add.
- **Fix:** When pushing a conflict loser, also walk its own superseded_by branch (including its conflict losers) with a visited set shared with the main walk; or treat chain membership as full DAG reachability from each head, sorted by ledger ordinal. Add the missing regression test (supersede the losing superseder, assert all four ids in one chain).

### M17. `templates/researcher-charter.md:54` — The researcher charter provides no recency-sweep executor (no 90-day instruction in METHOD, no recency field in RETURN VALUE), so Phase 2's seat-attributed --recency recordings are not backed by the seat's task spec — the orchestrator must either stamp a sweep that never ran (fail-unsafe past both gate and eval) or block every fast-moving dimension.

- **Category:** fix-incomplete
- **Why:** SKILL.md 1.3 defaults every dimension fast_moving:true and forbids saturation until a recency probe is recorded; Phase 2 instructs recording coverage probe --recency attributing the sweep to the seat. But the charter (the seat's complete task spec) has METHOD steps that stop at 'confirm latest version' with no last-90-days restriction and no sweep-report field. So the orchestrator either forges --recency records that falsely satisfy recomputeStatus AND the eval's recency-swept check (fail-unsafe in exactly the direction CONTRACT-7 exists to prevent), or every fast-moving dimension deadlocks the gate with no documented dedicated-probe path at panel/gate time.
- **Fix:** Add a METHOD step: for each fast-moving dimension, run at least one search restricted to the last 90 days, record window+queries in the dossier's negative-space section, and report recency_swept:[<dim-id>] in RETURN VALUE; have Phase 2 key --recency off that field. Alternatively amend Phase 3 to dispatch one dedicated recency probe (riff render probe) per unswept fast-moving dimension before the gate.

### M18. `templates/compiler-charter.md:37` — The charter tells the compiler to carry primary-new through from the record, but the ledger the bundle is compiled from structurally cannot hold that label: ledger.ts CONFIDENCES is [verified,provisional,not-in-corpus,n/a] and addEntry throws on anything else.

- **Category:** contract-violation
- **Why:** AssessLabel extends Confidence with primary-new, but nothing tells the orchestrator how to ledger a primary-new finding. SKILL.md Phase 4 step 5 mandates ledgering every turn with a confidence; an orchestrator faithfully recording the delivered label gets a hard fail ('unknown confidence "primary-new"'), while its legal options misstate the state (verified overclaims, provisional is the hedge I11 removed). The compiler then finds no primary-new label to keep dated, so this bullet is unexecutable and the documented workflow crashes.
- **Fix:** Either add 'primary-new' to CONFIDENCES in ledger.ts (documenting that ask-time decay does not retro-edit entries, per I3), or document the mapping in SKILL.md step 5 and this charter: ledger primary-new as provisional with the dated per-vendor framing in body and as_of in provenance, and have the compiler derive primary-new state from claim dates/tier/corroboration in section 7.

## Improvements (30)

### I1. `scripts/lib/coverage.ts:137` — Attestation short-circuits ahead of the CONTRACT-7 recency floor (recomputeStatus returns 'attested' before computing recencySwept), so an auditor can attest a never-recency-swept fast-moving dimension into gate-ready state while riff eval's recency-swept check hard-fails that same state. [merged: riff-cli + session]

- **Category:** contract-violation
- **Why:** riff coverage attest (only guard: probes>0) lets judgment substitute for the recent-window sweep the initCoverage comment says no grading rule can replace, producing an M28-class gate/eval disagreement (gate passes, report.ts:545 fails). Fails eventually-safe because eval catches it at Phase 5, but the gate is the phase boundary the floor was designed to hold, and the whole riff conversed over an unswept corpus.
- **Fix:** In attest(), refuse a fast-moving dimension with (recency_probes ?? 0) === 0 (message: run one coverage probe <id> --recency first; 'nothing new in the window' counts as its result), or require an explicit --force with a ledgered warning; surface the debt in computeGate's ready-side reason for capped fast-moving dims.

### I2. `scripts/lib/room.ts:446` — buildIcState drops the concurrent thinking SET: riff-live writes {seat, seats:[...], ts} and its comment says 'buildIcState renders the set', room.html:657-662 consumes st.thinking.seats, but the adapter forwards only {seat} and IcState.thinking is typed {seat:number} — the seats array never reaches the page. [merged: riff-live + room-state + room-page]

- **Category:** fix-incomplete
- **Why:** Under the two-model concurrency M11 targets, the room can only pulse the newest seat; an earlier in-flight seat's 'gathering their point' spinner vanishes mid-generation, so the chair may re-click and double-burn a generation. The daemon half and page half both shipped; the adapter in between silently voids the M11 fix, and the test at test-riff.ts:1392 writes a marker without a seats array so the gap is invisible.
- **Fix:** Type IcState.thinking as {seat:number; seats?:number[]} and set thinking = { seat: th.seat, seats: Array.isArray(th.seats) ? th.seats.filter(n=>typeof n==='number') : [] } after the freshness gate; add a two-seat buildIcState test asserting both surface.

### I3. `scripts/room/room.html:352` — playEvent clears the single global think-watchdog and wipes every seat's thinking class on ANY arriving turn, so when two calls are in flight the first seat's answer silently disarms the still-pending seat's watchdog and spinner — and because CONTRACT-8 excludes err turns from the timeline, a subsequent failure of the pending seat produces no page-visible event at all.

- **Category:** bug
- **Why:** Verified trace: chair calls A then B; A's turn lands -> playEvent clears the watchdog armed for B and strips B's thinking class; B's failure appends an err turn that never enters the timeline, so the SSE push changes nothing. Net: no watchdog, no spinner, no diagnostic — the chair waits forever, violating the page's own note that a dead responder must be distinguishable from a slow one. Review-2 M3 said to clear the watchdog for THE THINKING SEAT; the implementation clears on any turn. Applies to all three copies per the re-derive rule.
- **Fix:** Track the watched seat (startThinkWatch(n)) and only clearThinkWatch when ev.seat matches; after playEvent, re-apply st.thinking for seats still in the fresh marker. Apply identically to meeting_template.html and meeting.html.

### I4. `scripts/room/room.html:347` — appendChairLine dedupes only against the LAST transcript element, so whenever the chair sends a second question before the first answer arrives, every answer's chair-passthrough re-appends a duplicate, out-of-order chair line.

- **Category:** bug
- **Why:** Verified: chair sends q1 then q2 (both echoed); answer1 arrives with chair:q1 -> lastElementChild is q2 -> dedup misses -> transcript reads q1,q2,q1(dup),answer1; answer2 then re-appends q2 after answer1. Every queued exchange duplicates chair lines and scrambles question->answer adjacency in the visible meeting record; a dimmed session-log note between echo and answer breaks it the same way. Applies to all three copies.
- **Fix:** Dedup against the rendered record, not adjacency: scan for any existing [data-chair] with the same text and skip re-append, pairing via consumption (mark the matched echo consumed) to preserve legitimate repeats.

### I5. `scripts/test-riff.ts:3487` — The atomic-writeJson race check passes vacuously when the hammer child crashes or exits partway: neither hammerProc.exitCode nor the final written value ({n:400}) is asserted, the piped stderr is never read, and the pre-written {n:0} file alone satisfies reads>0 with tornReads===0; the reader loop is also unbounded.

- **Category:** test-gap
- **Why:** The realistic writeJson regression this test hunts (a tmp-write/renameSync race that throws mid-burst) is precisely a mid-run child crash, which this check converts into a silent pass. The unbounded while(hammerProc.exitCode===null) at 3475 also hangs the whole suite on a stuck child instead of registering one FAIL — the unraced-wait class I55 fixed elsewhere.
- **Fix:** After await hammerProc.exited, assert hammerProc.exitCode===0 && JSON.parse(read(wjPath)).n===400; bound the reader loop with a ~30s deadline that kills the hammer and fails; surface piped stderr on failure.

### I6. `SKILL.md:527` — The label table and the engine's echoed action string say a provisional answer means 'offer to deepen', while the deep-drill escalation triggers forty lines earlier mandate automatic escalation ('provisional qualifies always') with deliver-then-deepen dispatching the ladder probes, no offer.

- **Category:** docs-drift
- **Why:** Every distinct root question opens a thread, so every provisional answer is inside the ladder's scope; the two instructions conflict on the single most common label — one orchestrator asks permission (burning a turn, contradicting the worked example), another spends probe tokens unprompted, and the engine's action output sides with the retired 'offer' behavior, steering the model against the new protocol. A parallel-edit contradiction (the table row and action string predate the deep-drill rewrite).
- **Fix:** Align the table row and riff.ts's provisional action string with the ladder: 'Answer, say it is provisional, then escalate — dispatch the L1 sweep and deliver the deepened answer in-thread (I12)'. If cost-consent is wanted, state it once as an explicit exception in the escalation-triggers bullet.

### I7. `SKILL.md:606` — The promise that carrying thread/depth in ledger entries 'lets the report replay each drill' is mechanically unfulfillable: report.ts renderEntry (113-143) drops thread and depth, and the compile bundle includes neither field nor questions.jsonl.

- **Category:** fix-incomplete
- **Why:** The compiler charter binds the writer to ONE input, so fields absent from the bundle do not exist for the report. The orchestrator pays the per-turn discipline of stamping thread/depth, ledger.ts validates and stores them, and the only consumer the doc names (drill replay in the report) never sees them. The deep-drill design's change table listed no report.ts change — the seam the parallel edits left open.
- **Fix:** Render the fields in renderEntry (append '**Drill:** thread T2 · depth L1' when either is present) — one line makes the bundle claim true; optionally add a 'Drill threads' bundle section from questions.jsonl. Until then, soften the sentence to say the fields are recorded for riff thread replay, not for the report.

### I8. `scripts/lib/claims.ts:267` — Duplicate merges union only sources and discard the dropped duplicate's fresher as_of/published, so a claim re-confirmed today still reads stale forever and the delivered 'as of' date is wrong.

- **Category:** bug
- **Why:** Confirmed: unionSources (208-220) advances sources but not dates; a 90-day-old claim re-confirmed today merges the source (verified) but assess still reports stale:true with a 90-day-old as_of. Staleness can never be cleared by re-confirmation — every ask re-flags stale, inviting an endless re-probe loop whose results are dropped as duplicates again. Fail-safe (over-hedging) hence low, but it defeats the purpose of dispatching re-verification probes.
- **Fix:** In unionSources (or its callers), advance the survivor's as_of/published to the newest of survivor's and duplicate's dates when sources merge — dates are corroborating evidence too.

### I9. `scripts/riff.ts:898` — Every riff ask bumps moderator_streak, but the deep-drill protocol mandates a deepened re-ask in the same user turn, so one drilled question advances the streak by 2 and moderator_due fires after a single turn — breaking the documented 2-answered-TURNS cadence.

- **Category:** bug
- **Why:** SKILL.md Phase 4 defines the moderator firing after 2 consecutive answered turns (Co-STORM L=2), and deliver-then-deepen requires answer-at-L0 then re-ask at L1/L2 — two asks, one turn. The new mode systematically double-counts, so the aside interrupts mid-drill after nearly every escalated question, making the cadence claim false. New protocol; neither prior review could see it.
- **Fix:** Add a --deepened/--re-ask boolean to ask that suppresses the streak increment (still recording the question and stamp), passed by SKILL.md's deepen step; alternatively skip the increment when --thread is passed with --depth greater than that thread's last recorded depth.

### I10. `scripts/riff.ts:218` — numericFlag never checks args.bools, so a value-less numeric flag (--depth, --min, --strong, --tail, --limit, --port) silently reads as not-passed instead of dying loudly.

- **Category:** robustness
- **Why:** Confirmed for --depth: parseArgs parks a value-less flag in bools so flags[name] is undefined and numericFlag treats operator error as absence — 'riff ask ... --thread T1 --depth' records the question with no depth, corrupting the drill record. Directly contradicts the adjacent --thread guard's 'both die loudly (same spirit as numericFlag's M23 guard)' three lines above.
- **Fix:** In numericFlag before returning undefined: if (args.bools.has(name)) fail(`--${name} needs a numeric value`); — one line closes it for every numeric flag.

### I11. `scripts/riff.ts:935` — The transitional 'read tolerantly while claims.ts lands the field' cast for recency is stale — Sufficiency.recency landed and is typed — and the cast now erases the one compile-time check keeping this documented contract field wired.

- **Category:** simplification
- **Why:** SKILL.md lists recency among the 'contract, not decoration' ask outputs. With the field present in Sufficiency, plain a.recency would let tsc catch rename/shape drift; the as-cast plus ?? null compiles clean through such drift and silently outputs recency:null forever — the silent-degrade pattern M23/M24 refuse. The comment is also now docs-drift.
- **Fix:** Replace the three-line cast with recency: a.recency and delete the transitional comment; the type system takes over the guarantee.

### I12. `scripts/lib/report.ts:96` — chairMessageCount's docstring says it 'Mirrors riff-live's handle() dispatch condition' but only filters level-type messages, omitting riff-live's broader ignore branch, so an unknown-typed inbox line carrying a seat/text is counted as conversation even though riff-live never turned it into seat speech.

- **Category:** contract-violation
- **Why:** riff-live handle() drops any typed command other than speak/message/level as cmd_ignored without a turn, whereas this filter counts any non-level message with a chair/text or numeric seat. A {type:'close',seat:2} or {type:'xyz',chair:...} (chair-inbox is an open local channel; riff-server stamps any type up to 40 chars) is dropped by the responder but counted here, inflating 'asked' and potentially turning an honest research-only PASS into a low-ratio WARN. The real UI never emits such lines, so impact is limited.
- **Fix:** Mirror handle() exactly: after the level check, also if (typ && typ !== 'speak' && typ !== 'message') return false.

### I13. `scripts/lib/room.ts:352` — buildIcState re-reads the claim corpus and surfaced set raw (352-353), outside the section() harness that buildRoomState wraps the same loads in — any error the harness would catch and degrade still kills the entire IC build via these duplicate unguarded calls.

- **Category:** robustness
- **Why:** The harness exists so one failing load degrades its section into errors[] rather than blanking the room; the adapter defeats that for the claims data. Throw paths are narrow (readdirSync ENOENT on teardown, EACCES/EISDIR, fd exhaustion under stat churn) and transient ones heal, but a persistent one stalls the room with /state 500s while RoomState.errors never reaches anyone; it also duplicates work and can render inconsistent state.
- **Fix:** Wrap the two re-reads in the same degrade-and-record idiom (try/catch to []/new Set(), pushing into s.errors), or expose the already-loaded claimsBySlug/seen from buildRoomState so the adapter reuses one guarded load.

### I14. `scripts/lib/room.ts:371` — The ' (retired)' suffix is appended after shortLabel's ~18-char truncation, so a retired seat's arc short label reaches ~28-29 chars, defeating the cap whose purpose is that full titles overflow the tiny arc name slot.

- **Category:** ux
- **Why:** A retired long-titled seat overflows or clips in exactly the tiny arc name slot shortLabel protects, and retired seats are the ones this P11-fix path was added for; the dormancy signal is already carried by the sleep emoji and the DORMANT vote_label, so the suffix buys little where it costs most.
- **Fix:** Drop the ' (retired)' suffix from short entirely (do not compose-before-truncate), keep it on name, and rely on the emoji and DORMANT vote_label to carry dormancy in the arc.

### I15. `scripts/lib/room.ts:73` — The section() harness's justifying comment states 'writeJson is non-atomic', but fix pass 2 made writeJson atomic (util.ts:53-60: tmp sibling + renameSync) — the stated premise for the guards is now false.

- **Category:** docs-drift
- **Why:** Two comments assert opposite facts about the same function in the same fix pass. A maintainer reading room.ts could re-fix atomicity believing it missing, or one reading util.ts could strip the section() guards as obsolete — when their real justification is different (malformed agent-written JSON, evaluate() throw paths, fd errors), none of which the comment names.
- **Fix:** Rewrite lines 72-79 to the true rationale: writeJson targets are rename-atomic, but agent-written/hand-edited files can be malformed, evaluate() traverses the report dir while Phase 5 writers race it, and any load can fail on fd/permission errors — each must degrade its own section into errors[].

### I16. `scripts/lib/room.ts:20` — TIERS is imported but never used anywhere in room.ts — the P10 tier-display rewrite (Math.min with literal 9 sentinels) orphaned it.

- **Category:** simplification
- **Why:** Dead imports mislead readers into hunting for a tier-table dependency that no longer exists; it survives only because tsc settings do not flag unused imports (SATURATION_K and MODERATOR_L on the same line are used).
- **Fix:** Drop TIERS from the import list.

### I17. `scripts/room/room.html:596` — The adapter's new degradation channel (IcState.errors, room.ts:338-339) is never read by applyServerState, so a degraded build renders as a normal room with zero diagnostic.

- **Category:** fix-incomplete
- **Why:** The errors channel was built precisely because malformed dossier lines and section failures were killing state builds silently; it now stops one hop short of the user. A failed panel section yields seats:[] and the page tells the chair 'Panel not seated yet — the room fills in on its own' (misleading advice to wait for something that will not happen) while the cause sits unread in st.errors. Applies to all three copies.
- **Fix:** Render st.errors when non-empty (an amber strip under .live header, cleared when empty), preferring it over the 'Panel not seated yet' copy when the panel section failed; guard with Array.isArray since IC sends no errors field.

### I18. `scripts/room/room.html:623` — The comment and _isLiveTurn's !t.err arm promise err entries reach the record as dimmed notes, but CONTRACT-8 as implemented and tested (room.ts:411 filters them from the timeline) means an err entry can never arrive — the page's err rendering path is unreachable and riff-live's specific failure diagnostics are never shown.

- **Category:** docs-drift
- **Why:** A maintainer debugging invisible seat failures will trust this comment and conclude the diagnostics render; the current exclusion needlessly discards a diagnostic the page is already equipped to display safely. This is the seam that makes the line-352 silent-failure finding fully silent.
- **Fix:** Preferred: pass err turns through the adapter timeline tagged err (still excluded from turns_total and liveTl), letting the dimmed renderer surface them and clear thinking/watchdog for that seat; update the CONTRACT-8 test to assert 'not in liveTl / never played'. Otherwise correct the comment in all three copies to say err entries are dropped server-side.

### I19. `scripts/lib/session.ts:170` — pidIsLive returns true on EPERM without running the ps marker check, so a stale room-live.lock whose pid recycled to a root/other-user process makes liveOwnerPids report a live owner, and riff init --force refuses forever on a genuinely dead session.

- **Category:** bug
- **Why:** riff-live and riff-server are always spawned by the invoking user, so a pid the user cannot signal (EPERM) is by construction NOT the session's daemon — yet the early return skips the ps command-line verification that would exonerate it (ps -p works fine on other users' processes). The refusal then instructs killing an unrelated pid, and the real recovery (deleting room-live.lock) is documented nowhere. The recycled-pid/EPERM shape review-1 I6 fixed in riff-live's lock, reintroduced in this M7 code.
- **Fix:** In the catch, treat EPERM as 'exists — verify': fall through to the ps marker check and count as owner only when the command line contains the marker (assume-live only when ps itself fails); extend the refusal message with the manual recovery path (delete <root>/room-live.lock).

### I20. `scripts/lib/panel.ts:260` — emitCharter never enforces SLUG_RE — the I25 path-safety fix landed only in emitRoleCharter (throw) and validatePanel (warn) — so a path-unsafe panel-seat slug ('../gap','a/b') added via riff panel add reaches riff charter <slug> and writes the charter (and DOSSIER/CLAIMS paths) outside the session layout.

- **Category:** fix-incomplete
- **Why:** Mid-session gap-filler seats are model-authored JSON added when the orchestrator is moving fast (Phase 3 gap rounds), and the riff.ts panel add handler shape-checks fields but never applies SLUG_RE before addSeat saves (problems return as warnings, no approve gate). Once paths escape the layout, the dispatched agent writes its claims file where claims.ts's non-recursive readdir never finds it — the seat's output silently vanishes from ingest, corpus, and room, the exact record-scatter I25 named.
- **Fix:** Enforce SLUG_RE in emitCharter with the same throw emitRoleCharter uses, and make the riff.ts panel add handler fail (not warn) on a SLUG_RE violation before addSeat persists.

### I21. `scripts/riff.ts:1066` — The knownIds phantom-guard added to tree.ts insert() for I52 is dead code: this — its only production call site — never passes the parameter, so riff tree insert still silently files a typo'd claim id as a phantom, the defect the fix's docstring claims is prevented.

- **Category:** fix-incomplete
- **Why:** SKILL.md Phase 4 step 5 has the orchestrator hand-typing claim ids into riff tree insert every turn, so typos are routine; a phantom inflates the outline counts the report compiler projects sections from, and tree.ts's docstring reads as if the guard is live, so nobody will re-flag it. Same unreachable-escape-hatch pattern as review-2 M25 (this is the fix-audit lens's own I52 catch).
- **Fix:** Wire it: const node = insert(id, concept, claim, new Set(allClaims(id).map(c=>c.id))); and add a test asserting riff tree insert --claim no-such-id fails with the 'not in any dossier' message.

### I22. `scripts/lib/tree.ts:218` — reorganize checks membership against target.claim_ids which is only filtered AFTER the group loop, so one apply payload listing the same claim id in two groups files it into BOTH children, silently double-counting the claim in the outline and stats.

- **Category:** bug
- **Why:** Groups are model-authored JSON, and a claim relevant to two proposed subtopics is exactly what a splitting model double-lists; the result is one claim in two report sections and inflated per-concept counts with no ignored entry — the silent-distortion class the M25/ignored fixes cleaned up for duplicate children and typo'd ids.
- **Fix:** Track within-apply moves: before the target-membership check, if (moved.has(id)) { ignored.push(id); continue; } — or dedupe ids across groups up front and report collisions in the return value.

### I23. `scripts/test-riff.ts:2827` — The line-2827 assertion (Date.now()-hangT0 < 30000) is vacuous — it cannot fail for its named property (proving RIFF_JOB_TIMEOUT_MS shortens the watchdog) — and is fully redundant with the hangFailed check at 2824.

- **Category:** test-gap
- **Why:** A plumbing regression leaving the 60s default makes until() return false at ~25s so hangFailed fails, then this still prints ok at ~25s<30s; a watchdog firing at 5x the configured value (20s) passes both. A permanent green light presented as a timing assertion.
- **Fix:** Record elapsed inside the until condition at the moment the err turn appears and assert hangFailed && firedAt-hangT0 < 12_000 (3x the 4000ms knob); or delete the check as redundant with hangFailed.

### I24. `scripts/test-riff.ts:2674` — The M11 concurrent thinking-marker machinery (in-flight seats set, {seat,seats:[...],ts} shape, own-seat-only clearing) is fix-pass-2 code with zero assertions — this idle-'{}' comparison is the only thinking-file assertion in the live section, and no test ever has two seats in flight simultaneously.

- **Category:** test-gap
- **Why:** riff-live.ts:202-222 exists so one model's finisher cannot wipe another seat's spinner (review-2 M11); a regression back to the single-slot clobber, or a seats array that never drains, would leave all 480 checks green because the concurrency window is never created — even though the suite already owns STUB_HANG_NOW to create it.
- **Fix:** During the STUB_HANG_NOW window (seat A stalled), append a speak for a seat served by the other model (B): assert room-thinking.json's seats array contains A both while B generates and after B's turn lands, and only returns to {} after A's watchdog err turn.

### I25. `SKILL.md:573` — Step 2 says adding a panel seat 'is also the only move left when a thread is already at L2', contradicting the L2-ceiling paragraph (470-474) which lists three widening moves at L2 (adjacent L2 probes, new dimension + gap seat, or raising the conflict with the user).

- **Category:** docs-drift
- **Why:** Both passages are new deep-drill text describing the same state. An orchestrator reading step 2 will stop dispatching adjacent L2 probes (explicitly sanctioned by the ceiling paragraph) and never consider raising the conflict with the user, collapsing the documented widening menu to its most expensive option.
- **Fix:** Reword: 'offer to add a panel seat instead of probing the same sub-question again — that is widening; at L2 the other widening moves (adjacent L2 probes, riff coverage add + gap seat, or raising it with the user) apply per the ceiling rule above.'

### I26. `templates/probe-charter.md:90` — The probe RETURN VALUE confidence enum (verified|provisional|not-found) cannot express primary-new that the charter's RECENCY PROBES section mandates — a real schema inconsistency — though the probe's self-reported confidence is non-authoritative (riff ask recomputes the delivered label).

- **Category:** docs-drift
- **Why:** A recency/L2 probe returning a fresh single-source primary find must mislabel it: verified overclaims against the 2+-corroborating bar, provisional is the exact hedge the charter's recency rules forbid. The orchestrator relays this return value in the deliver-then-deepen loop before the re-ask computes the real label, so the wrong hedge reaches the conversation first; practical impact is minimal because the label is recomputed.
- **Fix:** Extend the enum to verified|primary-new|provisional|not-found and add one line defining primary-new for the probe (Tier 1-2 primary source inside the 90-day window, corroboration not yet possible — always return it dated).

### I27. `SKILL.md:417` — The drill-threads rule makes every distinct root question open a thread, yet the per-turn procedure's first routing option ('# plain') and the stamp section document an un-threaded ask as a normal path, with no criterion for when a user question is legitimately plain.

- **Category:** docs-drift
- **Why:** Under line 417 no user question should be asked plain, so an orchestrator following step 1's first line verbatim silently under-stamps — the thread's last-answered-depth bookkeeping that drives auto-escalation then has no thread to read, and riff thread replay misses those turns. The plain form is really only correct for the orchestrator's own non-drill lookups.
- **Fix:** State the criterion where the plain form is shown: 'plain (no --thread) is for non-drill utility asks only — every user root question gets a new thread id per the drill rule above', or drop the plain example from the per-turn procedure.

### I28. `SKILL.md:42` — I12 is stated universally ('Every question gets an immediate answer... with its honest label and stamp'), but the live-room seat-chat channel documented in the same phase produces answers with no assess label, no stamp, no thread, and no deepening — a documented feature violating a violating-this-is-a-bug invariant by design.

- **Category:** docs-drift
- **Why:** Room turns are generated by riff-live from dossier claims (honoring I2/I9) but never pass through riff ask, so nothing computes a label/stamp and nothing deepens them; the invariant as written brands the whole live-room Q&A path a bug. The prior review flagged the room's eval blindness; this is the doc-level half.
- **Fix:** Scope the invariant ('Every question asked in the riff/chat register...') or note in the room section that room turns are outside I12's label-and-stamp contract — dossier-grounded but unlabeled, with drill-grade questions belonging in the chat register.

### I29. `SKILL.md:480` — The scripted phrase 'a fresh sweep is out' is ambiguous and reads more clearly as 'a fresh sweep is on its way; deepened answer to follow' — a worthwhile polish, quoted again at line 672.

- **Category:** ux
- **Why:** This is scripted user-facing wording and the disambiguating narration sits outside the quoted phrase, so a user hearing only the quoted sentence can read 'out' as 'ruled out' — the opposite of the deliver-then-deepen promise; an orchestrating model reading the trigger bullet at line 450 in isolation can make the same inversion.
- **Fix:** Reword the scripted phrase in both places to a single reading: 'that label is below verified — a fresh sweep is on its way; deepened answer to follow.'

### I30. `SKILL.md:273` — SKILL describes the ingest conflicts channel as figure-differences only, but claims.ts also keeps and reports a conflict when NEGATION PARITY differs (e.g. 'X supports SSO' vs 'X does not support SSO', no numbers involved).

- **Category:** docs-drift
- **Why:** claims.ts stamps conflicts_with and emits a conflicts entry on either differing ordered figures OR differing negation parity (sameNegation guard, addClaims/ingestFile). A boolean capability contradiction is exactly the trust-critical disagreement this skill exists to surface, yet SKILL's framing ('the corpus now holds both numbers and one is wrong') does not apply, so an orchestrator reading only SKILL may not recognize a numberless contradiction as a conflict to reconcile.
- **Fix:** In the Phase 2 conflicts bullet, say near-identical claims are kept because their ordered FIGURES or their NEGATION PARITY differ (a direct refutation), noting a negation conflict carries no figures; the same supersede/probe resolution applies.
