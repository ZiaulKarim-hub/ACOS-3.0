timestamp: "2026-08-18T21:52:47Z"
status: "active"
type: "autocompact"
trigger: "precompact-hook:auto"
session_id: "d72f5b72-b88e-4c3c-be62-9fa7e54f0fbf"
estimated_tokens: 355575
written_by: "precompact-handoff.ts"

# Depth restoration (2026-08-13). This hook can only record FACTS — it is a
# script running in a frozen pane, so it cannot read the conversation and form a
# judgement the way the old handoff-agent did. Two later stages fill that in,
# and this field is how they find their work:
#   pending          -> written here, nothing enriched yet
#   summary-written  -> handoff-enrich.ts (PostCompact) pasted in the real
#                       model-written compaction summary. No extra model call:
#                       Claude Code hands the summary straight to the hook.
#   deepened         -> the resumed session itself added the judgement fields
#                       (decisions, blockers, learnings) that only a reader of
#                       the whole conversation can produce.
# A handoff stuck at "pending" is not broken — every mechanical fact below is
# still true. It just never got its narrative.
enrichment_status: "deepened"

current_work: |
  Mid /acos-safe-close: steps 1-2c done. Destination picked by Zee: row 3,
  "Research to Portfolio" (uuid 1b4e2505-151f-4e53-a633-da5b5c7b5daa).
  Next: dry-run gate, generation pass, blind round-trip, final pass.
completed_this_session: |
  R2P commits 8e0f36a (read-mutation-gate strict-null fix, byte-identical proof),
  d01ef82 (card 10 R-A: priority P0 marks + validator rule), a2d0197
  (P0-INVARIANT-AUDIT-2 sibling, findings G-01..G-05), c336c22 (card 2 both
  halves record). Card-2 research: 17 claims, 3 primary-new stamps. Memory file
  reference_subagent_cap_survives_compact.md written. Safe-close learnings JSON
  composed (16 facts).
decisions_made: |
  Zee chose /acos-safe-close over the proposed /clear restart. He picked
  destination row 3 (Research to Portfolio) for the park. Cards 1 and 2
  (A/B letters) remain UNANSWERED — recs were A and B respectively.
blockers: |
  200-subagent cap (200/200) survives /compact, so R2P pairs 36/37/38/40
  stay blocked in this session; the safe-close blind verifier dispatch may
  also hit it.
candidate_learnings: |
  - The 200-subagent cap survives /compact (session id unchanged).
  - Emergency-handoff filenames collide across concurrent sessions
    (2026-08-18-emergency-handoff-2.yaml clobbered by FruitSync ff036fd7).
  - riff ask abstains below 0.25 similarity; ask narrower questions.
context_for_next_session: |
  Resurrect "Research to Portfolio" — the close's reentry note there carries
  the re-arm command and the four blocked pair orders. First actions: re-arm
  R2P autopilot, collect Zee's card 1+2 letters, dispatch pairs 36 37 38 40
  with fresh agent capacity.

session_summary: |
  Written automatically by the PreCompact hook, immediately before Claude Code
  compacted this session. Trigger was "auto" ("auto" = the built-in token
  threshold fired on its own, with no keystrokes; "manual" = someone ran /compact).
  
  This file is the DURABLE, MECHANICAL record. It is deliberately not a
  narrative summary — the compaction summary in the conversation itself is the
  narrative, and it is written by the model with the whole transcript in view.
  What this file adds is the set of facts a summary can silently drop: the exact
  last thing the human typed, which background agents were still running, and
  what the repo looked like at the moment of the cut.

human_last_message: |
  why the fuck are you compacting conversations now?

assistant_tail: |
  Yes — and you just approved it. Here is the honest part first. I cannot restart my own process. I live inside it. It is like an engine asking to rebuild itself while running.
  
  But you can do it with one command. And I packed the bags already, so nothing is lost:
  
  1. **A fresh handoff is on disk.** It holds tonight's state, the four pair orders, and all rules. File: `memory/handoffs/2026-08-18-emergency-handoff-2.yaml`.
  2. **A resume note is staged for the machinery.** A hook (a small helper script) watches your next message. After a clear, it attaches my resume note to it. The new me reads it and re-arms the goal first thing.
  
  **Why this works.** `/clear` (the command that starts a fresh chat session) gives a new session id. The 200-agent limit counts per session. New session, fresh 200. That is the cure. A caveat, stated honestly: the hook path is the fail-safe route. If the note somehow does not attach, the handoff file still works. Just tell the new me to read it.
  
  **So the one action is this.** Type `/clear`. Then send one short message. Best of all: make that message your two letters — card 1 and card 2. Example: "card 1: A, card 2: B". The resume note rides along with it. The […]
  
  ---
  
  Understood — we park this tab properly instead. The close script does the safety work; I only route. Running preflight first, then composing the intent core:

in_flight_subagents:
  []

repo_state:
  branch: "acos-deficiency-fixes-2026-06-04"
  uncommitted_file_count: 188
  recent_commits: |
    66b7b71 fix(riffs): a half-installed charter overlay throws instead of aiming panel seats at the web
    9367960 feat(oracle): a missing goal asks instead of refusing
    ee8be86 feat(oracle): a real AI at the permission door — four settings, one ever on
    7f11ce2 feat(xl-update): Phase 3b — read-only email sweep across both work mailboxes
    71ca4a1 feat(investigate): /investigate — the inward twin of /research, on the riff engine

uncommitted_files:
  - "M .claude/scripts/eternity-protocol-core.sh"
  - "M .claude/scripts/eternity-resume-prepend.sh"
  - "M .claude/scripts/git-manager/ids.json"
  - "M .claude/scripts/html-to-pdf.js"
  - "M .claude/scripts/oracle-evaluate.py"
  - "M .claude/scripts/resurrection/adopt-project.sh"
  - "M .claude/scripts/resurrection/close-project.sh"
  - "M .claude/scripts/resurrection/enroll-project.sh"
  - "M .claude/scripts/resurrection/rename-workspace.sh"
  - "M .claude/skills/acos-eternity-protocol/SKILL.md"
  - "M .claude/skills/acos-resume-prompt/SKILL.md"
  - "M .claude/skills/acos-resurrect/SKILL.md"
  - "M .claude/skills/acos-safe-close/SKILL.md"
  - "M .claude/skills/investigate/SKILL.md"
  - "M \"Logo Builder/brandsync/avoid.json\""
  - "M \"Logo Builder/brandsync/commands.jsonl\""
  - "D \"Logo Builder/brandsync/symbol/candidates/round-3/r3-04.svg\""
  - "D \"Logo Builder/brandsync/symbol/candidates/round-4/r4-02.svg\""
  - "D \"Logo Builder/brandsync/symbol/candidates/round-4/r4-04.svg\""
  - "D \"Logo Builder/brandsync/symbol/candidates/round-4/r4-06.svg\""
  - "D \"Logo Builder/brandsync/symbol/candidates/round-4/r4-09.svg\""
  - "?? .claude/scripts/handoff-enrich.ts"
  - "?? .claude/scripts/precompact-handoff.ts"
  - "?? .claude/scripts/resurrection/backfill-key-tags.ts"
  - "?? .claude/scripts/resurrection/conflict-scan.py"

next_actions:
  - "Read human_last_message above. If it is still an open question, answer it FIRST."
  - "Verify real state on disk before trusting any summary — recount, re-check, re-run."
  - "If in_flight_subagents is non-empty, their results may still arrive. Do not discard them."

reconstruction_sources:
  transcript_path: "/Users/zee/.claude/projects/-Users-zee-Documents-Vibe-Coding-ACOS-3-0/d72f5b72-b88e-4c3c-be62-9fa7e54f0fbf.jsonl"
  transcript_lines_parsed: 1972
  cwd: "/Users/zee/Documents/Vibe Coding/ACOS 3.0"

# ── The model-written narrative ──────────────────────────────────────────────
# This is the ACTUAL compaction summary Claude Code produced, handed to the
# PostCompact hook verbatim in its `compact_summary` field. It replaces what
# handoff-agent used to write, and it is strictly better sourced: it was made
# with the whole conversation in context, not from a transcript tail.
#
# summary_quality is a HEURISTIC, not a verdict. "suspect" means the text looks
# like a refusal rather than a summary — the one failure that would otherwise
# lose a whole session silently. When it says suspect, the mechanical fields
# above are the trustworthy record, and the raw transcript named in
# reconstruction_sources is the fallback.
summary_quality: "ok"
compaction_summary: |
  <analysis>
  Let me walk the conversation chronologically.
  
  1. Session opened as a continuation after compaction (eternity-protocol resume). The resume prompt directed: (1) re-arm autopilot with exact command, (2) verify R2P state on disk (HEAD f400a06), (3) execute Zee's standing order "move to coding the next phase" — dispatch four blocked pairs (tasks 36/37/38/40) + 3 card-2 web seats, do orchestrator-direct tasks 35 and 41, (4) put card 1 and card 2 A/B back to Zee as plain text, never AskUserQuestion. A SessionStart hook also asked to enrich the autocompact handoff at `memory/handoffs/2026-08-18-autocompact-handoff-2.yaml` with judgement fields and set enrichment_status to "deepened".
  
  2. I re-armed autopilot — it errored: "already active (iteration 9/1000)" — the /compact kept the session id, so the sentinel survived. Verified R2P: HEAD f400a06, clean tree, 0 remotes. Found the card-2 research session `~/.acos-research/.acos/riffs/2026-08-18-freeze-in-place-versus-copy-then-freeze-for-returned-records` with charters for 3 seats (semantics-generalist, practice-reader, skeptic).
  
  3. Enriched the autocompact handoff (two edits: status flip + appended judgement fields).
  
  4. Dispatched 7 agents (4 DEV pairs + 3 web seats) + marked tasks in_progress. ALL SEVEN Agent calls FAILED: "Subagent spawn limit reached (200 of 200 agents spawned)". KEY DISCOVERY: the 200-subagent cap SURVIVES /compact because the session id is unchanged. The four production pairs stay blocked (Independence Wall forbids orchestrator-direct production code). I reset tasks 36/37/38/40 to pending with blocker notes.
  
  5. Task 35 (orchestrator-direct, allowed under OBS-MG21 precedent): fixed 20 strict-null tsc errors in `tools/read-mutation-gate.ts`. Method: captured baseline output on two logs (`TESTRUN-20260812T090000Z-12-mutation-gate-FULL-RUN.log`, `GATE-RERUN-latest.log`) with `--json` artifacts; applied 5 narrowing edits (`m[3] ?? null`, `(m[1] ?? "").trim()`, `lines[i] ?? ""`, `c[0] ?? ""`, destructuring defaults `= Number.NaN` on seven numeric cells); proved byte-identical via cmp on stdout + JSON with identical CLI args; tsc 20 errors → 0 (exit 0 verified cleanly, not via pipe). Wrote record `RECORD-TOOL-FIX-READ-MUTATION-GATE-STRICT-NULLS.yaml`; committed 8e0f36a with scoped add (staged list read first).
  
  6. Task 41 (card 10 R-A execution): Read `planning/epics/EPIC-001.yaml` (six economic_invariants INV-001..006), `P0-INVARIANT-AUDIT.yaml` (the v1 audit that stopped at step 1 with four readings R-A..R-D), `planning/templates/epic.yaml`, `tools/validate-planning-yaml.ts` (shallow: parse + artifact_type only). Edits: template gained documented `priority: P0` field; EPIC-001.yaml gained `priority: P0` on all six invariants + transcription comment citing RULING-BATCH-2026-08-18 card 10; validator gained its first field-level rule (Epic economic_invariants priority must be "P0"|"P1" when present). Proved with negative control (planted P9 → refused, exact message; removed → back to baseline). DISCOVERED six PRE-EXISTING validator failures: FAIRNESS-AUDIT-001.yaml YAML parse error line 82, five generated evidence files missing artifact_type (EVB-STORY-001-007: manifest-head-b, rollup.generated, testruns.generated; EVB-STORY-001-008: manifest-head-b, rollup.generated). Did NOT fix them — disclosed as G-03. Committed d01ef82.
  
  7. Audit re-run (part 3): gathered per-pair two-limb evidence via test-name extraction across property files and adversarial files for pairs 16-24. Findings per pair (scoped invariants via parent story): 16-18 INV-003; 19-21 INV-002+INV-003; 22-24 INV-005+INV-006. Results: both limbs evidenced on pairs 16, 17, 18, 20, 21, 22, 23, 24; pair 19's INV-002 adversarial limb NOT evidenced at name level (G-01). IW-01 and IW-03 from v1 both vindicated (pairs 20/21 in-unit [PROPERTY] tags — performance-benchmark-alpha has 32 [NEGATIVE/BOUNDARY]+14 [PROHIBITED], attribution-interaction 27+11; pair 17's 17 [PROHIBITED] cases quote INV-003's prohibited text at :1025). Wrote `P0-INVARIANT-AUDIT-2.yaml` as SIBLING (additive) with 5 findings G-01..G-05, all pending_human_review owner Zee. Committed a2d0197. Marked task 41 completed.
  
  8. Card-2 web research: the three seats run orchestrator-direct (disclosed in every dossier) since agents blocked. Loaded WebSearch/WebFetch. Round 1: MDN Object.freeze (shallow; strict-mode TypeError; returns SAME object; deepFreeze example caveats), Immer freezing docs (autoFreeze default recursive; "Any plain object or array that ends up in the produced result, will be frozen, even when these objects were not frozen before the start of the producer!"), RTK discussions #2908/#1189 (copy at consumer, keep autoFreeze), Apollo (freezeResults dev → immutable by default; reference-equality perf). Round 2: MDN WeakSet (identity membership, clone not a member), redux-toolkit.js.org/usage/immer-reducers ("#1 cause of Redux bugs"), structuredClone cost searches (Tier 3 only), dated recency sweeps for all three dims (2026-05-20..2026-08-18 windows, nothing new). Wrote 3 dossiers + 3 claims.jsonl to the session's dossiers/ dir; ingested (5+6+6=17 claims, 0 malformed, 0 conflicts); credited coverage probes incl. --recency; `riff ask` broad question returned "not-in-corpus" (0.17 < 0.25 threshold) — correctly abstained; three narrower asks all returned `primary-new` `[1 sources · primary-new]`.
  
  9. Wrote `RECORD-SBL-D4-INVESTIGATION-BOTH-HALVES-COMPLETE.yaml` (committed c336c22) laying out card 2's A/B: A = clone caller inputs at door; B = keep freeze-in-place + document loudly (rec B). Wrote memory file `reference_subagent_cap_survives_compact.md` + MEMORY.md index line.
  
  10. Final message (rewritten once after eden-selfcheck failure at Level 4): presented map, blocker, three completed items, card 1 A/B (rec A — fix the zero-price label), card 2 A/B (rec B), one action: reply with two letters.
  
  11. Zee: "can't you do a restart? I can approve?" — I explained I can't restart my own process; prepared for an approved /clear: wrote fresh handoff `memory/handoffs/2026-08-18-emergency-handoff-2.yaml` + fresh `pending-resume-d72f5b72-b88e-4c3c-be62-9fa7e54f0fbf.txt` (verified `eternity-resume-prepend.sh` hook: project-scoped, fires on next prompt, moves consumed files). Told Zee: type /clear then send his two letters, resume rides along. Also mentioned env-var option `CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION` (needs full restart, not needed — fresh 200 is plenty).
  
  12. IMPORTANT ambient note: `memory/handoffs/2026-08-18-emergency-handoff-2.yaml` was OVERWRITTEN by a concurrent FruitSync session (ff036fd7-705d-49a8-b24b-771e9cb47513) — a shared-path collision. My packed R2P handoff at that path is GONE (clobbered). The pending-resume file should still be intact (different path, keyed to my sid).
  
  13. Zee then invoked `/acos-safe-close` (instead of /clear) — park this tab via the Resurrection Protocol. Hard rules: never compose/modify receipt lines (relay cat output verbatim); never write to memory/handoffs/ or daemon state dir; never author round-trip verdicts; never pass --auto-close without explicit ask + RESURRECTION_DP2_CONFIRMED=1; intent core composed by me; blind verifier = ONE fresh general-purpose Task agent per attempt. Note: Task agent spawning may ALSO hit the 200-cap — an issue for Step 5 (round-trip). Eden level changed from 4 to 3 (middle school) per the latest injector.
  
  14. Step 1 preflight ran: SID=d72f5b72-b88e-4c3c-be62-9fa7e54f0fbf, roundtrip harness present.
  
  15. Step 2 + 2a ran: intent core written to `$SCRATCH/safe-close-intent.txt` with next_action "Re-arm R2P autopilot, collect card 1+2 letters, dispatch pair tasks 36 37 38 40" (first line, ≤90 chars, no "stub"); gatherer found 157 handoffs, 6 not yet harvested, 16 candidate learnings (R2P analogy/domain-gloss/two-clocks/wobble-file/no-remote items from 2026-08-17-emergency-handoff-3.yaml; truncated zsh/stat/skills items from 2026-08-18-autocompact-handoff-7.yaml; logo-forge items from 2026-08-18-emergency-handoff-3.yaml — these belong to OTHER sessions' projects, relevant for sorting in Step 2b).
  
  Remaining steps: 2b (compose learnings JSON folding in the 16 gathered candidates, sorted machine vs ruling), 2c (render book via resurrect-view.py, show verbatim, ask destination with typed reply — plain text, never AskUserQuestion), 3 (dry-run), 4 (generation pass), 5 (blind round-trip via ONE Task agent — may hit the spawn cap; if Task fails due to cap, that's a genuine issue to surface — the skill says harness present means Step 5 must run; a cap-blocked dispatch isn't covered by the skill; I'd need to relay honestly), 6 (final pass + verbatim receipt), 6b (Kind 2 questions, cap 2), 6c (mark harvested only on safe close), 7 (relay + banner).
  
  Key data for Step 2c: SCRATCH=/private/tmp/claude-501/-Users-zee-Documents-Vibe-Coding-ACOS-3-0/d72f5b72-b88e-4c3c-be62-9fa7e54f0fbf/scratchpad; RESDIR resolution pattern; PARK_ARGS as ARRAY.
  
  User messages to record: the resume instruction, "can't you do a restart? I can approve?", and the /acos-safe-close invocation.
  
  Constraints to preserve verbatim: R2P LOCAL-ONLY no remote never push; AP-06; AP-07; Zee IS the gate; records ADDITIVE (sibling supersede); never AskUserQuestion for rulings under autopilot; forbidden score levers; TS/Rust only; scoped git add + read staged list; never tsc during live stryker; macOS no timeout; repo path has SPACE; safe-close hard rules; sealed test files untouchable; card 4 stop-condition; boss email untouchable (standing, from memory); push defaults personal.
  
  Autopilot is STILL ACTIVE (iteration 9/1000) — the UserPromptSubmit hook confirmed at the /acos-safe-close turn.
  </analysis>
  
  <summary>
  1. Primary Request and Intent:
  
     **The user's most recent request (in force, mid-execution):** Zee invoked `/acos-safe-close` — park this tab's work via the Resurrection Protocol so a future session can resurrect it. The skill's steps 1, 2, and 2a are DONE; steps 2b through 7 remain. This superseded my prior proposal that he type `/clear` (he had asked "can't you do a restart? I can approve?" and I had prepared /clear artifacts; instead he chose safe-close).
  
     **The standing autopilot goal (STILL ACTIVE, iteration 9/1000, session d72f5b72-b88e-4c3c-be62-9fa7e54f0fbf):** re-arm command (exact):
     `python3 .claude/scripts/autopilot-activate.py on "Complete the R2P project in /Users/zee/Documents/Vibe Coding/R2P to the end-state defined by docs/PRD_v0.4.txt: first finish EPIC-001 (all 24 DEV/QA pairs through the adversarial cycle - DEV, QA, holdout, orchestrator seal - then EVB-STORY bundles x8, 4-reviewer swarm review, EVB-EPIC-001, close packet), then plan and execute each subsequent PRD phase as new epics under the same adversarial cycle with sealed evidence throughout. At every epic close, present the ratification packet (ADRs, finding dispositions, disclosure queue) to Zee and continue non-dependent work (e.g. planning the next epic) while awaiting his decision. Never self-approve; never treat silence as approval; never push (local-only repo). Re-derive current position from repo state (planning/, git log) at each iteration." --goal-file "/Users/zee/Documents/Vibe Coding/R2P/docs/PRD_v0.4.txt" --max-iter 1000`
  
     **Safe-close HARD RULES (verbatim-critical):** Receipts reach the user ONLY as unmodified `cat` output — never compose/retype/summarize/decorate a receipt line; sole exception the Step 7b verdict banner (byte-for-byte repeat of the script's own verdict line, conditional on it existing in the receipt). NEVER write to top-level `memory/handoffs/`, `memory/handoffs/archive/`, or `~/Library/Application Support/acos-token-monitor/state/`; NEVER touch `pending-resume-*.txt`/`RESCUED-resume-*.txt`. NEVER author a round-trip verdict or result file. NEVER pass `--auto-close` unless the user explicitly asked AND already exported `RESURRECTION_DP2_CONFIRMED=1`. The intent core is composed by the parent session; the blind verifier is ONE fresh general-purpose Task agent per attempt. Step 2c asks the destination with a plain TYPED reply, NEVER AskUserQuestion (autopilot auto-answers it). Intent refusals are the only fix-and-retry; any other refusal → relay verbatim and STOP.
  
     **Open decisions belonging to Zee (presented, unanswered):** Card 1 (SBL-D3 zero-price label): A = fix the label check to test the raw share delta, B = document; my rec A. Card 2 (SBL-D4 freeze-in-place): ruling question verbatim "Is freezing a caller's policy object intended? If not, deepFreeze must clone before freezing."; A = clone caller inputs at the door, B = keep freeze-in-place + document loudly (Immer precedent); my rec B. Plus new findings G-01..G-05 from the audit re-run.
  
     **Standing constraints (verbatim-critical):** R2P is LOCAL-ONLY — NO git remote, NEVER push. AP-06: never weaken a test, invariant or tolerance. AP-07: no agent approves, seals, closes or ratifies; records may TRANSCRIBE a ruling Zee actually made; silence is never approval. Zee IS the human gate ("Ben" is the PRD role name). Records are ADDITIVE — supersede with a SIBLING. NEVER AskUserQuestion for rulings while autopilot is active (OBS-AP1 — a hook forges it). Forbidden score levers: project-wide excludedMutations; Stryker disable except per-site demonstrated-equivalent with written reason; narrowed mutate globs; weakened tests; ignoreStatic. TypeScript/Rust only. Scoped git add; read the staged list before committing. NEVER run tsc while a stryker run is live. macOS has no timeout command; repo path contains a SPACE. Sealed test files (`tests/unit/presentation-risk-performance-view.test.ts`, `-contracts.test.ts`) must never be edited. Card 4 stop-condition: if `legal_position_figure_out_of_range` is already refused under siblings in every reachable path, return to Zee before code. Eden protocol now Level 3 (was 4). Git pushes default to PERSONAL account; never touch jason@ mail/cal/drive.
  
  2. Key Technical Concepts:
     - **200-subagent cap SURVIVES /compact** (PROVEN this session: all 7 Agent dispatches failed "Subagent spawn limit reached (200 of 200)"): /compact keeps the session id, so the counter and the autopilot sentinel both persist. Only /clear, a new window, or a restart with `CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION` raised restores capacity. Saved to memory (`reference_subagent_cap_survives_compact.md` + MEMORY.md line).
     - Independence Wall: production DEV/QA pairs REQUIRE separate agents; orchestrator-direct is sanctioned only for tool fixes (OBS-MG21 precedent, byte-identical output proof) and research (disclosed fallback).
     - The 16.7 two-limb gate: "Every P0 invariant must also have a property or model-based test and an adversarial negative case" — per pair; subject now DEFINED by card 10 R-A (INV-001..006 are the P0 set); priority field P0|P1 (PRD Appendix A scheme).
     - riff engine: dossier format (markdown + claims.jsonl per slug in session `dossiers/` dir), `riff claims ingest --session <id> --slug <slug>`, `riff coverage probe <dim> --novel <n> [--recency]`, `riff ask` label machinery (0.25 match threshold; abstains "not-in-corpus"; `primary-new` = Tier 1-2 primary, ≤60 days).
     - Research findings (Tier 1): Object.freeze is shallow, returns SAME object ("does not create a frozen copy"), strict-mode TypeError; WeakSet membership by reference identity (clone ≠ member); Immer autoFreeze recursive default, freezes caller-provided objects ending up in results (documented); RTK "#1 cause of Redux bugs"; Apollo immutable results ↔ reference-equality checks; no mainstream copy-then-freeze precedent found.
     - eternity-resume-prepend.sh: UserPromptSubmit hook, project-scoped (pending-resume session_id must have a JSONL in this project's dir), fires on next prompt, moves consumed files to state/consumed/.
     - Shared-path collision hazard: `memory/handoffs/2026-08-18-emergency-handoff-2.yaml` (which I wrote for the /clear plan) was OVERWRITTEN by concurrent FruitSync session ff036fd7-705d-49a8-b24b-771e9cb47513 — do not trust that file for R2P state.
     - Safe-close pipeline: close-project.sh (steps 0-10, 7-check gate), roundtrip-verify.sh (blind verifier, Wigum cap 5 → DEGRADED), gather-candidate-learnings.py, resurrect-view.py (book), knowledge_lib.confirm_ruling; PARK_ARGS as bash ARRAY (zsh word-split trap); Kind 1 "machine" learnings written silently, Kind 2 "ruling" asked (cap 2); check types whitelist: file_exists, file_count, path_contains, value_matches.
  
  3. Files and Code Sections:
     - **R2P commits this session (newest first): c336c22, a2d0197, d01ef82, 8e0f36a** (on top of f400a06).
     - `tools/read-mutation-gate.ts` (8e0f36a) — five narrowing edits: `h.staticMutantTimeShare = m[3] ?? null;`, `if (m) h.runExit = (m[1] ?? "").trim();`, `const raw = lines[i] ?? "";`, `const nameCell = c[0] ?? "";`, and destructuring `const [total = Number.NaN, _covered = Number.NaN, killed = Number.NaN, timeout = Number.NaN, survived = Number.NaN, noCoverage = Number.NaN, errors = Number.NaN] = nums;` with comment "Defaults are unreachable (c.length >= 8 guarantees seven cells) and feed the existing NaN guard below if that ever stopped being true." Proof inputs: `planning/evidence/EVB-EPIC-001/close-packet/testruns/TESTRUN-20260812T090000Z-12-mutation-gate-FULL-RUN.log` and `GATE-RERUN-latest.log`; cmp byte-identical stdout+JSON, exit 0/0, tsc 20→0.
     - `planning/evidence/EVB-EPIC-001/close-packet/RECORD-TOOL-FIX-READ-MUTATION-GATE-STRICT-NULLS.yaml` (id RECORD-TOOL-FIX-RMG-STRICT-NULLS) — records fix + proof + "pnpm run verify stays RED for ONE remaining reason: OBS-VG1".
     - `planning/templates/epic.yaml` (d01ef82) — invariant entry gains documented `priority: P0` (optional; P0 | P1; bound by §16.7 two-limb gate; RULING-BATCH-2026-08-18 card 10 R-A).
     - `planning/epics/EPIC-001.yaml` (d01ef82) — `priority: P0` added to all six INV entries + header comment transcribing the ruling ("An agent did not choose this; F-01 of P0-INVARIANT-AUDIT.yaml put the four readings to the human gate and R-A was his answer."). Version deliberately NOT bumped.
     - `tools/validate-planning-yaml.ts` (d01ef82) — first field-level rule added after the artifact_type check: for `record.artifact_type === "Epic"` with array `economic_invariants`, entries carrying `priority` must be `"P0"` or `"P1"` else error `economic_invariants <id>: "priority" must be "P0" or "P1", got <json>`. Negative control (planted P9) proven refused; six PRE-EXISTING failures unrelated to change.
     - `planning/evidence/EVB-EPIC-001/close-packet/P0-INVARIANT-AUDIT-2.yaml` (a2d0197) — sibling of v1; per-pair two-limb table with file:line citations (e.g. pair 17 property `ledger-sleeve-conservation.property.test.ts:697` P1 "sleeve sum + declared residual === the legal account position at ZERO tolerance"; adversarial `[PROHIBITED]` block at unit :1025 quoting INV-003's prohibited text; pair 22 `operator-run-compare-workflow.property.test.ts:505` P2 names INV-005 in title; pair 19 gap). Findings G-01 (pair-19 INV-002 adversarial not evidenced), G-02 (INV-006 testable-clause wrinkle), G-03 (six validator failures: FAIRNESS-AUDIT-001.yaml parse error line 82; EVB-STORY-001-007 manifest-head-b/rollup.generated/testruns.generated + EVB-STORY-001-008 manifest-head-b/rollup.generated missing artifact_type), G-04 (INV-001/004 have no pair in 16-24), G-05 (charter 16.7 sentence still untranscribed — offered, not performed). All pending_human_review owner Zee.
     - `planning/evidence/EVB-EPIC-001/close-packet/RECORD-SBL-D4-INVESTIGATION-BOTH-HALVES-COMPLETE.yaml` (c336c22) — certifies both ordered halves complete; investigate 47 claims; research 17 claims, three `[1 sources · primary-new]` stamps; the A/B framing with rec B; orchestrator-direct disclosure.
     - `~/.acos-research/.acos/riffs/2026-08-18-freeze-in-place-versus-copy-then-freeze-for-returned-records/dossiers/` — 6 files written (semantics-generalist.md/.claims.jsonl, practice-reader.md/.claims.jsonl, skeptic.md/.claims.jsonl), each opening with the ORCHESTRATOR-DIRECT disclosure; ingested 5+6+6=17 claims, 0 malformed; coverage credited (sem 5+2, prac 5, cost 1+4, plus 3 --recency probes with novel 0).
     - `memory/handoffs/2026-08-18-autocompact-handoff-2.yaml` — enriched (enrichment_status "deepened" + six judgement fields appended).
     - `~/.claude/projects/-Users-zee-Documents-Vibe-Coding-ACOS-3-0/memory/reference_subagent_cap_survives_compact.md` + MEMORY.md index line — the cap-survives-compact learning.
     - `pending-resume-d72f5b72-b88e-4c3c-be62-9fa7e54f0fbf.txt` (written BEFORE safe-close was invoked; now untouchable under safe-close hard rules) — carries re-arm command, card status, four-pair dispatch order.
     - **Safe-close scratch files (current work):** `$SCRATCH/safe-close-intent.txt` — first line `next_action: Re-arm R2P autopilot, collect card 1+2 letters, dispatch pair tasks 36 37 38 40`; decisions/rejected_alternatives/traps/open_questions blocks filled (traps include cap-survives-compact, the FruitSync handoff clobbering, LOCAL-ONLY/space-in-path, no-tsc-during-stryker, card 4 stop-condition, sealed files, no-AskUserQuestion). `$SCRATCH/gathered-candidates.json` — 16 candidates from 6 unharvested handoffs; `$SCRATCH/gathered-sources.json`.
     - SCRATCH = `/private/tmp/claude-501/-Users-zee-Documents-Vibe-Coding-ACOS-3-0/d72f5b72-b88e-4c3c-be62-9fa7e54f0fbf/scratchpad`; RESDIR = `$ROOT/.claude/scripts/resurrection` (exists locally); SID = d72f5b72-b88e-4c3c-be62-9fa7e54f0fbf; harness PRESENT.
  
  4. Errors and fixes:
     - **All 7 agent dispatches failed at 200/200 cap** — the resume plan's premise ("fresh session has fresh capacity") was WRONG because /compact keeps the session id. Fix: reset tasks to pending with blocker notes; did orchestrator-direct-eligible work only; production pairs deliberately NOT done solo (Independence Wall); saved the learning to memory; later prepared /clear artifacts when Zee asked about a restart.
     - **Autopilot re-arm errored "already active"** — benign; sentinel survived the compact; no --force needed.
     - **`$?` after a pipe measured tail, not tsc** — re-ran `npx tsc --noEmit > /dev/null 2>&1; echo "TSC_EXIT=$?"` cleanly (feedback_test_tally_without_exit_code_lies).
     - **First byte-identical diff showed one differing line** — my own differing `--json` filename echoed in stdout. Fix: re-ran with the IDENTICAL argument strings (copying baselines to .keep first); then all four cmp checks byte-identical.
     - **Six pre-existing validate-planning-yaml failures surfaced** — verified none relate to my change (negative control added a 7th, removal returned to 6); disclosed as G-03 rather than silently fixing evidence files.
     - **Broad `riff ask` returned "not-in-corpus" (best match 0.17 < 0.25)** — correct abstention; fix: asked three narrower questions, all `primary-new`.
     - **eden-selfcheck Stop hook FAILED my big reply at Level 4** (FK grade 10.8 vs 5.0 cap, 60% sentences over 12 words) — rewrote once from the intended answer (not the text), delivered. Eden is NOW Level 3 (per the latest injector) — sentences up to ~12-15 words, define every domain term.
     - **`memory/handoffs/2026-08-18-emergency-handoff-2.yaml` clobbered** by concurrent FruitSync session ff036fd7 — noted as a trap in the intent core; R2P state lives in repo records and the close's reentry note instead.
  
  5. Problem Solving:
     - Executed everything executable under the cap: task 35 (byte-identical tool fix), task 41/card 10 (schema+marks+validator+audit re-run: 8 of 9 pairs both limbs), card-2 web research (17 claims, 3 primary-new stamps), evidence records, commits with scoped adds.
     - Presented cards 1 and 2 to Zee with recommendations (A and B respectively) as plain text.
     - When Zee asked about a restart: verified the resume hook mechanics, wrote /clear preparation artifacts, explained the one action. He chose `/acos-safe-close` instead; steps 1-2a complete.
     - OPEN ISSUE for Step 5 of safe-close: the blind verifier requires ONE fresh Task agent — the 200-cap may block it; if dispatch fails, relay honestly (the skill doesn't cover a cap-blocked dispatch; never author the verdict myself).
  
  6. All user messages:
     - "Resume the eternity-protocol handoff and continue exactly where the previous session left off." (the auto-injected resume after /compact; carried Zee's prior standing order "now that all the decisions are resolved, please move to coding the next phase.")
     - "can't you do a restart? I can approve?"
     - `/acos-safe-close` (skill invocation — park this tab via the Resurrection Protocol)
  
  7. Pending Tasks:
     - **Safe-close steps 2b-7 (CURRENT):** 2b compose `$SCRATCH/safe-close-learnings.json` folding in the 16 gathered candidates (sort machine vs ruling; unsure → ruling; evidence required; checks whitelist file_exists/file_count/path_contains/value_matches; cap 2 ruling questions); 2c render the book (`python3 "$RESDIR/resurrect-view.py" --color never` + `--json`), show verbatim, ask destination as a TYPED number (0 = new project; resolve against close-book.json pick_number; PARK_ARGS as ARRAY); 3 dry-run gate; 4 generation pass; 5 blind round-trip (ONE fresh general-purpose Task agent per attempt via harness — cap risk); 6 final pass + verbatim receipt; 6b ask at most 2 Kind-2 questions; 6c mark harvested ONLY on `SAFE TO CLOSE THIS TAB` + non-empty sources; 7/7b relay + mechanical verdict banner LAST.
     - Blocked R2P pairs (need agent capacity in a future session): #36 card 4 producer (stop-condition), #37 card 5 mint-register into compareManifestIdentity (forged-pair/left/right/genuine pinned), #38 card 6a atUtc ISO_INSTANT_PATTERN (new sibling test file; sealed files untouchable), #40 card 7 specs for ALL 19 digest functions (no output may change; reconciliationChecksum recipe at sleeve-conservation.ts:3034-3053 under REV-SLEEVE-2).
     - Awaiting Zee: card 1 letter (rec A), card 2 letter (rec B), G-01, G-03, G-04, G-05.
     - Older open queue: tasks 12/13/15/16/27/31, MAP-Q1, OBS-VG1, 15 open HIGHs, 12 ADRs, OBS-SW2, next-epic decisions D-002-*/D-003-*.
  
  8. Current Work:
     Mid-way through `/acos-safe-close`. Step 1 preflight PASSED (`SID=d72f5b72-b88e-4c3c-be62-9fa7e54f0fbf`, "roundtrip harness: present"). Step 2 intent core WRITTEN to `$SCRATCH/safe-close-intent.txt` (first line verified: `next_action: Re-arm R2P autopilot, collect card 1+2 letters, dispatch pair tasks 36 37 38 40`). Step 2a gather RAN: "157 handoff(s) on disk, 6 not yet harvested, 16 candidate learning(s) found" → `$SCRATCH/gathered-candidates.json` + `$SCRATCH/gathered-sources.json`. The 16 candidates span: five R2P items from 2026-08-17-emergency-handoff-3.yaml (mechanics-harness analogy; domain-vocabulary orientation lesson; two-clocks Stryker/Vitest; wobble-file selection; R2P-no-remote + transcript-cwd-field lesson); four TRUNCATED items from 2026-08-18-autocompact-handoff-7.yaml (macOS stat symlinks; zsh no-word-split; zsh glob abort; ~/.claude/skills two conventions — truncated mid-sentence in the gather, treat carefully); six logo-forge/skills items from 2026-08-18-emergency-handoff-3.yaml (escXML double-quote; undo snapshot-once; persistLocal after undo/redo/reset; skills repo single-remote personal; shared-checkout multi-session authorship trap; eden-selfcheck one-rewrite behavior; port 8815 server restart); note these last groups belong to OTHER sessions' work — sort them honestly in 2b (machine kind where evidence shows, ruling where they encode Zee's calls).
  
  9. Optional Next Step:
     Continue the skill exactly where it left off: **Step 2b** — write `$SCRATCH/safe-close-learnings.json` (fold in all 16 gathered candidates per the skill: "Give each one the SAME kind/evidence/checks treatment below as anything you personally observed this session, and include it in the SAME safe-close-learnings.json array"; add this session's own machine facts, chiefly the cap-survives-compact observation and the FruitSync handoff-clobber collision), then **Step 2c** — run `python3 "$RESDIR/resurrect-view.py" --color never > "$SCRATCH/close-book.txt"` + `--json > "$SCRATCH/close-book.json"`, show the book verbatim in one fenced block, and ask Zee in plain text: "Park this tab's work where? Type the NUMBER of a project above, or `0` for a new project of its own." — then STOP for his typed reply (never AskUserQuestion; resolve the number against close-book.json's `pick_number` exactly; build `PARK_ARGS` as an ARRAY). The destination question is a genuine human-gate stop even under autopilot, per the skill: "An auto-answered destination would file real work onto a project Zee never chose — the failure this step exists to prevent."
  </summary>
