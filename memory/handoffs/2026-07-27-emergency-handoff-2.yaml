timestamp: "2026-07-27T13:15:00-06:00"
status: "active"
type: "emergency-manual"
trigger: "acos-handoff-agent"
session_id: "unknown"
estimated_tokens: 0

session_summary: |
  This tab was resurrected via /acos-resurrect into "OKOA Works" (project root
  stays "/Users/zee/Documents/Vibe Coding/ACOS 3.0" per cwd, but ALL business
  output lives outside the repo in ~/Documents/OKOA/). The session built a
  fraud-representation evidentiary record for the Ascent / Wolfgramm / Beehive
  matter: every borrower financial representation made in recorded Fireflies.ai
  meetings before OKOA's Senior loan approval date of 2024-02-09, delivered as
  a 42-page PDF report, a 72-row Word meeting index, and three machine-readable
  JSON ledgers, all in "~/Documents/OKOA/Ascent Fireflies Search/". Separately,
  a small side-thread hardened the personal ~/.claude/skills/restate/SKILL.md
  with a new hard "pause, change nothing" rule at Zee's instruction. Nothing
  from either thread has been committed to git (business deliverables are
  outside the repo entirely; the restate skill lives outside the repo too,
  under ~/.claude/skills/). The repo's own working tree carries many
  unrelated pre-existing modifications from other prior sessions (research-
  riffs, axiom-synthesis, Website Builder PRD, resurrection scripts) that are
  NOT part of this session and must not be conflated with it.

current_work:
  slice_id: "unknown (ad hoc business-deliverable work, not tracked by any planning/ slice/story/epic — confirmed no planning/ directory references Ascent/Wolfgramm/Fireflies)"
  story_id: "unknown"
  epic_id: "unknown"
  status: "ready_for_review"

completed_this_session:
  - "Built a 91-meeting Fireflies.ai corpus for the Ascent/Wolfgramm/Beehive matter (72 CORE, 14 mention-only, 5 flagged/added), spanning 2022-11-15 to 2024-02-09, 40,251 spoken lines, 3,721 minutes — via build-corpus.ts against the local Fireflies cache (~/.fireflies-cache/, last refreshed 2026-07-22T18:04:56.763979+00:00)."
  - "Extracted and hand-curated an 80-statement claims ledger of borrower financial representations, then verified EVERY quote verbatim (quote + speaker + meeting + timestamp) against source transcripts via verify-claims.ts, producing claims-verified.json with 0 problems before any render."
  - "Built a party map (parties.json / party-map.json) resolving who-is-who across 6 label variants for Phil Wolfgramm, a dial-in-number identity for Russ Handy (16198083767, confirmed by Koloa on 2023-11-02), and normalising Fireflies speaker labels that contain non-breaking spaces (\\xa0)."
  - "Resolved several identity traps: Darwin Fielding = seller of Ascent to Wolfgramm and borrower on the prior Benefit Street loan, NOT a Wolfgramm principal; David Edwards = OKOA's own outside accountant (not the borrower's CFO) despite producing 133 of 1,227 candidate lines; 'Cole' could not be identified in any of the 91 meetings; no 'Zach' found in the OKOA-side speaker roster (OKOA side = Jason Meyer, Ty, Brad Heitmann per Zee's instruction)."
  - "Rendered the 42-page PDF report via a two-pass build (build-report.ts -> report.html -> node ~/.claude/scripts/html-to-pdf.js, margin 0.75) with a contents-page-numbers pass using pagemap.json, then verified the printed page numbers against the finished PDF (pass1.pdf/pass1.txt/pass2.txt in the build folder)."
  - "Built the separate 72-row 'Ascent-Wolfgramm-Beehive Meeting Index' Word document via build_docx.py (python-docx, fixed tblLayout + rewritten tblGrid so Word does not autofit-ignore cell widths)."
  - "Copied all build scripts and data OUT of the ephemeral session scratchpad into a permanent 'build (reproducible source)' subfolder of the deliverables directory, with a README.md documenting the exact order of operations, rendering commands, and dependencies — this was an explicit trap/risk called out for this session and IT HAS ALREADY BEEN ADDRESSED (confirmed: files present, mtimes 13:11, all after the 13:08 report render)."
  - "Hardened ~/.claude/skills/restate/SKILL.md Step 1 with a HARD rule (added 2026-07-27, confirmed present lines 26-37): a /restate turn makes NO changes of any kind (no Write/Edit/file creation/move/delete/git), including changes requested in the same message as the /restate — read-only verification permitted only where needed to restate accurately."

files_modified:
  - path: "~/Documents/OKOA/Ascent Fireflies Search/Ascent Representation Record (pre-Senior loan 2024-02-09).pdf"
    changes: "New 42-page main report, 1,241,413 bytes, last written 13:08 today."
  - path: "~/Documents/OKOA/Ascent Fireflies Search/Ascent-Wolfgramm-Beehive Meeting Index (pre-Senior loan 2024-02-09).docx"
    changes: "New 72-row CORE meeting index, 44,298 bytes, last written 11:41 today."
  - path: "~/Documents/OKOA/Ascent Fireflies Search/claims-ledger (machine-readable).json"
    changes: "New machine-readable ledger of the 80 verified representations, 47,760 bytes."
  - path: "~/Documents/OKOA/Ascent Fireflies Search/meeting-corpus (machine-readable).json"
    changes: "New machine-readable corpus of the 91 meetings, 48,269 bytes."
  - path: "~/Documents/OKOA/Ascent Fireflies Search/party-map (machine-readable).json"
    changes: "New machine-readable party/identity map, 16,010 bytes."
  - path: "~/Documents/OKOA/Ascent Fireflies Search/build (reproducible source)/*"
    changes: "New: 23 files (build-corpus.ts, extract-borrower.ts, who-is-who.ts, verify-claims.ts, build-report.ts, build_docx.py, claims.json, claims-verified.json, corpus.json, parties.json, notes.json, roster.json, pagemap.json, report.html, plus scan-* / core-set.* / final-list.ts / check-cmb-cpace.ts / cmb-detail.ts helper scripts) copied out of the session scratchpad, with a new README.md documenting build order and dependencies. This is a permanent, git-untracked (business-output) directory, not part of the ACOS 3.0 repo."
  - path: "~/.claude/skills/restate/SKILL.md"
    changes: "Step 1 rewritten to 'PAUSE — CHANGE NOTHING (HARD, added 2026-07-27 at Zee's instruction)': zero side-effecting changes during a /restate turn, even ones requested in the same message; read-only inspection permitted only when unavoidable. File mtime confirmed 2026-07-27 12:17:16. This file also carries an unrelated, PRE-EXISTING step-6 hard delivery rule from 2026-07-26 (restatement must be final turn text, no trailing tool call) that belongs to a different session's work (see the same-day handoff memory/handoffs/2026-07-27-emergency-handoff.yaml for that thread's detail) — do not conflate the two edits."

decisions_made:
  - "None logged in memory/decisions/ today (most recent decision file is 2026-07-05-eternity-protocol-nonfiring-audit.md) — this is ad hoc business-deliverable work outside the ACOS decisions/evidence pipeline, consistent with 'OKOA Works' project convention of living outside the repo."
  - "Zee-directed: /restate turns must make zero side-effecting changes, full stop, even for changes requested in the same message (captured in ~/.claude/skills/restate/SKILL.md Step 1, 2026-07-27)."

blockers:
  - "Zee has documents/evidence he intends to test the 80 logged representations against (to add a contradiction column), but has not yet pointed the session at those financials — this comparison pass has NOT been started."
  - "Post-loan scope (52 meetings after 2024-02-09 that touch CMB Regional Centers or C-PACE, including 'Okoa v. Wolfgramm Call' 2026-06-12 and 'Call Re Okoa Complaint' 2026-06-29) is explicitly NOT yet authorised — Zee said 'I will let you know about the post loan meetings later.'"
  - "The main PDF's opening (pages 1-4) has not yet been confirmed as the right weight/tone for Zee's boss — this is an open review item, not a technical blocker."

next_actions:
  - "Wait for Zee's review of PDF pages 1-4 (opening/framing) and adjust weight/tone for his boss if he asks."
  - "When Zee provides supporting financial documents, build a contradiction column against the 80-item claims ledger (claims-ledger (machine-readable).json / build/claims-verified.json) — flag apparent tensions already noted (buyer cash-in-project 19M vs ~14M; NOI ~6M to lenders vs 4-5M to appraiser; cap-rate pressure vs appraised value) as first candidates to check against real pay applications/financials."
  - "Do NOT touch the post-2024-02-09 (post-loan) meeting scope until Zee explicitly authorises it — he said he'd raise it later."
  - "If the scratchpad build artifacts are still needed beyond this session, note they are ALREADY safely copied to '~/Documents/OKOA/Ascent Fireflies Search/build (reproducible source)/' with a README — no further scratchpad-rescue action needed for this thread."
  - "Separately (different session's unfinished thread, not this session's primary work — flagged here only because it touches the same restate file): a same-day earlier handoff (memory/handoffs/2026-07-27-emergency-handoff.yaml) records an in-progress acos-logo-forge rebuild with a stale-server blocker on port 8815 — unrelated to the Ascent work, listed for completeness only, do not merge the two threads."

context_for_next_session: |
  The primary deliverable of this session is COMPLETE and sitting in
  "~/Documents/OKOA/Ascent Fireflies Search/": a 42-page PDF ("Ascent
  Representation Record (pre-Senior loan 2024-02-09).pdf"), a 72-row Word
  meeting index, and three machine-readable JSON files (claims-ledger,
  meeting-corpus, party-map). The underlying analysis: OKOA Capital holds a
  $30m Senior loan plus preferred equity with the Wolfgramm/Ascent borrower
  group (personal guarantees given); Zee believes the borrowers materially
  misrepresented financial position and collateral performance before the
  Senior loan closed on 2024-02-09. The report captures all 80 such
  representations found across 18 of 91 recorded Fireflies meetings
  (2022-11-15 to 2024-02-09), every one independently verified verbatim by
  script against the source transcript (see verify-claims.ts / claims-
  verified.json — 0 problems). Speaker breakdown: Koloa Wolfgramm 38, Phil
  Wolfgramm 34, Russ Handy 7, Ribble Sean M 1. Borrower principals per Koloa's
  own words (2022-12-12 introduction call): Phil Wolfgramm (CEO, father),
  Koloa Wolfgramm, Russ Handy — the original founders. Two identity traps were
  resolved and must NOT be re-litigated: Darwin Fielding is the SELLER of
  Ascent to Wolfgramm and a borrower on the PRIOR Benefit Street loan, not a
  Wolfgramm principal; David Edwards is OKOA's OWN outside accountant despite
  producing the largest single share of candidate lines (133 of 1,227), not
  the borrower's CFO. 'Cole' could not be found anywhere in the corpus, and no
  'Zach' appears on the OKOA side (Jason Meyer, Ty, Brad Heitmann per Zee).
  The standout findings worth leading with if Zee wants a tighter opening:
  (1) the 2023-11-27 appraiser call where Phil Wolfgramm pushed to cut the
  cap rate from 8.5-9% to 5-6%, calling 8.5% "unreasonable"; (2) appraised
  value drift from Phil's "110 or 120" (2023-10-19) to Nuveen's "came back at
  103" (2023-12-01); (3) the 2023-11-16 Ballard Spahr exchange where Erin
  Kadowaki states the EB-5 letter was not a commitment and Phil answers
  ambiguously; (4) EB-5 funding falling from 28M to 20M, backfilled by an 8M
  OKOA note; (5) lien waivers reported as fully collected on 2023-10-24 yet
  still being forwarded on the Senior loan date itself, 2024-02-09; (6) and
  (7) two apparent numeric tensions flagged (not yet resolved) — buyer cash-
  in-project ~19M (2023-10-05) vs ~14M (2023-11-09), and NOI ~6M reported to
  lenders (2023-11-02) vs ~4-5M reported to the appraiser (2023-11-27) — both
  need checking against real pay applications before being called
  contradictions, since reporting periods may differ. The 2022-12-12
  introduction call alone contributed 13 of the 80 statements (headline
  numbers: 200M in acquisitions, 250M under contract, an "all cash close" on
  the Waldorf property). Two things are explicitly OUTSTANDING and must not be
  started without Zee's go-ahead: (a) a contradiction column against real
  financials — Zee said he has documents to test the claims against but has
  not yet pointed the session at them; (b) the 52 post-2024-02-09 meetings
  touching CMB Regional Centers / C-PACE (including two meetings literally
  named for the resulting dispute: "Okoa v. Wolfgramm Call" 2026-06-12 and
  "Call Re Okoa Complaint" 2026-06-29) — Zee deferred this explicitly. The one
  open, low-stakes review item is whether the PDF's pages 1-4 (the opening)
  are the right weight for Zee's boss. On process risk: the build scripts and
  intermediate JSON that produced these deliverables lived in the EPHEMERAL
  session scratchpad
  (/private/tmp/claude-501/-Users-zee-Documents-Vibe-Coding-ACOS-3-0/b3d0de8f-c541-45f2-91b0-bfe346cdd9a9/scratchpad/)
  and have ALREADY been copied out, with a README, into
  "~/Documents/OKOA/Ascent Fireflies Search/build (reproducible source)/" —
  confirmed present, so this risk is already retired for this thread. Fireflies
  speaker labels contain non-breaking spaces (\xa0); any future lookup against
  this corpus must normalise whitespace or it will silently miss matches (Phil
  Wolfgramm alone has 6 label variants; Russ Handy also appears under a raw
  dial-in number 16198083767). None of this work touches the ACOS 3.0 git
  repo — it is pure business-deliverable output per the 'OKOA Works' project
  convention, so there is nothing to commit for this thread; the repo's git
  working tree changes visible in `git status` belong to other, unrelated
  sessions (research-riffs, axiom-synthesis, Website Builder PRD, resurrection
  scripts) and must not be swept into anything related to this work.
  Separately, a small unrelated edit landed in
  ~/.claude/skills/restate/SKILL.md today (Step 1, "PAUSE — CHANGE NOTHING")
  at Zee's direct instruction; it coexists with an earlier, different
  session's Step 6 edit to the same file (see memory/handoffs/2026-07-27-
  emergency-handoff.yaml, session 8509526c-8120-4699-b519-ef40518dff10, whose
  own subject matter — an acos-logo-forge rebuild and a Logo Builder project —
  is entirely unrelated to the Ascent work and must not be conflated with it).

reconstruction_sources:
  - "git status --short (repo: ACOS 3.0) — confirms no OKOA/Ascent/Wolfgramm files inside the repo; unrelated pre-existing modifications from other sessions present"
  - "git log --oneline -20 (repo: ACOS 3.0) — most recent commit 551301a is Website Builder PRD promotion, unrelated to this session"
  - "git diff --stat HEAD~5..HEAD and git diff --name-only HEAD~3..HEAD (repo: ACOS 3.0) — confirms recent repo churn is Website Builder / research-riffs / resurrection scripts, not this session's work"
  - "ls -lt planning/ — no slice/story/epic tracks Ascent/Wolfgramm/Fireflies work (ad hoc business deliverable, outside ACOS planning hierarchy)"
  - "ls -lt .acos/evidence/ — most recent dated 2026-07-18, nothing from today; confirms this work bypassed the ACOS evidence pipeline (expected for OKOA Works business output)"
  - "ls -lt memory/decisions/ — most recent 2026-07-05, nothing from today"
  - "ls -lt memory/handoffs/*.yaml *.md (excluding *.resume.md) — found a same-day prior handoff memory/handoffs/2026-07-27-emergency-handoff.yaml (session 8509526c-8120-4699-b519-ef40518dff10, unrelated Logo Builder/acos-logo-forge/restate-step-6 thread), read in full for cross-reference/disambiguation"
  - "ls -la .acos/state/ — routine runtime state, nothing specific to this session's work"
  - "ls -la '/Users/zee/Documents/OKOA/Ascent Fireflies Search/' — confirmed all 5 top-level deliverables present with sizes/mtimes (PDF 1,241,413B @13:08; DOCX 44,298B @11:41; 3 JSON ledgers @12:37-12:48; 'build (reproducible source)/' dir @13:11)"
  - "ls -la '/Users/zee/Documents/OKOA/Ascent Fireflies Search/build (reproducible source)/' — confirmed 23 files present, all mtime 13:11 (after the 13:08 report render), i.e. the scratchpad-copy-out risk is already retired"
  - "Read '/Users/zee/Documents/OKOA/Ascent Fireflies Search/build (reproducible source)/README.md' — full read, documents the 9-step build order, rendering commands, and dependencies (bun, python3+python-docx 1.2.0, poppler pdftoppm/pdftotext, ~/.fireflies-cache/)"
  - "ls -la '/private/tmp/claude-501/-Users-zee-Documents-Vibe-Coding-ACOS-3-0/b3d0de8f-c541-45f2-91b0-bfe346cdd9a9/scratchpad/' — confirmed the original scratchpad build files still exist there too (redundant with the copied-out version; scratchpad itself remains ephemeral/at-risk, but no longer the only copy)"
  - "ls -la /Users/zee/.claude/skills/restate/ and stat of SKILL.md — confirmed mtime 2026-07-27 12:17:16"
  - "Read /Users/zee/.claude/skills/restate/SKILL.md (head -c 3000) — confirmed Step 1 'PAUSE — CHANGE NOTHING (HARD, added 2026-07-27 at Zee's instruction)' text present verbatim"
  - "User-supplied session context in the handoff-agent task prompt (git-state and file-state claims cross-checked above against disk where possible; narrative facts about the Fireflies corpus — meeting counts, speaker tallies, dates, quotes — could not be independently re-verified by this agent since it lacks Task/spawn access to re-run verify-claims.ts, and are reproduced here as reported)"
