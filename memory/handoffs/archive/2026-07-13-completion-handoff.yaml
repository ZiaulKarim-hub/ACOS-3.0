timestamp: "2026-07-13"
status: "completed"
session_summary: "Built the live pool-backed IC committee room (moderator-free ~5-7s turns), tooled all 8 seats (+2 new skills), added the burden-of-proof chair doctrine + distinct voices + reading-level dial, switched the live :8930 room to the pool engine, committed + pushed, and produced a self-contained shareable solo bundle."
origin_session: "dc2820e3-e2de-4ab8-9f2b-6cc7e9b06b35"

current_work:
  slice_id: null
  story_id: null
  epic_id: "EPIC-003-investment-committee"
  status: "completed"

completed_this_session:
  - "Committee-room speed engine: ic-pool.py (warm `claude -p --safe-mode` subprocess pool, no API key, ~4.9-6.7s turns, moderator out of the loop) + ic-live.py (sole chair-inbox consumer; grounded per-seat prompts + burden-of-proof doctrine; warm-cache serve) + ic-launch.sh launcher; ic_turns.py single-consumer lock; runner pace fix 55->135; ic-server poll tightening."
  - "Chair-input doctrine flipped: seats BRING evidence (accept-unless-contradicted); a chair personal ASSURANCE counts as evidence (CP-verified-by-chair); proven live (Credit accepted comps but challenged with $325k/$365k file facts)."
  - "Distinct per-seat VOICE blocks in all 8 ic-0N agents; open-call UX (call once, keep typing, Esc ends)."
  - "All 8 seats tooled: #2 PRISM, #4 title-sleuth+legal-analysis, #7 hypercore, #8 deal-analyzer, #3 data-extractor, #5 acos-peril-scan (NEW), #6 acos-sponsor-verify (NEW) — the 2 new skills authored via a parallel workflow."
  - "Reading-level dial (0 Expert .. 5 Very simple) in the room + injected into pool prompts (ic-live LEVELS)."
  - "Switched the live :8930 Alaska room to the pool engine (killed old moderator ic-server pid 2508, reset clean, launched via ic-launch.sh)."
  - "Committed (cc62ecd, 91 files) + pushed to origin + personal remotes."
  - "Built a SELF-CONTAINED solo share bundle: acos-investment-committee.zip (918K) with all 8 tools folded into tools/ + 11 agents repointed; no deal data."

files_modified:
  - path: ".claude/skills/acos-investment-committee/scripts/committee-room/"
    changes: "NEW ic-pool.py, ic-live.py, ic-launch.sh; ic_turns.py (cache+lock+level), ic-turn-daemon.py, ic-meeting-runner.py (pace), ic-server.py (poll), meeting_template.html (open-call + thinking + reactions-stagger + level dial), build_meeting.py, meeting_state.py; .gitignore"
  - path: ".claude/agents/ic-01..ic-08 + ic-10 + ic-research-bot"
    changes: "Voice blocks, burden-of-proof chair doctrine, per-seat tool embeds"
  - path: ".claude/skills/{acos-peril-scan,acos-sponsor-verify}"
    changes: "NEW authored skills (Insurance + Fraud seat tools)"
  - path: ".claude/skills/{title-sleuth,prism-financial-modeler,okoa-deal-analyzer}"
    changes: "vendored into the repo"
  - path: ".claude/skills/acos-investment-committee/SKILL.md"
    changes: "Mode B engine + chair doctrine + launch (background daemons; cmux tab via user's focused shell)"

decisions_made:
  - "Burden of proof is on the SEATS, not the chair (accept-unless-contradicted) — user correction."
  - "Fully-live model: no auto-played script, no auto-chair; the pool generates each turn on demand."
  - "Pool uses --safe-mode: strips project hooks (incl. the EDEN reading filter that leaked into seats) + speeds warmup, keeps OAuth; NO api key ever."
  - "cmux tab cannot be opened from the Claude Bash subprocess (caller:null / TabManager not available); engine runs as background daemons regardless, tab is opened by the user from their own focused shell."
  - "Share as ONE self-contained skill (tools folded into tools/) built bundle-only; live repo untouched."

blockers: []

next_actions:
  - "Optional research items not yet built: (3) speculative pre-generation, (4) PENDING->pool soft timeout, (5) fuller Opus effort-tiering, (6) UX perceived-speed pack."
  - "Send acos-investment-committee.zip to the boss (self-contained; OKOA-internal tools folded in; hypercore needs OKOA access)."
  - "Add the 'ask-the-basis' + 'meaningful hand-raise' behaviors if desired (discussed, not built)."

context_for_next_session: |
  The live IC committee room now runs on the warm-pool engine at http://127.0.0.1:8930/ (started via
  scripts/committee-room/ic-launch.sh; pool = ic-pool.py, consumer = ic-live.py, server = ic-server.py).
  Turns are generated by warm `claude -p --safe-mode` workers (~5-7s, no API key), so the main session is
  out of the live loop. Reading-level dial 0-5 in the room. Full detail in memory project_acos_ic_committee_room.
  The unrelated 2026-07-13 keychain-eternity diagnosis handoff was LEFT ACTIVE on purpose (different
  session, unresolved).
