timestamp: 2026-08-06T03:07:33+00:00
status: parked
type: close-project
session_id: 2d67bfab-e4f2-4342-9104-bdd762f0535a
slug: 2026-08-06-Resurrection-Protocol-close
project_root: /Users/zee/Documents/Vibe Coding/ACOS 3.0
session_summary: Safe close of Resurrection Protocol — branch=acos-deficiency-fixes-2026-06-04 head=d3ee7714ce0f0bf822035068a5a79775c6d8cf2f dirty_count=12; next: Open a second tab on one project, work in both, close both, reopen and verify carryover
git_state: branch=acos-deficiency-fixes-2026-06-04 head=d3ee7714ce0f0bf822035068a5a79775c6d8cf2f dirty_count=12
git:
  branch: acos-deficiency-fixes-2026-06-04
  head: d3ee7714ce0f0bf822035068a5a79775c6d8cf2f
  dirty_count: 12
  dirty_listed: listed 12 of 12
dirty_files:
  -  M "Logo Builder/brandsync/commands.jsonl"
  - ?? memory/handoffs/2026-08-05-emergency-handoff-2.yaml
  - ?? memory/handoffs/closed/2026-07-21-OKOA-Works-close/.reentry-consumed
  - ?? memory/handoffs/closed/2026-07-22-OKOA-Works-close/.reentry-consumed
  - ?? memory/handoffs/closed/2026-07-26-OKOA-Works-close/.reentry-consumed
  - ?? memory/handoffs/closed/2026-07-28-OKOA-Works-close/.reentry-consumed
  - ?? memory/handoffs/closed/2026-08-02-OKOA-Works-close/.reentry-consumed
  - ?? memory/handoffs/closed/2026-08-04-OKOA-Works-close/.reentry-consumed
  - ?? memory/handoffs/closed/2026-08-05-OKOA-Works-close-2/
  - ?? memory/handoffs/closed/2026-08-05-OKOA-Works-close/
  - ?? memory/handoffs/closed/2026-08-05-Resurrection-Protocol-close/
  - ?? memory/handoffs/closed/2026-08-06-OKOA-Works-close/
next_actions:
  - Open a second tab on one project, work in both, close both, reopen and verify carryover
next_action: Open a second tab on one project, work in both, close both, reopen and verify carryover
intent_core: |
  next_action: Open a second tab on one project, work in both, close both, reopen and verify carryover
  project: Resurrection Protocol
  decisions: |
    - Built the ENTIRE 2026-08-04 brief in one day: MW-A..E and KB-A..E, plus MW-A2
      (same-day close collision) found while testing. All committed and pushed to
      personal: 275989d (bulk, by the OKOA Works window), 092fcb8 (MW-E feeder),
      3f73cc8 (name-ambiguity refusal), aa1553d ([folder] tags).
    - Reentry notes are project-filtered and MERGED at adopt: ownership ladder is
      .project-uuid marker > registry last_close.reentry_path > slug name match, and
      the slug rung REFUSES when a display name is shared by 2+ live rows.
    - Knowledge is a per-project GRAPH at ~/.acos/knowledge/<project_uuid>/ —
      append-only facts/entities/edges. Supersede happens ONLY when the caller sets
      single_valued=true; subject-based supersede collapsed 25 facts to 3.
    - The "learned since you were last here" watermark advances on REOPEN only,
      never at close — advancing at close made the digest permanently empty.
    - Zee's rulings 2026-08-05: NEVER suggest retiring registry rows; N tabs of one
      project must consolidate into ONE store/row/merged notes; a folder basename is
      never a project identity ([folder] tag now marks the 21 placeholder rows).
  rejected_alternatives: |
    - One folder per project (Zee asked): rejected — label the notes instead of
      rebuilding the room; 19 rows share the ACOS 3.0 root and splitting kills
      accumulation. He accepted.
    - Per-item approval of captured learnings: rejected by Zee in the brief (D3/D4)
      — machine-checkable facts write silently; rulings asked, max 2 per session.
    - Newest-first when the backfill cap trims: rejected — oldest decisions are the
      load-bearing ones.
  traps: |
    - Eden protocol level 3 is ACTIVE globally (~/.claude/state/eden-level): top-level
      replies must be plain language, short sentences, no unexplained jargon.
    - ANOTHER WINDOW commits and pushes on this same branch (275989d, ef22b3e came
      from the OKOA Works window mid-session). Check git log before assuming state.
    - MW-E is inert until BOTH the switch file ~/.acos/windows/collision-warning.enabled
      exists AND the PostToolUse hook is registered. Off is the designed default.
    - DP2 is still unanswered: never set RESURRECTION_DP2_CONFIRMED, never auto-close;
      the user closes tabs by hand.
    - rm -rf ~/.acos/knowledge was safe TODAY only because backfill could rebuild it;
      once sessions add their own learnings, that data exists nowhere else.
  open_questions: |
    - DP2 battery, launcher live rename, and MW-E watcher registration remain
      user-scheduled live tests; Zee will run them on his own time.
    - Whether Zee strikes any of the 371 backfilled facts when he reads the digest.
