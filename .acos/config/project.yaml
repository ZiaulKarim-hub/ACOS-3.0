# ACOS Project Configuration
project:
  name: "ACOS 3.0 Self-Improvement"
  initialized: true
  acos_version: "3.0.0"

settings:
  review_depth: maximum
  auto_evidence: true

status:
  phase: in_progress
  current_epic: "EPIC-003"
  current_story: "STORY-003-002"
  current_slice: null
  last_completed_story: "STORY-003-001"
  last_completed_slice: "SLICE-003-001-07"
  last_reconciliation: "2026-06-04"
  last_reconciliation_note: "STORY-003-002 (two-pane shell + canvas + drag-drop + corner gestures) PLANNED. 7 mandatory slices + 1 conditional contingency slice (01b, fires only if BG3-audit finds multi-layer background-image rules in real Brad CSS). MANDATORY SVG sanitize-at-render acceptance criterion baked in as the consumer-side half of the STORY-003-001 Option B / CWE-79 contract (enforced by tests/editor/forbidden-patterns.test.ts grep-asserting absence of forbidden innerHTML/outerHTML/document.write of rawMarkup outside the sanitize boundary). All 5 forward findings from STORY-003-001 closure folded into SLICE-003-002-01 (S1 IPv4-mapped IPv6 denial, F2-neg negative-path test, bracket-comment correction, fixture-size real-Brad recalibration, BG3-audit kickoff gate). State store: Zustand. Drag/resize: react-rnd. SVG sanitize: DOMPurify (SVG profile). Story effort estimate: L. Plan file: planning/stories/STORY-003-002-canvas-drag-drop-corner-gestures.yaml. Next: /acos-plan slice for SLICE-003-002-01 (parser prep tick)."
