{
  "name": "acos-reverse-cleanroom-scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TypeScript drivers for the /acos-reverse-cleanroom skill: dirty-room capture, HAR→OpenAPI inference, parity oracle, dirty-fingerprint builder, and the fail-closed egress guard. Run with bun (no build step).",
  "scripts": {
    "capture": "bun capture.ts",
    "recon": "bun capture.ts --recon",
    "select-layers": "bun select-layers.ts",
    "har2oas": "bun har-to-openapi.ts",
    "parity": "bun parity.ts",
    "parity-bind": "bun parity-bind.ts",
    "fingerprint": "bun fingerprint-build.ts",
    "guard": "bun egress-guard.ts",
    "gate": "bun gate.ts",
    "capture-diff": "bun capture-diff.ts",
    "selftest": "bun selftest.ts"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  }
}
