# Research brief — <topic>

**Session:** <session-id>
**Date:** <YYYY-MM-DD>
**Tier:** <lite|standard|deep>

## Question of record
<The single question this research exists to answer. One sentence. If it cannot
be written as one sentence, the scope is still too loose.>

## What a good answer looks like
<Concretely: a recommendation? a comparison? a risk list? a go/no-go? Name the
decision this feeds, and who makes it.>

## The real decision (reframe guard)
<What decision does this feed, and is the question above a stand-in for a bigger
one? If "which engine" is really a proxy for "the whole shipping model", say so
here and research the real decision. A covered proxy is still the wrong answer.>

## Given vs. assumed (I10 — do not blur these)
**Given — what the user actually said** (constraints, priorities, exclusions you
were told, verbatim):
- <…>

**Priority order, as stated by the user** (not inferred; "unranked" is a valid,
honest entry — do not invent a cost-first or any-first lens the user never set):
- <…>

**Assumed — anything not given that the research leans on** (every item here is
logged as an `assumption` ledger entry and surfaced, never delivered as a given):
- <…>

## In scope
- <…>

## Explicitly out of scope
- <…>
<Out-of-scope is load-bearing. It is what stops the panel from drifting, and it
is what the auditor checks drift against. This list is the ONLY source of
exclusions — when the user relaxes a rule, delete it here and log the change. An
exclusion kept only in memory will silently hold options out of the answer.>

## Evidence bar
<maximum: 3+ independent sources for material claims |
 standard: 2+ sources for key claims, single source acceptable for detail |
 exploratory: single sources acceptable, breadth over depth>

## Known constraints and must-cover items
- <anything the reader already named — a tool, a vendor, a regulation, a prior
  decision. These become coverage dimensions, so nothing named here can be
  quietly skipped.>

## Volatile facts to re-verify before reliance
- <pricing, limits, availability, version numbers — anything that moves>

## Coverage dimensions (derived)
<The checklist the research is measured against. Each becomes a row in
coverage.json. A dimension that is never probed can never read as covered.>

| id | dimension | why it must be probed |
|---|---|---|
| <id> | <name> | <why> |
