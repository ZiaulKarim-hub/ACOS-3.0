{
  "_note": "EXPECTED entity schema (data-model.md A4). Likely/expected modeling target until API access (OQ1). Authored in SLICE-HCA-02; validated in SLICE-HCA-05.",
  "entity_type": "payment",
  "version": "0.1.0-placeholder",
  "primary_key": "payment_id",
  "expected_fields": {
    "payment_id": "string",
    "loan_id": "string",
    "amount": "number",
    "currency": "string",
    "payment_date": "string",
    "allocation": "object",
    "status": "string"
  },
  "required_fields": [
    "payment_id",
    "loan_id",
    "amount",
    "currency"
  ]
}
