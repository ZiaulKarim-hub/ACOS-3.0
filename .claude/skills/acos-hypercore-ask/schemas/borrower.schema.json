{
  "_note": "EXPECTED entity schema (data-model.md A2). Likely/expected modeling target until API access (OQ1). `contact` is PII and is minimized/redacted in derived views (data-model.md D). Authored in SLICE-HCA-02; validated in SLICE-HCA-05.",
  "entity_type": "borrower",
  "version": "0.1.0-placeholder",
  "primary_key": "borrower_id",
  "expected_fields": {
    "borrower_id": "string",
    "legal_name": "string",
    "entity_type": "string",
    "jurisdiction": "string",
    "relationship_manager": "string",
    "contact": "string",
    "loan_ids": "array"
  },
  "required_fields": [
    "borrower_id",
    "legal_name"
  ],
  "pii_fields": [
    "contact"
  ]
}
