{
  "_note": "EXPECTED entity schema (data-model.md A1). Field names/types are the likely/expected modeling target until partner-gated API access confirms them (OQ1). Authored in SLICE-HCA-02; VALIDATED against fixtures in SLICE-HCA-05 (schema + drift gate). This is a lightweight stdlib-checkable descriptor, not a JSON-Schema-draft document.",
  "entity_type": "loan",
  "version": "0.1.0-placeholder",
  "primary_key": "loan_id",
  "expected_fields": {
    "loan_id": "string",
    "facility_type": "string",
    "borrower_id": "string",
    "commitment_amount": "number",
    "funded_amount": "number",
    "outstanding_principal": "number",
    "currency": "string",
    "interest_rate": "number",
    "rate_type": "string",
    "origination_date": "string",
    "maturity_date": "string",
    "status": "string",
    "collateral_ids": "array"
  },
  "required_fields": [
    "loan_id",
    "outstanding_principal",
    "currency"
  ]
}
