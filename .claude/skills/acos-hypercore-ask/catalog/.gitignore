# Generated, regenerable artifacts (large; rebuilt from the tooling). Do not commit.
_introspection_current.json
_introspection_refresh.json
_candidates_refresh.json
catalog-candidates.json
probe-*.json
hypercore-catalog.yaml

# Committed durable outputs (machine index + human map + drift snapshot + enrichment overlay):
#   catalog-index.json, HYPERCORE-FIELD-MAP.md, catalog-paths.json, enrichment.json
