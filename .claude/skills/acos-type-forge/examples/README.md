# acos-type-forge — worked example

The complete reference instance (Cinzel Decorative → a custom titling face) is at:
`<your font-foundry working dir>`

It shows: source TTFs + OFL, the FontForge tracking + Shapely stencil-cut pipeline,
OFL-compliant rename, PIL specimens, and the browser editor instance. Use it as a
concrete end-to-end template.
