# acos-dataroom-v2 Python dependencies
# Install with: pip install -r requirements.txt

# Excel generation
openpyxl>=3.1.0

# PDF text extraction
pdfplumber>=0.10.0
pypdf>=4.0.0

# Image processing (for vision-bridge PNG rendering)
Pillow>=10.0.0
pdf2image>=1.16.0

# DOCX text extraction
python-docx>=1.0.0

# PPTX text extraction
python-pptx>=0.6.21

# OCR fallback (used when vision-bridge is unavailable)
pytesseract>=0.3.10

# YAML config support
PyYAML>=6.0

# Text encoding detection
chardet>=5.0.0
