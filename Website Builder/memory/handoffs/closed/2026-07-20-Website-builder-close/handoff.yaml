timestamp: 2026-07-20T18:11:44+00:00
status: parked
type: close-project
session_id: d2a4cca4-35d8-4b45-af3e-546336cc1b00
slug: 2026-07-20-Website-builder-close
project_root: /Users/zee/Documents/Vibe Coding/ACOS 3.0/.acos/swarm/swarm-20260718-022431
session_summary: Safe close of Website-builder — non-git; next: Decide whether to /acos-plan the v1 marketing-site builder (swarm report blueprint)
git_state: non-git project (no usable git state at root)
git:
  branch: null
  head: null
  dirty_count: 0
  dirty_listed: listed 0 of 0
dirty_files:
next_actions:
  - Decide whether to /acos-plan the v1 marketing-site builder (swarm report blueprint)
next_action: Decide whether to /acos-plan the v1 marketing-site builder (swarm report blueprint)
intent_core: |
  next_action: Decide whether to /acos-plan the v1 marketing-site builder (swarm report blueprint)
  project: ACOS Design-First AI Website Builder (research phase)
  decisions: |
    - Ran /acos-swarm-research (20 isolated agents + 1 synthesizer) on how to build a
      DESIGN-FIRST AI website builder as an ACOS skill. Scope: award-quality visual
      design (Awwwards/FWA tier); site types = marketing/brand (primary), game promo
      (FruitSync), and login web-app shells (design only, not backend).
    - Deliverable is the synthesis report at
      .acos/swarm/swarm-20260718-022431/synthesis/report.md — 28 cross-referenced
      findings, 11 conflicts, 14 data gaps, and a full build blueprint.
    - Recommended architecture = "Hybrid C": loan-doc-shaped router + phase
      orchestrators, concept gate + design-system-forge front end, two-tier QA loop
      (sighted inner Wigum + blind outer gate), per-type presets, v1->v3 ramp.
    - Recommended stack: Astro 5 static + Tailwind v4 @theme tokens + GSAP + Lenis
      (+ three.js lazy island); Cloudflare Pages deploy.
  rejected_alternatives: |
    - Genesis/synthesis component-tree architecture (Agent 17 Arch B) — higher effort,
      lower art-direction ceiling than Hybrid C.
    - Per-page multi-agent decomposition (planner/coder/reviewer) — evidence says a
      single-context sighted loop is cheaper AND higher quality; multi-agent only for
      phase isolation, parallel pages, and independent gates.
    - Next.js / un-themed shadcn for brand sites — server surface + sameness attractor.
  traps: |
    - This was RESEARCH only. Nothing was built; do not treat the blueprint as code.
    - The swarm's shared 200-call WebSearch budget was exhausted mid-run; agents
      15,16,18,19,20 (and parts of 12,13) worked via direct WebFetch only — coverage
      skews canonical over exploratory for those topics.
    - 8 agents were relaunched after a session-limit outage; all 20 findings verified
      on disk afterward.
    - Motion (animation) verification is the single largest engineering risk and an
      open research gap (Data Gap 2) — deterministic motion-lint carries most of v1.
    - A guided-reader server may still be running (state dir
      ~/.acos/state/guided-reader/report-md-8ccbb0/) with a 30-min idle auto-shutdown.
  open_questions: |
    - Does the user want to proceed to /acos-plan (vision->epics->slices) for v1, or
      keep the research as reference only?
    - Whether aesthetic (not fidelity) quality plateaus at the same 3-5 iterations
      (Data Gap 1) — instrument the v1 loop to measure.
    - Fontshare FFL current terms, FLUX.2/Veo commercial terms (Data Gap 11).
