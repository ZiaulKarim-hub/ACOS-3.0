# Website Builder PRD — open items after the harden pass

Deferred deliberately: 66 (15 need YOUR decision, 51 are unmeasured/cross-section)

## A. Needs a decision from Zee

- **[3]** Left open as an explicit 'no known mitigation / requires user decision' item inside §3.2: whether the Step-1 interview (§2) should ask about a given project's tertiary audience's known access needs and, if so, how that would tighten per-project gate thresholds in §13 — this PRD does not currently propose that mechanism and I did not fabricate one.
- **[2. Goals, non-goals, and success criteria]** S9's underlying measurement mechanism (telemetry/analytics existence, project-identifier persistence, window length) is left as an explicit open question with 'no known mitigation' rather than fabricated, because no other section of this PRD specifies a usage-tracking system and the product has no backend (NG3) — this requires an actual product/user decision, not an editorial fix.
- **[11]** Whether sibling-anchored free-positioning (§11.4 rule 1's original 'parent edge, sibling, or grid cell' language) ships at all: left as an explicit open question requiring user sign-off, with the subgrid-promotion compile strategy noted as unprototyped and 'no known mitigation beyond the idea stated' — this is a genuine unresolved product decision, not something this section can honestly close on its own authority.
- **[6]** Initial size/count of the font-catalog.json OFL shortlist (how many typefaces seed it) is left as an explicit open question with no known mitigation — it's a product decision this section can't default without fabricating a number
- **[6]** §17-O15 (whether '20 artworks' means 20 pieces or 20 style sets) is referenced and the Art-chunk budget is stated as provisional pending it, but not resolved here — it requires a user decision per §17's own framing
- **[13]** Motion-concurrency cap numbers in gate 4a are carried over from prior research, not benchmarked against this product's own render stack — left open in §13.11 as requiring either a user decision or a benchmarking pass
- **[7 — The design system inventory]** O31 (20 artworks total vs 20 per family) left open as a user decision rather than silently resolved. This revision adopts the 20-total reading only as the v1 hand-carry quota and flags the reduction against the FruitSync exemplar (231 sprites) as an explicit 'requires user sign-off' deviation notice.
- **[7 — The design system inventory]** O35 — the original contents of categories K and M are unrecoverable from the surviving text. §7.14/§7.15 are labelled a reconstruction, not a recovery, and require user confirmation.
- **[12 — Document model, persistence, and the LOCK/UNLOCK contract]** O33 — Astro/Vite build byte-reproducibility across two installs is NOT established by any source consulted for this PRD. §12.8 only constrains our generator's determinism. Left visible as an open question with a required Phase-0 spike, explicit reproducibility preconditions, and a documented normalised-comparison fallback that is flagged as weakening D3's proof and therefore requiring user sign-off. Not fabricated a source or a claim of reproducibility.
- **[12 — Document model, persistence, and the LOCK/UNLOCK contract]** O32 — no wide/xl override tier in v1; §10.1's 'full' previews base rules and carries no overrides. Flagged as requiring user decision because adding one introduces the only upward override in an otherwise desktop-down cascade.
- **[12 — Document model, persistence, and the LOCK/UNLOCK contract]** Step-5 regeneration review burden — the v1 policy makes a brand-new-direction regeneration a reviewed operation (per-node flag, LOCK blocked until acknowledged, bulk-acknowledge available). Marked 'requires user sign-off' in §12.16 because the user's vision step 5 might have assumed a new direction just applies; the PRD does not silently choose the frictionless-but-silent behaviour.
- **[10]** Whether 1440 should become a fifth live-switcher option — flagged as requires user decision, no mitigation implemented
- **[8. The component inventory]** v1 scope decision itself — left OPEN by design. The corrected v1 set is 87 items / 674 variants against the '~50 / ~430' §18 and §13 were sized on. The PRD does not choose between accept-and-re-baseline, demote-specific-rows, or split-v1a/v1b; it is marked '⚠ REQUIRES USER SIGN-OFF' and §8.8-X3 asks §18 to re-baseline or record demotions. Radio group and Toggle switch are named as non-demotable.
- **[8. The component inventory]** Who authors the variant axis schema (§8.6) — three candidates costed, PRD's inference is hand-authored-in-the-skill for determinism, but it is an unbudgeted effort line §18 does not carry. Left as new open question O33 with 'requires user decision', and as new risk R46.
- **[18]** O7 is narrowed, not closed. Lanes A and B are decided for v1 and Lane C is decided OUT with a runbook, but the residual case — a project with no asset library whose direction genuinely needs photographic/painterly raster — has no solution that preserves the paste-only path. Kept visible inline as new O32 with 'requires user decision per project' and 'no known mitigation'.

## B. Unmeasured, unsourced, or cross-section follow-ups

- **[1]** Whole-document gap 'sections 1-12.6 absent from reviewed text' — explicitly identified by the task instructions as a pipeline-truncation artifact, not a real defect against section 1; left untouched as instructed.
- **[3]** No entry existed under key '3' in gaps-by-section.json's bysec object (only sections 0,1,5,7,12-16,18,19 are present, consistent with the truncation bug covering 13-20 plus a few stray earlier keys) — so there were no critic-recorded blocking/major/minor gaps to close for this section beyond the newly-found one supplied directly in the task.
- **[2. Goals, non-goals, and success criteria]** The §4 Step 3 'one-paste protocol' naming mismatch is flagged and cross-referenced but not renamed here, since the mechanism's name and description live in §4, outside this section's scope; §4 should be revised in its own pass to either rename it or state the retry semantics explicitly.
- **[4 — The pipeline, all 8 steps]** Exact heat sizing in Step 4's bracket (e.g. whether Round 1 is literally 3+3+2+2 or some other split) is left as an inference tagged [I], since the true count depends on how many directions survive Step 2's shortlist floor at runtime -- the invariant that IS fixed (bracket must cover the full ~10, max 3 compared at full size per round) is stated as non-negotiable per D1, but the precise arithmetic is explicitly flagged as inferred, not fabricated as a hard number.
- **[4 — The pipeline, all 8 steps]** Whether the user can relax the ~10-direction floor below ~10 (an edge case introduced by tying the shortlist explicitly to D1) is left as a permitted-but-flagged deviation requiring the user's sign-off and a session.json record, rather than silently disallowed or silently allowed -- this is a new explicit escape hatch, not a resolved question, since the PRD gave no prior guidance on what happens if Stage A under-generates capsules.
- **[11]** Actually editing §12.3's persisted schema (anchor.to: "parent"|nodeId) to match the v1 restriction: out of scope for a section-11-only revision; flagged inline as a coordination requirement for §12's owner rather than silently drifting the two sections out of sync.
- **[6]** The exact real per-message/per-conversation output ceiling (§17-O2) remains genuinely unmeasured; the calibration protocol reduces the cost of not knowing it but does not resolve the underlying open question, which is inherited from §17 and explicitly left open rather than papered over
- **[6]** §17-O27 (whether the user's claude.ai plan includes Projects) is referenced as a variant that would shrink prompt bulk further, but not designed further since the answer is unknown
- **[6]** The hero-card glyph-subset size reduction for Chunk A's fonts is stated as a directionally-sound but unmeasured optimisation — flagged for measurement on first real run rather than asserted as a solved number
- **[6]** Recommended wording tightening of §19's A12 and §17-R3's 'smallest-first' mitigation phrase are both flagged inline as follow-up edits owed to those sections, but not performed — out of scope for a section-6-only revision
- **[9]** Kinetic type container's 8 named variants vs Text reveal/kinetics animation kind's 10 — left as an explicit OPEN QUESTION (no known mitigation) since no source material establishes the mapping; fabricating a reconciliation was avoided per the no-fabrication rule
- **[9]** Cursor-reactive container's 5 variants vs partial name-overlap with Hover micro-reactions' 10 — left as an explicit OPEN QUESTION for the same reason
- **[9]** 'easing matrix' and 'motion toggle' from §8's v1 list have no home in §9.2/§9.3 — left as OPEN QUESTIONS in new §9.7; recommended likely homes (§7 tokens; a site-facing UI component) but did not assign them since this section lacks authority over §7/§8/§10
- **[9]** Whether §10.8's v1 'In-editor Preview mode' plays motion or freezes it — surfaced explicitly but not answered, since the deciding text lives in §10.8 not §9
- **[9]** Cross-section edits this pass could not make directly (§5 new interview question on user-supplied footage, §5 question on mobile cursor substitute, §7 signature-moment sentence sync, §10.8/§18 PREVIEW MOTION priority bump) — each flagged inline as 'requires a matching edit in §X, not made in this pass' rather than silently left inconsistent
- **[13]** Exact LOCK wall-clock budget numbers (p50 90s/p95 180s) and the gate-20 sampling fallback are stated as inference, not measured — left as an explicit open item in new §13.11 pending a real multi-page prototype timing pass; no known mitigation beyond flagging it
- **[13]** Skip-link component-catalog entry (gate 11a) and pauseAffordanceRef schema field (gate 13a) and font-fallback-metrics token family (gate 21) all require edits in §8/§18, §9, and §7/§8 respectively — this subagent only owns §13, so these are documented as flagged cross-section coordination items in new §13.11 rather than fabricated as already resolved
- **[13]** A66/A67 reconciliation in §19 cannot be performed from this file — flagged as a required §19 edit in gate 20's row and again in §13.11
- **[13]** Motion-consistency 3-variant threshold in §13.8 is a carried-over default, not independently validated against user testing — stated as provisional in both §13.8 and §13.11
- **[5 — Step 1: the interview question bank]** Exact mapping of jurisdiction (G0) to specific legal regimes (GDPR/ePrivacy, UK GDPR, CCPA/CPRA, EU Accessibility Act) is left as an explicit open question requiring user/legal sign-off — this section captures the input (region) and flags the derivation as unresolved rather than fabricating a legal-compliance mapping it cannot verify.
- **[5 — Step 1: the interview question bank]** A19-A3's '≤45 answered questions' acceptance criterion is revised in this section's own honest computation to ≤55, but that criterion lives in §19, which this section does not have authority to edit — flagged for cross-section reconciliation, not resolved here.
- **[5 — Step 1: the interview question bank]** §17-R21's '78 questions, 40-80 minutes' and §18's 'Full interview (78 bank)' scope line both still cite the old, now-corrected figures — flagged for their owning sections to update; not edited here since they're outside section 5's file.
- **[5 — Step 1: the interview question bank]** Compatibility of the T1-T10 -> TS1-TS10 rename with any other PRD section or stored answers.json from prior projects (Step 0 warm-start) could not be verified — flagged as a cross-section grep-and-fix task this agent could not perform without visibility into those files.
- **[7 — The design system inventory]** Recorded critic gap #1 — 'Local Regeneration Mode mechanism undefined' (what model it calls, credentials, cost, output parity). Out of §7's scope: the mechanism belongs to §6.5 and §17-R7. §7 now leans on it explicitly (Lane A artwork, the v2/v3 deferral policy), so the gap has become MORE load-bearing, not less — noted inline in §7.9 and §7.18 so §6/§17 owners see the dependency, but not fabricated here.
- **[7 — The design system inventory]** O32 — the ~8KB median SVG art piece is an unmeasured placeholder and is labelled as such in-table. Not fabricated into a defensible figure; the whole §7.17.2 payload arithmetic is marked as resting on it.
- **[7 — The design system inventory]** O33 — two CSS facts stated from working knowledge (container-query conditions reject var(); light-dark() safety against the pinned compiler). Both hedged in place and routed to a verification question rather than upgraded to [V].
- **[7 — The design system inventory]** O36 — whether icon.family-spec / mark.logo-lockups / system.voice-and-microcopy should be promoted into a 27-slot hash-bearing vector. This revision chose the direction-bound-artefact treatment and stated its reasoning as inference; the alternative is left open rather than closed by assertion.
- **[7 — The design system inventory]** The eight named §8 edits (icon set rationale, OG 3-of-6 annotation, divider composition note, pattern count 20→24, empty/error 'places one of', and the blank Team template rationale — suggested text supplied) cannot be applied from this section; they are listed as required actions in §7.19.
- **[7 — The design system inventory]** The three named cross-section edits in §7.17.2 (§6.3's 'Art | Variable' chunk row, §20.3's U8/U10 re-derivation, §11.5's cross-reference to layout.container-breakpoints) are recorded, not made.
- **[12 — Document model, persistence, and the LOCK/UNLOCK contract]** O34 — file-mode enforcement of doc ownership is only a speed bump because the editor and the Claude process share a uid; a separate uid or container is out of scope for a local ACOS skill. Stated inline as having no known in-scope mitigation; the reconciliation layer is what actually carries the guarantee.
- **[12 — Document model, persistence, and the LOCK/UNLOCK contract]** O31 — the pairing of breakpoint boundaries (991/479, from §11.3's cited Webflow row) with preview widths (768/390, from §10.1's switcher) is a reconciliation this PRD makes, not a cited figure. Marked [I] and left as an open question because the alternative (boundaries = preview widths) is equally defensible and must be changed in four sections at once.
- **[12 — Document model, persistence, and the LOCK/UNLOCK contract]** O35 — whether wb migrate should do semantic cross-direction variant matching (slot-signature) instead of canonical fallback. Explicitly recorded as having no risk-removing mitigation: both options need mandatory review, and the choice is the user's.
- **[12 — Document model, persistence, and the LOCK/UNLOCK contract]** Ignored as instructed: the recorded gap claiming 'sections 1–12.6 absent from reviewed text' — known truncation artifact, not acted on.
- **[10]** Per-page vs site-wide undo/history/lock scoping for pages/<id>.doc.json — left as an explicit open question; requires a §12 (not §10) update to ratify
- **[10]** layout.json rename in §4, §11 (partially independently revised already), §12.6, §12.10 — out of this section's edit authority, flagged as a required follow-up rather than silently fixed
- **[10]** Four rows kept at their pre-reconciliation v1 priority purely by inference because §18 never names them (Breakpoint switcher, Type-aware resize, the section-level/general split on tree drag-reorder, Per-breakpoint visibility) — marked [I] inline and listed in §10.10 as first candidates to re-check against the next §18 revision
- **[10]** The fallback consequence of a user declining the custom-component/chart sign-off (i.e., pulling charts out of v1 entirely in §18) is named but not resolved, since it would require editing §18
- **[16]** The gap claiming 'sections 1-12.6 absent from reviewed text' — ignored per instruction; known truncation artifact, not a defect.
- **[16]** O31 (can a running skill session call Task?) left OPEN rather than answered: it cannot be answered from documents, only by running the probe in a live harness. Mitigated rather than closed — v1 is redesigned so nothing depends on the answer.
- **[16]** O5 / Gate 16-A left OPEN as to its RESULT: the re-proof test has not been run here (it requires executing a server across real Claude turn boundaries, which a document revision cannot do). What was closed is the missing fallback plan and the missing conditionality of v1 scope, which is what the critic asked for.
- **[16]** O4's actual answer left OPEN by design: the critic offered 'promote to settled OR mark as candidate'; the honest option was to mark it candidate, since the PRD has no spike data. Choosing a topology here would have fabricated a decision.
- **[16]** F2 rung honestly flagged as possibly unreachable in pure TS (setsid does not exist on this Mac and the session-leader syscall may not be exposed) with 'no known mitigation if the required syscall is not exposed' left visible rather than papered over.
- **[8. The component inventory]** Lock-time capture TOTAL — cannot be computed honestly. The per-variant figure (≤30) is arithmetic; the page total needs the number of distinct selected variants on a real v1 page, which nobody has. Recorded as new open question O31 with 'no known mitigation beyond measuring it on the first real build — do not put a total in §13's budget until it is measured'.
- **[8. The component inventory]** The §8.5.2 perceptual thresholds — `T_ssim = 0.98` is marked [I] as an engineering guess with NO source; `T_colour = 2.0` is anchored only to the conventional ΔE00 just-noticeable-difference/close-match convention, stated as convention rather than as a validated threshold. The rule therefore ships ADVISORY (flags, does not delete) until calibrated against a real generated set — new open question O32. Consequence stated plainly in §8.8-X4/R47: §17-R33's jam-study failure is only partly mitigated until O32 closes.
- **[8. The component inventory]** Third-party iframe accessibility (Booking / scheduling block) — recorded as an accepted, disclosed limitation in the evidence bundle rather than claiming conformance we cannot deliver.
- **[8. The component inventory]** No new WCAG gate added for 1.4.12 at page level — §13.4 gate 10 already covers it; this section only adds the component-level coverage state, and says so explicitly rather than duplicating a gate.
- **[18]** O10 itself is not ANSWERED — I costed the three branches and recommended Branch A+ as inference, but the choice requires the user. Closing it by picking would have fabricated a decision. The contradiction the critic flagged (committed + open simultaneously) IS closed: the v1 lines are now explicitly PROVISIONAL and gated on new O31, and the v1 exit criteria require O31 to be resolved before build start.
- **[18]** R47 (D2 unvalidated at first ship) is stated with a mitigation marked explicitly PARTIAL. There is no way to validate a constraint drag model without building a drag model, so the v1 exit report detects the problem late by construction. Left visible rather than papered over.
- **[18]** Final UI wording for the freeze affordance ('Freeze' vs 'Pin') left open as O33 — the constraint (must not be 'Lock') is settled; the word is a user preference and inventing one would over-specify.
- **[18]** The Branch B effort figure (~16–24d, ~25–35d against the revised L3a) is inference derived from O10's own 'roughly doubles editor scope' phrase and is labelled [I] rather than presented as measured. No new source or benchmark was invented.
- **[18]** Ignored per instructions: the recorded gap claiming sections 1–12.6 are absent from the reviewed text (known truncation artifact) — it does not appear under key 18 in any case.
