# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S55-lane-a-code-drawn-artwork.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S55-lane-a-code-drawn-artwork"
story_id: "ST-18"          # Art and motion in one container contract
epic_id: "E11"            # Artwork lanes, asset library and containers
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Lane A - code-drawn, token-parameterised artwork that re-skins with the direction"
objective: |
  Deliver the artwork lane that is actually available from the generation channel, and make it move with the hue anchors.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A20"
  - "[ ] A21"
  - "[ ] A23"
  - "[ ] Artifacts: the Lane A artwork set, the ratio computation, the re-skin transcript, the manifest entries, the cursor rules."
  - "[ ] Validation: the ratio is recomputed and ≥0.60; a hue-anchor change produces a visibly different render with no regeneration; every `cursor: url()` has a fallback; no cursor exceeds 128×128."
  - "[ ] Demo-able increment: flip the direction's hue anchors in the editor and watch the artwork re-skin live."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A20, A21, A23]`, `verification_method: recompute` (A21: `screenshot-diff`, A23: `grep-assert`)."

verification_method: "recompute"

dependencies:
  slices:
    - slice_id: "S54-art-motion-container-contract"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/artwork-lane-a.ts`, `scripts/lib/cursors.ts`, `02-system/<directionId>/artwork/**`, `04-site/assets/manifest.json` (through the register-asset op only)."
  - "TypeScript on Bun."

files_prohibited:
  - "**No `.py` file anywhere in the skill tree.**"

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-150", "FR-155"]
  cqs: ["CQ1"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S55-lane-a-code-drawn-artwork.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
