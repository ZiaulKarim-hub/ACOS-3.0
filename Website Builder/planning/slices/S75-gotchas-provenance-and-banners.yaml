# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S75-gotchas-provenance-and-banners.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S75-gotchas-provenance-and-banners"
story_id: "ST-25"          # A project that survives month six and a context reset
epic_id: "E18"            # Durability and diagnostics
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Gotchas reference, provenance and do-not-hand-edit banners"
objective: |
  Write down the harness facts that cost time, and make generated files say what to edit instead.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S75-1"
  - "[ ] SL-S75-2"
  - "[ ] SL-S75-3"
  - "[ ] Artifacts: `references/gotchas.md`, `banner.ts`, `provenance.ts`, selftest assertions for the no-re-derivation and durable-path rules."
  - "[ ] Validation: every named harness fact present by grep; every generated file carries a banner naming an alternative file; the re-derivation grep returns zero; no durable artifact path resolves inside the session-cleanup scope."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S75-1, SL-S75-2, SL-S75-3]`, `verification_method: grep-assert` (SL-S75-3: `exit-code`)."

verification_method: "grep-assert"

dependencies:
  slices:
    - slice_id: "S09-install-config-session-selftest"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`references/gotchas.md` (new), `scripts/lib/banner.ts`, `scripts/lib/provenance.ts`, `04-site/provenance.json` (write via the editor process), `scripts/selftest.ts` (extend)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "S"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-243", "FR-244", "FR-245"]
  cqs: ["CQ6"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S75-gotchas-provenance-and-banners.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
