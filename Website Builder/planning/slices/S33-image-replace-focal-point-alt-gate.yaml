# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S33-image-replace-focal-point-alt-gate.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S33-image-replace-focal-point-alt-gate"
story_id: "ST-10"          # Edits that are typed, atomic and undoable as one thought
epic_id: "E8"            # Editor shell and core operations
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Image replace, focal point, blocking alt gate and auto-recompression"
objective: |
  Make image handling preserve composition and make missing alt text impossible to place, not merely impossible to ship.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A35"
  - "[ ] A36"
  - "[ ] SL-S33-1"
  - "[ ] SL-S33-2"
  - "[ ] Artifacts: the image panel, the asset ops, the recompressor, the manifest entries with derivative records."
  - "[ ] Validation: a replace leaves container geometry byte-identical in the document; a placement without alt is refused; an oversize drop recompresses, confirms and undoes cleanly; both manifest gates run and a hallucinated path is caught by the second."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A35, A36, SL-S33-1, SL-S33-2]`, `verification_method: manual-observation` (A36 and SL-S33-2: `exit-code`)."

verification_method: "manual-observation"

dependencies:
  slices:
    - slice_id: "S31-typed-ops-autosave-history-undo"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`app/editor/image-panel.ts`, `scripts/lib/ops/asset-ops.ts`, `scripts/lib/recompress.ts`, `04-site/assets/manifest.json` (through the op path only), `04-site/assets/**` (derived files)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-084"]
  cqs: ["CQ11"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S33-image-replace-focal-point-alt-gate.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
