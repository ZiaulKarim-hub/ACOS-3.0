# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S58-section-notes-scoped-regeneration.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S58-section-notes-scoped-regeneration"
story_id: "ST-19"          # Regeneration with a middle gear and no silent substitution
epic_id: "E12"            # Step 5 regeneration
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Per-section notes driving scoped inline regeneration as one undo step"
objective: |
  Ship the middle gear between swapping one variant and regenerating everything, executed inline with no second hand-carry.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A32"
  - "[ ] SL-S58-1"
  - "[ ] SL-S58-2"
  - "[ ] SL-S58-3"
  - "[ ] Artifacts: the note model, the scoped regeneration path, the regeneration log, the LOCK strip entry."
  - "[ ] Validation: a scoped run changes one section only, verified by document hashes; one Cmd+Z restores the pre-run state exactly; a grep of published output finds zero section notes; the log has one entry per run."
  - "[ ] Demo-able increment: write a note on a section in the editor, regenerate it inline, then undo it in one step."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A32, SL-S58-1, SL-S58-2, SL-S58-3]`, `verification_method: exit-code` (SL-S58-1: `hash-compare`, SL-S58-3: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S57-more-variants-and-more-like-this"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S20-local-regeneration-mode"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S31-typed-ops-autosave-history-undo"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/regenerate-section.ts`, `scripts/lib/section-notes.ts`, the `regenerate-section` op handler, `04-site/pages/<id>.doc.json` + `04-site/content.json` (through typed ops only), the regeneration log, the LOCK strip list as a call site."
  - "TypeScript on Bun."

files_prohibited:
  - "**No `.py` file anywhere in the skill tree.**"

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-180"]
  cqs: ["CQ15"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S58-section-notes-scoped-regeneration.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
