# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S77-selftest-and-evidence-bundles.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S77-selftest-and-evidence-bundles"
story_id: "ST-26"          # The ship bar, proven and instrumented
epic_id: "E19"            # Acceptance, demos and learning capture
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Selftest at full assertion pass and per-slice evidence bundles"
objective: |
  Make the release claim testable and make every slice's evidence reproducible from disk.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A85"
  - "[ ] A84"
  - "[ ] A86"
  - "[ ] A87"
  - "[ ] A88"
  - "[ ] A89"
  - "[ ] SL-S77-1"
  - "[ ] Artifacts: the consolidated `selftest.ts`, packaging assertions, `evidence-audit.ts`, the audit report, the recorded `passed/total`."
  - "[ ] Validation: `bun selftest.ts` exits 0 with `passed == total`; a seeded `.py` file fails it; a seeded missing evidence section is named by slice and section; the symlink assertion fails against a copied install."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A85, A84, A86, A87, A88, A89, SL-S77-1]`, `verification_method: exit-code` (A84/A86/A87/A88/A89: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S09-install-config-session-selftest"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S76-acceptance-criteria-sweep"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/selftest.ts`, `scripts/lib/packaging-assertions.ts`, `scripts/evidence-audit.ts`, `.acos/evidence/[DATE]/[SLICE-ID]/` (read + index write)."
  - "**This slice may not edit another slice's evidence bundle** — it reports a missing section, it does not fill one in."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-251"]
  cqs: []
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S77-selftest-and-evidence-bundles.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
