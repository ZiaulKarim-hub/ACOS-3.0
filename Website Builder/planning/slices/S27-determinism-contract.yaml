# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S27-determinism-contract.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S27-determinism-contract"
story_id: "ST-08"          # One document, one renderer, one deterministic output
epic_id: "E7"            # Document model and pure renderer
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Determinism contract: six hazards designed out"
objective: |
  Make generation a pure function of documents, system lock and generator version, so verify can be trusted.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A53"
  - "[ ] SL-S27-1"
  - "[ ] SL-S27-2"
  - "[ ] SL-S27-3"
  - "[ ] Artifacts: the determinism module, the marker emitter, the asset hash comparator, the grep gate, both empty-diff transcripts."
  - "[ ] Validation: two consecutive generates diff empty; generate-then-ten-ops-then-generate diffs empty; the marker header is present in 100% of generated files; a timestamp grep returns zero; the ambient-input grep returns zero."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A53, SL-S27-1, SL-S27-2, SL-S27-3]`, `verification_method: hash-compare` (SL-S27-1 and SL-S27-3: `grep-assert`)."

verification_method: "hash-compare"

dependencies:
  slices:
    - slice_id: "S25-pure-renderer-and-resolution-policy"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/determinism.ts`, `scripts/lib/generate.ts` (marker emission only), `scripts/lib/asset-hash.ts`, `scripts/lib/verify-generate.ts`, the generate-time grep gate."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-075"]
  cqs: ["CQ9"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S27-determinism-contract.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
