# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S15-font-catalog-and-snapshot.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S15-font-catalog-and-snapshot"
story_id: "ST-05"          # A prompt that cannot silently lose or invent anything
epic_id: "E4"            # Step 2 prompt generator
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Font catalog with pre-subsetted cuts, and the per-session snapshot"
objective: |
  Build a skill-owned font catalog whose base64 cuts are computed locally ahead of time, and pin a hash-stamped copy per session.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S15-1"
  - "[ ] SL-S15-2"
  - "[ ] Artifacts: the catalog, the subsetting tool, the session snapshot, the licence assertion."
  - "[ ] Validation: every entry has a licence class and a file hash; the snapshot hash is compared at emission; a deliberately licence-less entry fails the build."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S15-1, SL-S15-2]`, `verification_method: exit-code` (SL-S15-2: `hash-compare`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S02-o1-font-policy-probe"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/font-catalog.ts`, `scripts/tools/subset-font.ts`, the library catalog path, `01-prompt/font-catalog.snapshot.json`."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-040"]
  cqs: ["CQ10", "CQ5"]
  evidence_refs: ["EL-067"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S15-font-catalog-and-snapshot.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
