# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S73-resume-from-disk-and-reattach.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S73-resume-from-disk-and-reattach"
story_id: "ST-25"          # A project that survives month six and a context reset
epic_id: "E18"            # Durability and diagnostics
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Resume from disk and re-attach rather than relaunch"
objective: |
  Reconstruct the pipeline phase from on-disk state alone and survive a context reset without restarting a live server.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A13"
  - "[ ] SL-S73-1"
  - "[ ] SL-S73-2"
  - "[ ] Artifacts: `resume.ts`, `frontier.ts`, `reattach.ts`, the resume text, selftest assertions for the no-memory and no-relaunch properties."
  - "[ ] Validation: a session tree copied to a fresh location resumes to the correct phase with an empty conversation; a killed server is detected as stale rather than silently re-attached; the post-boundary curl returns 200 in its own tool call."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A13, SL-S73-1, SL-S73-2]`, `verification_method: exit-code` (SL-S73-1/SL-S73-2: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S09-install-config-session-selftest"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S37-eight-control-security-posture"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/resume.ts`, `scripts/lib/frontier.ts`, `scripts/lib/reattach.ts`, the resume text in `SKILL.md`, `scripts/selftest.ts` (extend)."
  - "Reads `state.json`, `session.json`, `events.jsonl` and the directory tree."
  - "**Writes nothing in the doc-owned zone** — Claude reaches documents only through `wb op`."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "S"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-240", "FR-241"]
  cqs: ["CQ6"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S73-resume-from-disk-and-reattach.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
