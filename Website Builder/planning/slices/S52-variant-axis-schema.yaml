# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S52-variant-axis-schema.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S52-variant-axis-schema"
story_id: "ST-17"          # Swaps that never destroy copy and never ship a placeholder
epic_id: "E10"            # Component bar and deterministic variants
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Hand-authored variant-axis schema"
objective: |
  Define structural distinctness machine-checkably, per component, so the variant budget stays finite.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S52-1"
  - "[ ] SL-S52-2"
  - "[ ] Artifacts: the vector data set, `variant-axes.ts`, `distinctness.ts`, the coverage selftest, the recorded effort line."
  - "[ ] Validation: coverage selftest green at 88 rows; a fixture pair differing only on a computed axis is reported as the **same** variant; the skip-link row is present with exactly one variant."
  - "[ ] Demo-able increment: `bun selftest.ts --axes` prints the coverage table and the excluded-axis list."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S52-1, SL-S52-2]`, `verification_method: exit-code`."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S21-token-compiler-dtcg-and-forge"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/variant-axes.ts`, `scripts/data/variant-axes/*.json` (the hand-authored vectors), `scripts/lib/distinctness.ts`, the coverage selftest."
  - "TypeScript on Bun."

files_prohibited:
  - "**No `.py` file anywhere in the skill tree.**"

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-130", "FR-131", "FR-132"]
  cqs: ["CQ14"]
  evidence_refs: ["EL-037"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S52-variant-axis-schema.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
