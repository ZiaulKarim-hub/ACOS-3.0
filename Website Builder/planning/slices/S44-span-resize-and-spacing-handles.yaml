# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S44-span-resize-and-spacing-handles.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S44-span-resize-and-spacing-handles"
story_id: "ST-14"          # Direct manipulation that cannot destroy the token system
epic_id: "E9"            # The canvas (DECISION-1 option B)
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Span resize, padding/gap handles on the scale, and smart guides"
objective: |
  Make direct manipulation teach the token system rather than erode it, with token-named readouts and fluid-consequence feedback.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A28"
  - "[ ] SL-S44-1"
  - "[ ] SL-S44-2"
  - "[ ] SL-S44-3"
  - "[ ] Artifacts: resize module, spacing-handle module, guide module, the off-scale rejection test, the equal-spacing detector."
  - "[ ] Validation: a resize commits whole cells and the readout matches the recomputed fraction; an off-scale `set-space` POST returns 400; a `grep` of the handle path finds no raw-pixel readout; the equal-spacing indicator fires at three matching siblings and not at two."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A28, SL-S44-1, SL-S44-2, SL-S44-3]`, `verification_method: grep-assert` (SL-S44-2: `exit-code`; SL-S44-1/3: `manual-observation`)."

verification_method: "grep-assert"

dependencies:
  slices:
    - slice_id: "S43-drag-to-place-and-drop-algorithm"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/canvas/resize.ts`, `scripts/lib/canvas/spacing-handles.ts`, `scripts/lib/canvas/guides.ts`, the snap engine (S42) and the op client (S31), read-only; the spacing-scale tokens from the token compiler (S21), read-only."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-108", "FR-109", "FR-110"]
  cqs: ["CQ4"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S44-span-resize-and-spacing-handles.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
