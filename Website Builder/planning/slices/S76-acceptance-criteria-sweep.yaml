# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S76-acceptance-criteria-sweep.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S76-acceptance-criteria-sweep"
story_id: "ST-26"          # The ship bar, proven and instrumented
epic_id: "E19"            # Acceptance, demos and learning capture
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Acceptance-criteria sweep with the collision handled"
objective: |
  Map all 90 criteria to a gate, a script or an observable behaviour, and cite the unstable ones section-qualified.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S76-1"
  - "[ ] SL-S76-2"
  - "[ ] SL-S76-3"
  - "[ ] Artifacts: `acceptance-sweep.json`, `acceptance-sweep.ts`, `acceptance-sweep.md`."
  - "[ ] Validation: the table has exactly 90 rows; the checker exits non-zero on a seeded unclaimed criterion; a seeded bare `A93` in an artifact is reported; the amendments-owed list has three entries."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S76-1, SL-S76-2, SL-S76-3]`, `verification_method: exit-code` (SL-S76-2: `grep-assert`; SL-S76-3: `manual-observation`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S68-lock-time-checklist-32-checks"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S72-demo-4-locked-published-evidence"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`acceptance-sweep.json` (new, at the feature root), `scripts/acceptance-sweep.ts` (the checker), `acceptance-sweep.md` (the human-readable render)."
  - "**Read-only against `stories.json`** — this slice checks it and never edits it."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-250"]
  cqs: ["CQ1"]
  evidence_refs: ["EL-069", "EL-070"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S76-acceptance-criteria-sweep.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
