# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S69-lock-snapshots-manifest-and-unlock.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S69-lock-snapshots-manifest-and-unlock"
story_id: "ST-23"          # A lock that is reversible and a checklist that is complete
epic_id: "E14"            # Step 7 LOCK
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Snapshots, lock manifest, git tag, non-mutating LOCK and the unlock path"
objective: |
  Make LOCK reversible and hand-edits inside the exported tree detectable rather than silently overwritten.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A54"
  - "[ ] A55"
  - "[ ] A56"
  - "[ ] A58"
  - "[ ] A59"
  - "[ ] SL-S69-1"
  - "[ ] SL-S69-2"
  - "[ ] Artifacts: snapshot writer, `lock-manifest.json`, the tag, the unlock diff prompt, the restore path, `extract-override --from-dist`."
  - "[ ] Validation: document mtimes unchanged across a LOCK; the snapshot contains all nine named members and **no `dist/`**; a hand-edited file in the exported tree raises the blocking prompt at unlock **and** at the next LOCK; a restore against a mismatched library stops with the migrate command; zero `rm -rf` in the export path."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A54, A55, A56, A58, A59, SL-S69-1, SL-S69-2]`, `verification_method: exit-code` (A54: `hash-compare`; A55/A58: `manual-observation`; A59: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S67-eight-purity-gates"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/snapshot.ts`, `scripts/lib/lock-manifest.ts`, `scripts/lib/unlock.ts`, `scripts/extract-override.ts`, `.wb/locks/<iso>/` (write), `07-lock/lock-manifest.json` (write)."
  - "**`dist/` is deliberately excluded from the snapshot — it is reproducible**, and including it would make `.wb/locks` grow without bound. `.wb/locks/**` is **committed**; `.wb/tmp/**` and `.wb/conflicts/**` are git-ignored."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-205", "FR-207", "FR-208", "FR-209"]
  cqs: ["CQ9"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S69-lock-snapshots-manifest-and-unlock.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
