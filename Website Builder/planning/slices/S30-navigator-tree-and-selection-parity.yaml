# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S30-navigator-tree-and-selection-parity.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S30-navigator-tree-and-selection-parity"
story_id: "ST-09"          # A surface where everything is reachable
epic_id: "E8"            # Editor shell and core operations
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Navigator/layers tree and three-way selection parity"
objective: |
  Guarantee everything is reachable, including what canvas clicking provably cannot reach.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A25"
  - "[ ] SL-S30-1"
  - "[ ] Artifacts: the tree, the selection module, the breadcrumb binding, the four-case fixture, the two-column reachability record."
  - "[ ] Validation: every fixture case selectable from the tree; selection parity demonstrated in both directions for each of the three routes; hide/show leaves the document and the rendered output byte-identical."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A25, SL-S30-1]`, `verification_method: manual-observation` (SL-S30-1 recorded case by case, with the document diff for hide/show by `hash-compare`)."

verification_method: "manual-observation"

dependencies:
  slices:
    - slice_id: "S29-editor-shell-and-overlay"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`app/editor/navigator/**`, `app/editor/selection.ts`, `app/editor/breadcrumb.ts`, `.wb/session-ui.json` (`selection`, `openPanels` — ephemeral state only)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-081"]
  cqs: ["CQ3"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S30-navigator-tree-and-selection-parity.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
