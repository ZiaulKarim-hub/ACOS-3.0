# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S29-editor-shell-and-overlay.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S29-editor-shell-and-overlay"
story_id: "ST-09"          # A surface where everything is reachable
epic_id: "E8"            # Editor shell and core operations
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Three-pane editor shell with the out-of-iframe overlay and same-origin preview"
objective: |
  Stand up the editing surface with zero DOM injection for hit-testing and a preview that can be captured without chrome.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S29-1"
  - "[ ] SL-S29-2"
  - "[ ] Artifacts: the shell, the overlay module, the preview frame module, the height-assertion transcript, a capture of the preview."
  - "[ ] Validation: a grep proves no injected hit-test node; the overlay is a sibling outside the layout root; the measured height equals the pinned height on a `vh` fixture; a preview capture contains zero editor chrome; a preview restart loses nothing."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S29-1, SL-S29-2]`, `verification_method: grep-assert` (SL-S29-2: `recompute`)."

verification_method: "grep-assert"

dependencies:
  slices:
    - slice_id: "S25-pure-renderer-and-resolution-policy"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S08-server-ts-port"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`app/editor/**`, `app/editor/overlay.ts`, `app/editor/preview-frame.ts`, `scripts/lib/routes.ts` (static + SSE wiring only)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "L"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-080", "FR-082", "FR-120"]
  cqs: ["CQ3", "CQ17"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S29-editor-shell-and-overlay.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
