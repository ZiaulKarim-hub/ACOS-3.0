# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S51-slot-contracts-and-swap-safety.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S51-slot-contracts-and-swap-safety"
story_id: "ST-17"          # Swaps that never destroy copy and never ship a placeholder
epic_id: "E10"            # Component bar and deterministic variants
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Typed slot contracts, superset-only swaps, content orphanage and lock-blocking placeholders"
objective: |
  Make a variant swap incapable of destroying hand-written copy and incapable of shipping an unfilled placeholder.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A29"
  - "[ ] A30"
  - "[ ] A38"
  - "[ ] SL-S51-1"
  - "[ ] SL-S51-2"
  - "[ ] Artifacts: `slots.ts`, `swap.ts`, the op-handler rejection path, the parked panel, the placeholder flag, `coherence-ledger.json`."
  - "[ ] Validation: a swap dropping a slot parks the copy and deletes nothing; the reverse swap restores it; a placeholder-bearing doc is refused by the LOCK precondition check; the tab-order sequence is byte-identical across the swap."
  - "[ ] Demo-able increment: swap a live section in the editor, watch the copy park and return."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A29, A30, A38, SL-S51-1, SL-S51-2]`, `verification_method: exit-code` (A38: `manual-observation`, SL-S51-1: `manual-observation`, SL-S51-2: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S31-typed-ops-autosave-history-undo"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S25-pure-renderer-and-resolution-policy"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/slots.ts`, `scripts/lib/swap.ts`, the `swap-variant` op handler, `04-site/pages/<id>.doc.json` + `04-site/content.json` (through typed ops only), `04-site/coherence-ledger.json` (create)."
  - "TypeScript on Bun."

files_prohibited:
  - "**No `.py` file anywhere in the skill tree.**"

flow: "slice-execution-flow"

effort_estimate: "L"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-138", "FR-139", "FR-140", "FR-141", "FR-142"]
  cqs: ["CQ14"]
  evidence_refs: ["EL-037"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S51-slot-contracts-and-swap-safety.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
