# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S26-breakpoint-vocabulary-and-cascade.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S26-breakpoint-vocabulary-and-cascade"
story_id: "ST-08"          # One document, one renderer, one deterministic output
epic_id: "E7"            # Document model and pure renderer
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Breakpoint vocabulary and the desktop-down cascade compiler"
objective: |
  Make one breakpoint vocabulary normative across switcher, cascade, free-position rules, save format and gates.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A41"
  - "[ ] \"§12.17-A91\""
  - "[ ] SL-S26-1"
  - "[ ] Artifacts: the vocabulary module, the cascade emitter, the compiled stylesheet for the seeded page, the two annotated call sites."
  - "[ ] Validation: the emitted rule order is `base, md, sm`; a base-only node yields `grid-column: 1 / -1` under `max-width: 479px`; `grep -c '!important'` over emitted CSS is 0; the 390 and 479 assertions both pass."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A41, \"§12.17-A91\", SL-S26-1]`, `verification_method: exit-code` (SL-S26-1: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S24-doc-schema-and-canonical-serialisation"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/breakpoints.ts`, `scripts/lib/render/emit-css.ts` (cascade emission only), `04-site/site.json` (`breakpoints`, `preview`, `freePositionPolicy` fields)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-072", "FR-073"]
  cqs: ["CQ2"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S26-breakpoint-vocabulary-and-cascade.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
