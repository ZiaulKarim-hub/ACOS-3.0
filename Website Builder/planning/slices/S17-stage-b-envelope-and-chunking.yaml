# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S17-stage-b-envelope-and-chunking.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S17-stage-b-envelope-and-chunking"
story_id: "ST-05"          # A prompt that cannot silently lose or invent anything
epic_id: "E4"            # Step 2 prompt generator
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Stage-B prompt, envelope manifest, terminator and runtime chunk computation"
objective: |
  Make truncation detectable by construction and size chunks from measured artifacts rather than any published ceiling.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S17-1"
  - "[ ] SL-S17-2"
  - "[ ] SL-S17-3"
  - "[ ] Artifacts: Stage-B assembler, envelope generator, chunker, a generated envelope sample."
  - "[ ] Validation: the shared-section hash equality; the no-constant grep; a chunking run recorded against measured sizes."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S17-1, SL-S17-2, SL-S17-3]`, `verification_method: exit-code` (SL-S17-2: `grep-assert`, SL-S17-3: `manual-observation`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S16-token-manifest-and-stage-a-prompt"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S06-channel-fidelity-and-ceiling-battery"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/prompt-stage-b.ts`, `scripts/lib/envelope.ts`, `scripts/lib/chunker.ts`, `01-prompt/**`, `02-system/manifest.json` (write)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-045", "FR-047", "FR-048"]
  cqs: ["CQ18", "CQ13"]
  evidence_refs: ["EL-066"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S17-stage-b-envelope-and-chunking.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
