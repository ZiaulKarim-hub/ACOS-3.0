# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S08-server-ts-port.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S08-server-ts-port"
story_id: "ST-02"          # A TypeScript spine that installs once and resumes from disk
epic_id: "E1"            # Skill scaffold and the TypeScript spine
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Port the in-estate Python server to server.ts (the TypeScript spine)"
objective: |
  Establish the TypeScript spine as the first code written, serving the route contract on the fixed loopback port with state.json at boot.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A84"
  - "[ ] A80"
  - "[ ] SL-S08-1"
  - "[ ] Artifacts: `server.ts`, `routes.ts`, `package.json`, the boot `state.json` sample, the survival transcript."
  - "[ ] Validation: the eight-field `state.json`; the zero-Python assertion; a post-boundary 200."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A84, A80, SL-S08-1]`, `verification_method: exit-code` (A80: `probe`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S01-gate-16a-and-launcher-rung"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/server.ts`, `scripts/lib/routes.ts`, `scripts/package.json`, the S01 launcher."

files_prohibited:
  - "**No `.py` file may be created anywhere in the skill tree.**"

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-012", "FR-227"]
  cqs: ["CQ6"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S08-server-ts-port.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
