# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S22-flat-variable-layer-and-extract-override.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S22-flat-variable-layer-and-extract-override"
story_id: "ST-07"          # Coherence by computation, enforced by lints
epic_id: "E6"            # Token compiler and coherence lints
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Flat variable layer per direction change, machine-owned stylesheet, extract-override"
objective: |
  Keep drag-time cost off the token graph and give hand-tuning one sanctioned door.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S22-1"
  - "[ ] SL-S22-2"
  - "[ ] Artifacts: flattener, override tool, banner, the drag counter assertion."
  - "[ ] Validation: the counter is unchanged across ten drags; an override survives regeneration byte-identically."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S22-1, SL-S22-2]`, `verification_method: recompute` (SL-S22-2: `exit-code`)."

verification_method: "recompute"

dependencies:
  slices:
    - slice_id: "S21-token-compiler-dtcg-and-forge"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/flatten-tokens.ts`, `scripts/tools/extract-override.ts`, `02-system/<directionId>/tokens.css` (machine-owned), `src/overrides/**` (created by the tool only)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "S"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-062", "FR-067"]
  cqs: ["CQ5"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S22-flat-variable-layer-and-extract-override.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
