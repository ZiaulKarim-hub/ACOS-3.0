# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S56-lane-b-asset-library-and-chips.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S56-lane-b-asset-library-and-chips"
story_id: "ST-18"          # Art and motion in one container contract
epic_id: "E11"            # Artwork lanes, asset library and containers
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Lane B ingestion, asset manifest, affinity filter chips, Lane C runbook and the honest warning"
objective: |
  Ingest a real asset library with licence classes and affinity tags, and tell the user the truth at interview time when there is none.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S56-1"
  - "[ ] SL-S56-2"
  - "[ ] SL-S56-3"
  - "[ ] SL-S56-4"
  - "[ ] Artifacts: the ingester, the manifest, the chipped asset pane, `docs/lane-c-raster-runbook.md`, the interview-time warning and its record."
  - "[ ] Validation: an asset with no licence class fails ingestion; every ingested asset carries hash + affinity + class; the chips filter the pane; a no-library project shows the warning before generation; no external-generation call exists."
  - "[ ] Demo-able increment: point the skill at a real asset folder, ingest it, and filter the pane by direction affinity."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S56-1, SL-S56-2, SL-S56-3, SL-S56-4]`, `verification_method: exit-code` (SL-S56-2 and SL-S56-4: `manual-observation`, SL-S56-3: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S54-art-motion-container-contract"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S10-warm-start-and-asset-library-detection"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/assets-ingest.ts`, `scripts/lib/asset-pane.ts`, `04-site/assets/manifest.json` (through `register-asset` / `record-derivative` ops only), `docs/lane-c-raster-runbook.md`, the interview warning hook from S10."
  - "TypeScript on Bun."

files_prohibited:
  - "**No `.py` file anywhere in the skill tree.**"

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-151", "FR-152", "FR-153", "FR-154"]
  cqs: ["CQ10"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S56-lane-b-asset-library-and-chips.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
