# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S40-demo-2-live-surface-across-boundaries.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S40-demo-2-live-surface-across-boundaries"
story_id: "ST-12"          # Two writers, no lost work, and a localhost that is not a doorway
epic_id: "E8"            # Editor shell and core operations
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "DEMO 2 - a live editable surface proven across at least two turn boundaries"
objective: |
  Prove the editor premise holds in this harness, with survival evidence rather than a same-turn response.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A80"
  - "[ ] SL-S40-1"
  - "[ ] SL-S40-2"
  - "[ ] Artifacts: the recording, per-step screenshots, the raw curl/`ps` transcript, the hash table, `demo-2.md` naming the rung."
  - "[ ] Validation: at least two post-boundary checks, each in its own tool call, each 200 with the original pid alive; four operations demonstrated; autosave verified against disk."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A80, SL-S40-1, SL-S40-2]`, `verification_method: probe` (SL-S40-2: `manual-observation`)."

verification_method: "probe"

dependencies:
  slices:
    - slice_id: "S32-inline-plaintext-editing"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S33-image-replace-focal-point-alt-gate"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S34-reorder-duplicate-paste-freeze"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S35-snapshots-variations-recovery-bin"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S36-page-ops-seo-preview-and-health-hud"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S39-ownership-guard-wb-op-and-inbox"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`evidence/demo-2/**` (recording, transcripts, hashes), `docs/demos/demo-2.md`."
  - "Read-only: everything the demo exercises."

files_prohibited:
  - "**No product file may be edited by this slice.**"

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "demo"
  moscow: "MUST"
  fr_ids: []
  cqs: ["CQ6", "CQ7"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S40-demo-2-live-surface-across-boundaries.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
