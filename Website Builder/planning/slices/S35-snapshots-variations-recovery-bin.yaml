# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S35-snapshots-variations-recovery-bin.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S35-snapshots-variations-recovery-bin"
story_id: "ST-11"          # A safety net that makes experimenting cheap
epic_id: "E8"            # Editor shell and core operations
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Named snapshots, save-as-variation and the recovery bin"
objective: |
  Make experimenting cheap and deletion non-destructive, with a recovery path independent of undo.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S35-1"
  - "[ ] SL-S35-2"
  - "[ ] SL-S35-3"
  - "[ ] Artifacts: the snapshot command, the fork path, the trash writer, `restore-node`, the recovery-bin panel."
  - "[ ] Validation: a subtree deleted, then ten unrelated edits, then restored in place with those ten edits intact; the source document hashes are unchanged after a variation fork; a grep finds no base64 blob written to browser storage; the bin is empty in a LOCK-bound tree."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S35-1, SL-S35-2, SL-S35-3]`, `verification_method: exit-code` (SL-S35-2: `grep-assert`; SL-S35-3: `hash-compare`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S31-typed-ops-autosave-history-undo"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/snapshot.ts`, `scripts/lib/trash.ts`, `scripts/lib/ops/delete-restore.ts`, `.wb/snapshots/**` (command only), `04-site/**` (fork target, through the writer)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-088"]
  cqs: ["CQ15"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S35-snapshots-variations-recovery-bin.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
