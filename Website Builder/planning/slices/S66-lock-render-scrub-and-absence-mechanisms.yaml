# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S66-lock-render-scrub-and-absence-mechanisms.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S66-lock-render-scrub-and-absence-mechanisms"
story_id: "ST-22"          # An export that proves it is clean
epic_id: "E14"            # Step 7 LOCK
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "LOCK as a re-render, with five layered editor-absence mechanisms"
objective: |
  Build the published tree by re-rendering with the editor flag false, never by copying and stripping.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A49"
  - "[ ] A50"
  - "[ ] SL-S66-1"
  - "[ ] SL-S66-2"
  - "[ ] Artifacts: `lock.ts` (build+scrub+assert stages), publish config, scrub module, the scrub output file, one re-rendered tree."
  - "[ ] Validation: `grep -r 'data-wb-'` over the published tree returns zero; the dev-runtime patterns return zero; a deliberately reintroduced `WB_DESIGN` undefined fails the selftest; the strip set is absent from published output and present in the project."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A49, A50, SL-S66-1, SL-S66-2]`, `verification_method: grep-assert`."

verification_method: "grep-assert"

dependencies:
  slices:
    - slice_id: "S27-determinism-contract"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S63-gates-ts-verdicts-and-tiers"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lock.ts`, `scripts/lib/publish-config.ts`, `scripts/lib/scrub.ts`, `scripts/lib/strip-editor-state.ts`, the publish build config, `07-lock/dist/` (write)."

files_prohibited:
  - "**No `.py` file anywhere.** No `rm -rf` in the export path — write-to-new-dir-then-swap only (the permission layer scores destructive commands +5)."

flow: "slice-execution-flow"

effort_estimate: "L"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-200", "FR-201", "FR-206"]
  cqs: ["CQ9"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S66-lock-render-scrub-and-absence-mechanisms.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
