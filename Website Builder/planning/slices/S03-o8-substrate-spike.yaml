# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S03-o8-substrate-spike.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S03-o8-substrate-spike"
story_id: "ST-01"          # Prove the product is buildable as written before committing scope
epic_id: "E0"            # Phase-0 spikes and blocking gates
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Build-substrate spike: framework versus plain generated HTML"
objective: |
  Choose the preview and build substrate, or confirm that invariant I6 keeps the choice open, without hard-depending on any framework.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S03-1"
  - "[ ] SL-S03-2"
  - "[ ] Artifacts: both spike trees, a scored comparison table with measured values, ADR-03."
  - "[ ] Validation: every score is a measured number or a counted string, never an impression."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S03-1, SL-S03-2]`, `verification_method: manual-observation`."

verification_method: "manual-observation"

dependencies:
  slices:
    []

files_allowed:
  - "`spikes/substrate/{a-framework,b-plain}/**` (new, disposable)"
  - "`docs/adr/ADR-03-substrate.md` (new)"

files_prohibited:
  - "**No file under `scripts/` or `app/` may be modified.**"

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "diagnostic"
  moscow: "MUST"
  fr_ids: ["FR-005"]
  cqs: ["CQ9"]
  evidence_refs: ["EL-085"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S03-o8-substrate-spike.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
