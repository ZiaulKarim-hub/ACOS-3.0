# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S74-verify-and-doctor.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S74-verify-and-doctor"
story_id: "ST-25"          # A project that survives month six and a context reset
epic_id: "E18"            # Durability and diagnostics
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "verify and doctor"
objective: |
  Ship the two diagnostics that keep the drift guarantee real and name month-six rot before it is fatal.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A53"
  - "[ ] SL-S74-1"
  - "[ ] SL-S74-2"
  - "[ ] SL-S74-3"
  - "[ ] Artifacts: `verify.ts`, `doctor.ts`, the canonical serialiser, both report shapes, the ten-drag transcript, the false-positive log."
  - "[ ] Validation: empty diff fresh and after ten drags; a seeded out-of-band edit is caught by `doctor` as a hash mismatch; a seeded orphan override is listed by node id; a re-serialisation diff of one byte fails."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A53, SL-S74-1, SL-S74-2, SL-S74-3]`, `verification_method: hash-compare` (SL-S74-2: `exit-code`; SL-S74-3: `manual-observation`)."

verification_method: "hash-compare"

dependencies:
  slices:
    - slice_id: "S27-determinism-contract"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S69-lock-snapshots-manifest-and-unlock"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/verify.ts`, `scripts/doctor.ts`, `scripts/lib/canonical-serialise.ts`, `.wb/tmp/verify/` (scratch, git-ignored), the doctor report path."
  - "**Regeneration goes to a temp directory only** — `verify` never overwrites the live tree, and never `rm -rf`s anything."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-242"]
  cqs: ["CQ9"]
  evidence_refs: ["EL-063"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S74-verify-and-doctor.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
