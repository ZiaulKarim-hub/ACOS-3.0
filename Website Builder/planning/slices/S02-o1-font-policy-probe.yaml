# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S02-o1-font-policy-probe.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S02-o1-font-policy-probe"
story_id: "ST-01"          # Prove the product is buildable as written before committing scope
epic_id: "E0"            # Phase-0 spikes and blocking gates
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Generation-surface font-policy probe (60 seconds)"
objective: |
  Establish whether a generation-surface artifact renders a linked web font or silently falls back, before the Step-2 prompt spec is written.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S02-1"
  - "[ ] SL-S02-2"
  - "[ ] Artifacts: both fixtures, both screenshots, the raw network entry, ADR-02."
  - "[ ] Validation: the answer is derived from an observed network entry **and** a visual comparison — one alone is insufficient, because a fallback can look plausible."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S02-1, SL-S02-2]`, `verification_method: probe`."

verification_method: "probe"

dependencies:
  slices:
    []

files_allowed:
  - "`docs/adr/ADR-02-font-policy.md` (new)"
  - "`scripts/probes/font-policy/*.html` (new, throwaway fixtures)"
  - "Screenshots and a network log under the slice's evidence directory."

files_prohibited:
  - "**No product path may be touched.**"

flow: "slice-execution-flow"

effort_estimate: "S"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "diagnostic"
  moscow: "MUST"
  fr_ids: ["FR-004"]
  cqs: ["CQ5"]
  evidence_refs: ["EL-067"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S02-o1-font-policy-probe.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
