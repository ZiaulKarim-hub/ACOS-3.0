# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S43-drag-to-place-and-drop-algorithm.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S43-drag-to-place-and-drop-algorithm"
story_id: "ST-13"          # Drag that writes grid integers, not pixels
epic_id: "E9"            # The canvas (DECISION-1 option B)
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Drag-to-place writing grid integers, with the normative drop algorithm"
objective: |
  Implement the full normative drop algorithm including row derivation, span preservation, displace-down occupancy with ghost preview, re-parenting and the three legal rejections.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A40"
  - "[ ] SL-S43-1"
  - "[ ] SL-S43-2"
  - "[ ] Artifacts: drop algorithm, occupancy model, ghost preview, pre-commit chip, nine branch tests, the integers-only grep."
  - "[ ] Validation: all nine branches pass; a drag produces exactly one `history.jsonl` entry; `grep` proves no pixel coordinate is written to any document; a 6-of-12 block measures 50% at 768 and at 1440."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A40, SL-S43-1, SL-S43-2]`, `verification_method: exit-code` (A40: `recompute`; SL-S43-2: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S42-snap-engine"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S31-typed-ops-autosave-history-undo"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/canvas/drop.ts`, `scripts/lib/canvas/occupancy.ts`, `scripts/lib/canvas/ghost.ts`, `scripts/lib/canvas/precommit-chip.ts`, the snap engine (S42) and the op client (S31), both read-only."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-101", "FR-102", "FR-103", "FR-104", "FR-105", "FR-106"]
  cqs: ["CQ4", "CQ2"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S43-drag-to-place-and-drop-algorithm.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
