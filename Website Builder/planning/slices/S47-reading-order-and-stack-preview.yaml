# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S47-reading-order-and-stack-preview.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S47-reading-order-and-stack-preview"
story_id: "ST-15"          # Responsive authoring where nothing is invisible
epic_id: "E9"            # The canvas (DECISION-1 option B)
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Reading-order invariant, focusable-node hard block and numbered mobile stack preview"
objective: |
  Guarantee document order is reading order, and make any mobile reordering visible before it is committed.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S47-1"
  - "[ ] SL-S47-2"
  - "[ ] SL-S47-3"
  - "[ ] Artifacts: reading-order walker, order-override validator, stack-preview component, the no-tree-reorder test, focusable and non-focusable fixtures."
  - "[ ] Validation: `set-order-override` on a focusable node returns 403; on a non-focusable node it warns and the chip persists across reload; a stacking-changing commit renders the numbered preview first; the invariant test passes over every mutation path."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S47-1, SL-S47-2, SL-S47-3]`, `verification_method: exit-code` (SL-S47-3: `manual-observation`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S46-override-cascade-and-precommit-chip"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/canvas/reading-order.ts`, `scripts/lib/canvas/order-override.ts`, `scripts/lib/canvas/stack-preview.ts`, the `set-order-override` op validator (S31), the cascade resolver from S46 (read-only)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-114", "FR-115"]
  cqs: ["CQ11", "CQ2"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S47-reading-order-and-stack-preview.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
