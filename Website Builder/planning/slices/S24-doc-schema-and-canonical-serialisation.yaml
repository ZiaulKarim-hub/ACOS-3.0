# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S24-doc-schema-and-canonical-serialisation.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S24-doc-schema-and-canonical-serialisation"
story_id: "ST-08"          # One document, one renderer, one deterministic output
epic_id: "E7"            # Document model and pure renderer
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Document schema and canonical serialisation"
objective: |
  Define the scene graph as per-page documents plus a project record, serialised canonically so a re-serialise is always a zero diff.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] \"§12.17-A98\""
  - "[ ] \"§12.17-A92\""
  - "[ ] SL-S24-1"
  - "[ ] SL-S24-2"
  - "[ ] Artifacts: types, serialiser, validator, seeded `site.json` + `home.doc.json`, the `verify` subcommand, the hook, the zero-diff transcript."
  - "[ ] Validation: re-serialise produces a zero diff; a hand-collapsed array fails the hook; an upward-key fixture is rejected naming the rule; `grep -rn 'layout\\.json'` returns only the alias note; a `\"md\": null` fixture is rejected."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [\"§12.17-A98\", \"§12.17-A92\", SL-S24-1, SL-S24-2]`, `verification_method: hash-compare` (§12.17-A92: `exit-code`; SL-S24-1/2: `grep-assert`)."

verification_method: "hash-compare"

dependencies:
  slices:
    - slice_id: "S03-o8-substrate-spike"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S21-token-compiler-dtcg-and-forge"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/doc-schema.ts`, `scripts/lib/canonical-json.ts`, `scripts/lib/verify-serialisation.ts`, `04-site/site.json`, `04-site/pages/*.doc.json` (seed only), the pre-commit hook."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-070", "FR-074"]
  cqs: ["CQ3"]
  evidence_refs: ["EL-071"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S24-doc-schema-and-canonical-serialisation.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
