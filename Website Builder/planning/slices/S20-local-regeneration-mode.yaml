# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S20-local-regeneration-mode.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S20-local-regeneration-mode"
story_id: "ST-06"          # An ingest that refuses rather than half-applies
epic_id: "E5"            # Step 3 ingest and Local Regeneration Mode
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Local Regeneration Mode: the zero-paste path"
objective: |
  Make the web hop a UX preference rather than a technical dependency by producing an identical-format bundle with zero pastes.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A12"
  - "[ ] SL-S20-1"
  - "[ ] Artifacts: local-regeneration module, a produced bundle, its import report."
  - "[ ] Validation: the same validator binary accepts both a pasted fixture and a locally produced bundle; a diff of the two formats shows the same envelope structure."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A12, SL-S20-1]`, `verification_method: exit-code`."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S19-ast-validator-quarantine-and-reverification"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/local-regen.ts`, `scripts/steps/step3.ts` (extend), `02-system/**` via the existing importer only."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-056"]
  cqs: ["CQ18"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S20-local-regeneration-mode.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
