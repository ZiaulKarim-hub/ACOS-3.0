# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S46-override-cascade-and-precommit-chip.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S46-override-cascade-and-precommit-chip"
story_id: "ST-15"          # Responsive authoring where nothing is invisible
epic_id: "E9"            # The canvas (DECISION-1 option B)
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Per-breakpoint override cascade, pre-commit chip and overridden-here dots"
objective: |
  Make invisible overrides visible and make every breakpoint-scoped edit announce its blast radius before it commits.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A42"
  - "[ ] SL-S46-1"
  - "[ ] SL-S46-2"
  - "[ ] SL-S46-3"
  - "[ ] Artifacts: cascade resolver, extended chip, dots with reset, the breakpoint indicator, the counting module, the gate-report fields."
  - "[ ] Validation: an edit at `md` produces an `md` key only; the chip names the affected sizes before commit; a dot appears where a key exists and disappears after reset-to-inherited; changing a threshold in `site.json` changes the escalation point."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A42, SL-S46-1, SL-S46-2, SL-S46-3]`, `verification_method: manual-observation` (SL-S46-3: `exit-code`)."

verification_method: "manual-observation"

dependencies:
  slices:
    - slice_id: "S43-drag-to-place-and-drop-algorithm"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S26-breakpoint-vocabulary-and-cascade"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/canvas/override-cascade.ts`, `scripts/lib/canvas/precommit-chip.ts` (extending S43's chip), `scripts/lib/canvas/override-dots.ts`, `scripts/lib/doctor/override-counts.ts`, the breakpoint compiler from S26 (read-only), `site.json` doctor thresholds (read via `set-doctor-thresholds`)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "L"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-113"]
  cqs: ["CQ2"]
  evidence_refs: ["EL-063"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S46-override-cascade-and-precommit-chip.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
