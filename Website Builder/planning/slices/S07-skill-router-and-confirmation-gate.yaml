# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S07-skill-router-and-confirmation-gate.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S07-skill-router-and-confirmation-gate"
story_id: "ST-02"          # A TypeScript spine that installs once and resumes from disk
epic_id: "E1"            # Skill scaffold and the TypeScript spine
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Thin router SKILL.md with the mandatory Confirmation Gate"
objective: |
  Ship a router skill whose frontmatter is exactly as specified and whose Phase 0 refuses to write anything before an explicit human confirmation.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A90"
  - "[ ] A87"
  - "[ ] A86"
  - "[ ] Artifacts: `SKILL.md`, both role prompts, the assertion script."
  - "[ ] Validation: `grep -c 'Task' SKILL.md` on the `allowed-tools` line returns 0; a dry run halts at Phase 0 with no file written."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A90, A87, A86]`, `verification_method: grep-assert` (A90: `manual-observation`)."

verification_method: "grep-assert"

dependencies:
  slices:
    - slice_id: "S01-gate-16a-and-launcher-rung"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`SKILL.md` (new), `prompts/*.md` (new role rubrics, executed inline)"
  - "**Forbidden:** `.claude/agents/**` (human-approval-restricted infrastructure), any `Task(` call anywhere in the skill."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "S"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-010", "FR-011"]
  cqs: []
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S07-skill-router-and-confirmation-gate.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
