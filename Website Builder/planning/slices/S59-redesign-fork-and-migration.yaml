# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S59-redesign-fork-and-migration.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S59-redesign-fork-and-migration"
story_id: "ST-19"          # Regeneration with a middle gear and no silent substitution
epic_id: "E12"            # Step 5 regeneration
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Redesign as a fork, reviewed application, and the migration report"
objective: |
  Make a new direction a reviewed operation that never silently substitutes and never silently drops a node.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] \"§12.17-A99\""
  - "[ ] SL-S59-1"
  - "[ ] SL-S59-2"
  - "[ ] SL-S59-3"
  - "[ ] SL-S59-4"
  - "[ ] Artifacts: `migrate.ts`, the fork path, `migration-report.json`, the flag list UI, the LOCK refusal."
  - "[ ] Validation: original documents hash-identical after a fork; a node with no canonical target appears in `unmappable` and nowhere else; LOCK refuses with the node list until acknowledged; every change in the report names its deciding rule."
  - "[ ] Demo-able increment: import a second direction, review the plan, apply it, and see the flag list block LOCK until acknowledged."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [\"§12.17-A99\", SL-S59-1, SL-S59-2, SL-S59-3, SL-S59-4]`, `verification_method: exit-code` (SL-S59-3: `grep-assert`, SL-S59-4: `hash-compare`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S25-pure-renderer-and-resolution-policy"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S58-section-notes-scoped-regeneration"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/migrate.ts`, `scripts/lib/redesign-fork.ts`, the `acknowledge-migration` op handler, `04-site/migration-report.json` (write), `04-site/pages/<id>.doc.json` + `site.json` through typed ops only."
  - "TypeScript on Bun."

files_prohibited:
  - "**No `.py` file anywhere in the skill tree.**"

flow: "slice-execution-flow"

effort_estimate: "L"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-181", "FR-182", "FR-183", "FR-184"]
  cqs: ["CQ16", "CQ15"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S59-redesign-fork-and-migration.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
