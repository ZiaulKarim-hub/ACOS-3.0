# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S54-art-motion-container-contract.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S54-art-motion-container-contract"
story_id: "ST-18"          # Art and motion in one container contract
epic_id: "E11"            # Artwork lanes, asset library and containers
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "One container contract for art and motion, with the pause affordance field"
objective: |
  Implement the container contract and its seven rules so unpausable or unreduced motion is structurally unbuildable.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A22"
  - "[ ] SL-S54-1"
  - "[ ] SL-S54-2"
  - "[ ] SL-S54-3"
  - "[ ] SL-S54-4"
  - "[ ] Artifacts: the contract type, the seven validators, the cost-class table, the tabbed picker, the stated-limitation UI text."
  - "[ ] Validation: motion-capable container with no reduced-motion sibling fails; autoplay video that is not muted fails; a rule touching width/height/grid placement is rejected; a needs-pause container without `pauseAffordanceRef` cannot be constructed; the reduced-motion render **differs** from the full-motion render."
  - "[ ] Demo-able increment: place a motion container in the editor, see it inert in edit mode and animating in preview with a working pause control."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A22, SL-S54-1, SL-S54-2, SL-S54-3, SL-S54-4]`, `verification_method: exit-code` (A22: `screenshot-diff`, SL-S54-4: `manual-observation`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S21-token-compiler-dtcg-and-forge"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S25-pure-renderer-and-resolution-policy"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/containers.ts`, `scripts/lib/container-validate.ts`, the renderer's container branch, the component-bar tabbed picker, `04-site/pages/<id>.doc.json` through typed ops only."
  - "TypeScript on Bun."

files_prohibited:
  - "**No `.py` file anywhere in the skill tree.**"

flow: "slice-execution-flow"

effort_estimate: "L"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-160", "FR-161", "FR-162", "FR-163", "FR-164", "FR-165", "FR-166", "FR-167", "FR-168", "FR-169", "FR-170"]
  cqs: ["CQ12"]
  evidence_refs: ["EL-044", "EL-062", "EL-064"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S54-art-motion-container-contract.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
