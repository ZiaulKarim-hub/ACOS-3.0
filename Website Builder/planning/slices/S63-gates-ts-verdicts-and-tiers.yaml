# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S63-gates-ts-verdicts-and-tiers.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S63-gates-ts-verdicts-and-tiers"
story_id: "ST-21"          # Gates that measure, and captures that are valid evidence
epic_id: "E17"            # Quality gates and capture
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Structured gate verdicts and the four severity tiers"
objective: |
  Give every gate one shape - measured value, threshold, verdict, evidence reference - and never throw on a normal fail.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S63-1"
  - "[ ] SL-S63-2"
  - "[ ] SL-S63-3"
  - "[ ] Artifacts: `gates.ts`, the tier engine, `gate-report.json`, the two shipped gates, the recorded A66/A67 inconsistency."
  - "[ ] Validation: a forced failure returns a verdict rather than an exception; an inconclusive verdict blocks like a fail; each tier produces its documented consequence; ten identical violations show one badge reading 10."
  - "[ ] Demo-able increment: trigger a Tier 0 violation in the editor and watch the edit blocked, then a Tier 2 and watch it land in the pill."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S63-1, SL-S63-2, SL-S63-3]`, `verification_method: exit-code` (SL-S63-3: `manual-observation`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S23-versioned-coherence-lint-set"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S25-pure-renderer-and-resolution-policy"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/gates.ts`, `scripts/lib/tiers.ts`, `scripts/lib/gate-report.ts`, `07-lock/gate-report.json` (write), the Design Health pill's consumption point."
  - "TypeScript on Bun."

files_prohibited:
  - "**No `.py` file anywhere in the skill tree.**"

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-230", "FR-233", "FR-237"]
  cqs: ["CQ11"]
  evidence_refs: ["EL-061"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S63-gates-ts-verdicts-and-tiers.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
