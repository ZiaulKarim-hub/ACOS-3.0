# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S32-inline-plaintext-editing.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S32-inline-plaintext-editing"
story_id: "ST-10"          # Edits that are typed, atomic and undoable as one thought
epic_id: "E8"            # Editor shell and core operations
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Inline plaintext-only text editing"
objective: |
  Let the human edit copy in place without source-app markup surviving into the type system.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S32-1"
  - "[ ] SL-S32-2"
  - "[ ] Artifacts: the inline editor module, the `set-text` op, the sanitiser, the enumerated editable-class list."
  - "[ ] Validation: a rich-text paste from a word processor into each named class stores plain text only, proven by reading `content.json`; a markup payload posted directly is rejected with 400; each edit is exactly one undo entry."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S32-1, SL-S32-2]`, `verification_method: grep-assert` (SL-S32-2: `exit-code`)."

verification_method: "grep-assert"

dependencies:
  slices:
    - slice_id: "S31-typed-ops-autosave-history-undo"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`app/editor/inline-text.ts`, `scripts/lib/ops/set-text.ts`, `04-site/content.json` (written only through the op path)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "S"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-083"]
  cqs: []
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S32-inline-plaintext-editing.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
