# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S53-deterministic-lazy-variant-generator.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S53-deterministic-lazy-variant-generator"
story_id: "ST-17"          # Swaps that never destroy copy and never ship a placeholder
epic_id: "E10"            # Component bar and deterministic variants
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Deterministic, lazy, append-only variant generator with the indistinguishability rule"
objective: |
  Generate variants from tokens with no model call, only on first panel open, and never offer two the eye cannot tell apart.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A33"
  - "[ ] A34"
  - "[ ] SL-S53-1"
  - "[ ] SL-S53-2"
  - "[ ] SL-S53-3"
  - "[ ] Artifacts: `variants.ts`, the indistinguishability module, the per-direction cache, the instrumentation counter, the two-run determinism transcript."
  - "[ ] Validation: two runs produce identical bytes; an untouched family shows a zero counter; two rounds produce disjoint index sets; a deliberately near-duplicate candidate is rejected by recomputation."
  - "[ ] Demo-able increment: open a family panel in the editor and see generated variants with live ghosting."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A33, A34, SL-S53-1, SL-S53-2, SL-S53-3]`, `verification_method: exit-code` (A33: `manual-observation`, A34 and SL-S53-2: `recompute`, SL-S53-1: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S52-variant-axis-schema"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S51-slot-contracts-and-swap-safety"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/variants.ts`, `scripts/lib/indistinguishability.ts`, `scripts/lib/variant-cache.ts`, `05-variants/**` (write), the `POST /variants` route as a call site."
  - "TypeScript on Bun."

files_prohibited:
  - "**No `.py` file anywhere in the skill tree.**"

flow: "slice-execution-flow"

effort_estimate: "L"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-133", "FR-135", "FR-136", "FR-137"]
  cqs: ["CQ14"]
  evidence_refs: ["EL-037"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S53-deterministic-lazy-variant-generator.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
