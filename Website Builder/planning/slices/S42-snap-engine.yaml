# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S42-snap-engine.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S42-snap-engine"
story_id: "ST-13"          # Drag that writes grid integers, not pixels
epic_id: "E9"            # The canvas (DECISION-1 option B)
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Snap engine with prioritised target classes and zoom-scaled tolerance"
objective: |
  Snap to the four target classes in priority order with tolerance divided by zoom, using interval indexes rather than per-frame scans.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A47"
  - "[ ] SL-S42-1"
  - "[ ] SL-S42-2"
  - "[ ] Artifacts: the snap module, the interval index, the move counter, the priority test, the zoom test."
  - "[ ] Validation: the counter is flat across a 200-move synthetic drag; priority is asserted by a constructed collision per adjacent class pair; tolerance measured at 25% and 200% equals the base tolerance divided by zoom."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A47, SL-S42-1, SL-S42-2]`, `verification_method: exit-code` (A47: `manual-observation`; SL-S42-1: `recompute`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S41-gridline-overlay"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/canvas/snap.ts`, `scripts/lib/canvas/interval-index.ts`, the overlay measurements from S41 (read-only)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-107"]
  cqs: ["CQ4"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S42-snap-engine.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
