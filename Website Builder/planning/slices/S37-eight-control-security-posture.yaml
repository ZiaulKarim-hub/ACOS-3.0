# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S37-eight-control-security-posture.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S37-eight-control-security-posture"
story_id: "ST-12"          # Two writers, no lost work, and a localhost that is not a doorway
epic_id: "E16"            # Security, concurrency and lifecycle
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "The eight-control local-server security posture"
objective: |
  Treat localhost as a hostile boundary: bind, origin, exact CORS, bearer token, pinned host settings, idle shutdown, Host validation and security headers.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A76"
  - "[ ] A77"
  - "[ ] \"§12.17-A100\""
  - "[ ] SL-S37-1"
  - "[ ] SL-S37-2"
  - "[ ] SL-S37-3"
  - "[ ] Artifacts: `security.ts` with eight named middlewares, `token.ts`, the hook register/remove pair, the control→threat README table, a transcript per control."
  - "[ ] Validation: foreign-`Host` GET rejected and loopback-`Host` GET accepted; non-allowlisted `Origin` POST rejected; SSE upgrade rejected without a valid origin and token; `stat` shows `0600`; idle shutdown observed in the event log; a forced hook error still allows the tool call."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A76, A77, \"§12.17-A100\", SL-S37-1, SL-S37-2, SL-S37-3]`, `verification_method: exit-code` (SL-S37-1: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S08-server-ts-port"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S29-editor-shell-and-overlay"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/security.ts`, `scripts/lib/token.ts`, `scripts/lib/hooks.ts`, the `server.ts` middleware wiring from S08, the substrate config file."
  - "Runtime-only: `.wb/editor.token`."

files_prohibited:
  - "**No `.py` file may be created anywhere in the skill tree.**"

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-220", "FR-229"]
  cqs: ["CQ8"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S37-eight-control-security-posture.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
