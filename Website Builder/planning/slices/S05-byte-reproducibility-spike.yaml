# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S05-byte-reproducibility-spike.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S05-byte-reproducibility-spike"
story_id: "ST-01"          # Prove the product is buildable as written before committing scope
epic_id: "E0"            # Phase-0 spikes and blocking gates
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Two-build byte-reproducibility spike"
objective: |
  Establish whether byte-identical two-build comparison is achievable on the selected toolchain before success criterion S4 is treated as committed.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S05-1"
  - "[ ] SL-S05-2"
  - "[ ] Artifacts: both manifests, the diff, ADR-05, the before/after hashes of the live dependency surface."
  - "[ ] Validation: the comparison is a hash diff, not an inspection."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S05-1, SL-S05-2]`, `verification_method: hash-compare`."

verification_method: "hash-compare"

dependencies:
  slices:
    - slice_id: "S03-o8-substrate-spike"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`spikes/repro/**` (new, disposable), `docs/adr/ADR-05-byte-reproducibility.md` (new)"
  - "**The live manifest, lockfile and dependency tree must be byte-unchanged when this slice ends** — verify by hashing them before and after."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "S"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "diagnostic"
  moscow: "MUST"
  fr_ids: ["FR-006"]
  cqs: ["CQ9"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S05-byte-reproducibility-spike.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
