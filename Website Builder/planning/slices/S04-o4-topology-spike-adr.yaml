# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S04-o4-topology-spike-adr.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S04-o4-topology-spike-adr"
story_id: "ST-01"          # Prove the product is buildable as written before committing scope
epic_id: "E0"            # Phase-0 spikes and blocking gates
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Process-topology spike and ADR"
objective: |
  Decide single-origin proxy versus two-origin iframe plus message passing on measured evidence, not preference.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S04-1"
  - "[ ] SL-S04-2"
  - "[ ] Artifacts: both spikes, a scored table with measured values, both preview screenshots, ADR-04."
  - "[ ] Validation: latency is a median of five measured runs; the restart behaviour is observed, not reasoned about."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S04-1, SL-S04-2]`, `verification_method: manual-observation`."

verification_method: "manual-observation"

dependencies:
  slices:
    - slice_id: "S01-gate-16a-and-launcher-rung"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S03-o8-substrate-spike"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`spikes/topology/{a-two-origin,b-single-origin}/**` (new, disposable)"
  - "`docs/adr/ADR-04-topology.md` (new)"

files_prohibited:
  - "**No product path.**"

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "diagnostic"
  moscow: "MUST"
  fr_ids: ["FR-003"]
  cqs: ["CQ3"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S04-o4-topology-spike-adr.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
