# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S60-registry-table-embed-form.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S60-registry-table-embed-form"
story_id: "ST-20"          # A narrow custom-component path that stays on-system
epic_id: "E13"            # Step 6 custom components
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Whitelisted registry components: table, embed and form"
objective: |
  Ship the narrow custom-component registry against the direction's tokens, with a real placement path.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S60-1"
  - "[ ] SL-S60-2"
  - "[ ] SL-S60-3"
  - "[ ] Artifacts: the three generated kinds, the chart node registration + data field, the custom-slot contract, the insertion paths, the sign-off note."
  - "[ ] Validation: inserting a fifth kind is refused with the scope message; each of the four kinds is placeable in the editor; an off-token value in an inline-authored component is caught by the lint set before acceptance."
  - "[ ] Demo-able increment: insert a table, an embed and a form onto a page and see them read as part of the direction."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S60-1, SL-S60-2, SL-S60-3]`, `verification_method: exit-code` (SL-S60-2: `manual-observation`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S51-slot-contracts-and-swap-safety"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S23-versioned-coherence-lint-set"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/registry/{table,embed,form,index}.ts`, `scripts/lib/custom-slot.ts`, `06-custom/**` (write), the insertion path in the component bar, `04-site/pages/<id>.doc.json` through typed ops only."
  - "TypeScript on Bun."

files_prohibited:
  - "**No `.py` file anywhere in the skill tree.**"

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-190", "FR-191", "FR-194"]
  cqs: ["CQ1"]
  evidence_refs: ["EL-084"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S60-registry-table-embed-form.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
