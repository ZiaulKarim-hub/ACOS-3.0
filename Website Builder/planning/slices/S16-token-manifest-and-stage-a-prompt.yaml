# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S16-token-manifest-and-stage-a-prompt.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S16-token-manifest-and-stage-a-prompt"
story_id: "ST-05"          # A prompt that cannot silently lose or invent anything
epic_id: "E4"            # Step 2 prompt generator
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Mechanical token manifest and the Stage-A capsule prompt"
objective: |
  Emit a frozen token-name manifest generated mechanically from the inventory, and a Stage-A prompt that is greppably complete and traceable to interview ids.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A6"
  - "[ ] A4"
  - "[ ] SL-S16-1"
  - "[ ] SL-S16-2"
  - "[ ] Artifacts: manifest generator, Stage-A assembler, a generated prompt sample, the deviation record shape."
  - "[ ] Validation: seven greps prove the seven required contents; a diff proves the manifest is byte-identical wherever it is re-pasted; a sample directive shows its question id."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A6, A4, SL-S16-1, SL-S16-2]`, `verification_method: grep-assert` (SL-S16-1: `hash-compare`, SL-S16-2: `exit-code`)."

verification_method: "grep-assert"

dependencies:
  slices:
    - slice_id: "S14-concept-document-synthesis"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S15-font-catalog-and-snapshot"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/steps/step2.ts`, `scripts/lib/prompt-stage-a.ts`, `scripts/lib/token-manifest.ts`, `01-prompt/**` (write), `session.json` (deviation record)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "L"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-041", "FR-042", "FR-043", "FR-044", "FR-046"]
  cqs: ["CQ5", "CQ14"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S16-token-manifest-and-stage-a-prompt.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
