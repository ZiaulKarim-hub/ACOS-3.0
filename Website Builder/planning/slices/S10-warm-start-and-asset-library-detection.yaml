# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S10-warm-start-and-asset-library-detection.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S10-warm-start-and-asset-library-detection"
story_id: "ST-03"          # Start warm without inheriting the last site's identity
epic_id: "E2"            # Step 0 warm start
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Warm-start scan and asset-library detection"
objective: |
  Offer any reusable prior system inside the first three exchanges and determine, early, whether the artwork category is real for this project.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A1"
  - "[ ] A2"
  - "[ ] Artifacts: `warmstart.ts`, `step0.ts`, a `session.json` sample for both branches (library present, library absent)."
  - "[ ] Validation: a fixture project with a design-library entry produces an offer within three exchanges; a fixture with a real asset path records it; a fixture with none sets the warn flag."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A1, A2]`, `verification_method: exit-code` (A1: `manual-observation`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S09-install-config-session-selftest"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/warmstart.ts`, `scripts/steps/step0.ts`, `session.json` (write)."
  - "Read-only globs over the design-library, systems and project framework directories."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "S"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-020", "FR-021"]
  cqs: ["CQ16"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S10-warm-start-and-asset-library-detection.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
