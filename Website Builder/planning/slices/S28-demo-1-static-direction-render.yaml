# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S28-demo-1-static-direction-render.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S28-demo-1-static-direction-render"
story_id: "ST-08"          # One document, one renderer, one deterministic output
epic_id: "E7"            # Document model and pure renderer
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "DEMO 1 - interview to prompt to ingest to one direction rendered static"
objective: |
  Prove end to end that a conversation becomes a coherent, licence-clean design system and a page a human can look at.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A4"
  - "[ ] A5"
  - "[ ] A7"
  - "[ ] A12"
  - "[ ] SL-S28-1"
  - "[ ] Artifacts: `answers.json`, `concept.md`, both prompt stages, the ingested system, the rendered page, both screenshots, the run record with hashes."
  - "[ ] Validation: the A4 grep over emitted directives; the refusal-line check; the one-`pbpaste` ingest; the zero-paste regeneration leg; both screenshots at pinned heights; a human verdict in words."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A4, A5, A7, A12, SL-S28-1]`, `verification_method: manual-observation` (A4: `grep-assert`; A5, A7, A12: `exit-code`)."

verification_method: "manual-observation"

dependencies:
  slices:
    - slice_id: "S14-concept-document-synthesis"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S20-local-regeneration-mode"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S22-flat-variable-layer-and-extract-override"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S26-breakpoint-vocabulary-and-cascade"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S27-determinism-contract"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`evidence/demo-1/**` (write), the session directory (read), the capture wrapper."

files_prohibited:
  - "**No file under `scripts/` may be modified by this slice.**"

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "demo"
  moscow: "MUST"
  fr_ids: []
  cqs: ["CQ1", "CQ5", "CQ13"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S28-demo-1-static-direction-render.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
