# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S72-demo-4-locked-published-evidence.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S72-demo-4-locked-published-evidence"
story_id: "ST-24"          # Published, with the licence question answered
epic_id: "E15"            # Step 8 publish and evidence
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "DEMO 4 - locked, published, evidence-complete"
objective: |
  Demonstrate the full exit condition: a locked site with proof of purity, published, with a complete bundle.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A51"
  - "[ ] A72"
  - "[ ] A73"
  - "[ ] SL-S72-1"
  - "[ ] Artifacts: the run transcript, `gate-report.json`, `lock-manifest.json`, the tag, the publish record with a live URL, the complete bundle, the screenshot matrix, the demo README."
  - "[ ] Validation: eight purity gates pass (or gate 2 records the signed-off fallback with its enumerated exception set); all tier-one lock checks pass; `publishRecord.live` is `true`; zero missing licence classes; zero conformance strings."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A51, A72, A73, SL-S72-1]`, `verification_method: exit-code` (A51: `hash-compare`; A72/A73: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S70-publish-automated-with-runbook-fallback"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S71-licence-and-evidence-bundle"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`demos/demo-4/**` (transcript, artifacts, README), `.acos/evidence/<date>/website-<session>/` (verdict mirror)."

files_prohibited:
  - "**No product source file may be edited by this slice.** If the demo cannot pass without a code change, the demo **fails** and the change is made in its owning slice."

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "demo"
  moscow: "MUST"
  fr_ids: []
  cqs: ["CQ9", "CQ10"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S72-demo-4-locked-published-evidence.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
