# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S01-gate-16a-and-launcher-rung.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S01-gate-16a-and-launcher-rung"
story_id: "ST-01"          # Prove the product is buildable as written before committing scope
epic_id: "E0"            # Phase-0 spikes and blocking gates
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Gate 16-A turn-boundary probe and launcher-rung decision"
objective: |
  Determine whether a long-running local server survives this harness's turn boundary, and at which rung of the F1-F5 ladder.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A80"
  - "[ ] SL-S01-1"
  - "[ ] SL-S01-2"
  - "[ ] Artifacts: the probe script, the launcher for the passing rung, ADR-01, and a raw evidence log of every curl (command, exit code, HTTP status, timestamp, pid check)."
  - "[ ] Validation: at least three post-boundary checks plus one post-`/clear` check, all 200, original pid alive at each."
  - "[ ] Evidence bundle: all seven Dev sections, with the raw curl transcript attached, not summarised."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A80, SL-S01-1, SL-S01-2]`, `verification_method: probe`."

verification_method: "probe"

dependencies:
  slices:
    []

files_allowed:
  - "`scripts/probes/probe-turn-boundary.ts` (new)"
  - "`scripts/probes/launch-f1.ts`, `launch-f2.ts`, `launch-f3.sh` (new)"
  - "`docs/adr/ADR-01-launcher-rung.md` (new)"
  - "Read-only: `references/gotchas.md` if it exists."
  - "**Nothing under `src/`, `app/`, `.claude/agents/`, or any product path.**"

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "S"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "diagnostic"
  moscow: "MUST"
  fr_ids: ["FR-001", "FR-002"]
  cqs: ["CQ6"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S01-gate-16a-and-launcher-rung.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
