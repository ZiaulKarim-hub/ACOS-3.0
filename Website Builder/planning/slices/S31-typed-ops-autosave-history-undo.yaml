# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S31-typed-ops-autosave-history-undo.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S31-typed-ops-autosave-history-undo"
story_id: "ST-10"          # Edits that are typed, atomic and undoable as one thought
epic_id: "E8"            # Editor shell and core operations
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Typed-op engine, autosave, op log and transactional undo"
objective: |
  Make every edit a validated typed op applied atomically by the single writer, with one command stack over the document.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A31"
  - "[ ] A32"
  - "[ ] SL-S31-1"
  - "[ ] SL-S31-2"
  - "[ ] SL-S31-3"
  - "[ ] Artifacts: op schemas, the apply path, `history.jsonl` writer, the allowlist module, the queue, the undo stack."
  - "[ ] Validation: a swap and a section regeneration each undo in one step; a scripted continuous drag produces exactly one entry; every rejection case returns 400 and appears in the log; a killed process mid-write leaves no partial file."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A31, A32, SL-S31-1, SL-S31-2, SL-S31-3]`, `verification_method: exit-code`."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S29-editor-shell-and-overlay"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/ops/**`, `scripts/lib/op-schema.ts`, `scripts/lib/apply.ts`, `scripts/lib/history.ts`, `scripts/lib/allowlist.ts`, `app/editor/op-queue.ts`, `app/editor/history-ui.ts`."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "L"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-085", "FR-086", "FR-087"]
  cqs: ["CQ7", "CQ15"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S31-typed-ops-autosave-history-undo.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
