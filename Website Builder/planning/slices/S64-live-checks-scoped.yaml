# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S64-live-checks-scoped.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S64-live-checks-scoped"
story_id: "ST-21"          # Gates that measure, and captures that are valid evidence
epic_id: "E17"            # Quality gates and capture
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Live checks: scoped, sub-100ms, drop and mouseup only"
objective: |
  Run the live half of the gate suite without ever fighting the drag, including the running motion-concurrency counter.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A27"
  - "[ ] SL-S64-1"
  - "[ ] SL-S64-2"
  - "[ ] SL-S64-3"
  - "[ ] Artifacts: the live check set, the instrumentation trace, the motion counter, the Tier-2 anti-slop wiring, the latency record."
  - "[ ] Validation: the trace shows zero checks mid-drag and zero per-frame; the scoped run is under the latency budget on the reference fixture; a touched pair's contrast is recomputed in both measures; a missing alt blocks the placement; the counter increments visibly on each motion container placed."
  - "[ ] Demo-able increment: drag a low-contrast text block onto the canvas and be blocked at drop, with the pill showing the batched advisories."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A27, SL-S64-1, SL-S64-2, SL-S64-3]`, `verification_method: recompute` (SL-S64-3: `manual-observation`)."

verification_method: "recompute"

dependencies:
  slices:
    - slice_id: "S63-gates-ts-verdicts-and-tiers"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S43-drag-to-place-and-drop-algorithm"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/live-checks.ts`, `scripts/lib/contrast.ts` (reuse), `scripts/lib/motion-counter.ts`, the drop/mouseup handlers from S43, the Design Health pill."
  - "TypeScript on Bun."

files_prohibited:
  - "**No `.py` file anywhere in the skill tree.** `await document.fonts.ready` before ANY `getBoundingClientRect`."

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-231", "FR-234"]
  cqs: ["CQ11", "CQ12"]
  evidence_refs: ["EL-005", "EL-064"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S64-live-checks-scoped.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
