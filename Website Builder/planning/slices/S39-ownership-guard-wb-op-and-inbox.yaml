# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S39-ownership-guard-wb-op-and-inbox.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S39-ownership-guard-wb-op-and-inbox"
story_id: "ST-12"          # Two writers, no lost work, and a localhost that is not a doorway
epic_id: "E16"            # Security, concurrency and lifecycle
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Ownership guard with stated limits, the sanctioned agent write path, and the inbox"
objective: |
  Give the agent a legal write path so the ownership rule is followed rather than routed around, and keep one validation path for both writers.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A78"
  - "[ ] A79"
  - "[ ] A81"
  - "[ ] A89"
  - "[ ] \"§12.17-A96\""
  - "[ ] SL-S39-1"
  - "[ ] SL-S39-2"
  - "[ ] Artifacts: allowlist module, guard, `wb op`, inbox reader/writer, the amended skill-instruction text."
  - "[ ] Validation: symlinked path rejected; raw JSON Patch rejected; file path in body rejected; `/systemLock` patch rejected 400 from two different ops; `wb op` applies with no browser open; an inbox entry applies and is marked; a forced guard error still allows the tool call."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A78, A79, A81, A89, \"§12.17-A96\", SL-S39-1, SL-S39-2]`, `verification_method: exit-code` (A89 and SL-S39-2: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S38-reconciliation-409-lock-and-tab-claim"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/write-allowlist.ts`, `scripts/lib/ownership-guard.ts`, `scripts/wb.ts` (the `op` subcommand), `scripts/lib/inbox.ts`, the op validator from S31, the skill instruction file."
  - "Runtime-only: `.wb/inbox.jsonl`."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-221", "FR-223", "FR-224", "FR-228"]
  cqs: ["CQ7", "CQ8"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S39-ownership-guard-wb-op-and-inbox.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
