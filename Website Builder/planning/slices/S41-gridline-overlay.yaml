# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S41-gridline-overlay.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S41-gridline-overlay"
story_id: "ST-13"          # Drag that writes grid integers, not pixels
epic_id: "E9"            # The canvas (DECISION-1 option B)
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Gridline overlay derived from computed styles"
objective: |
  Paint the real resolved grid tracks as the snap target, in the out-of-iframe overlay so they vanish at LOCK by construction.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A39"
  - "[ ] SL-S41-1"
  - "[ ] SL-S41-2"
  - "[ ] Artifacts: the overlay module, the measurement module, the three-archetype test, the capture and its assertion."
  - "[ ] Validation: painted tracks equal computed tracks for three archetypes at `base`, `md` and `sm`; the preview capture contains no overlay pixels; no overlay DOM node exists inside the iframe."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A39, SL-S41-1, SL-S41-2]`, `verification_method: recompute` (SL-S41-2: `screenshot-diff`)."

verification_method: "recompute"

dependencies:
  slices:
    - slice_id: "S29-editor-shell-and-overlay"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S26-breakpoint-vocabulary-and-cascade"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/canvas/overlay-grid.ts`, `scripts/lib/canvas/measure.ts`, the S29 overlay mount point, the breakpoint vocabulary from S26 (read-only)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-100"]
  cqs: ["CQ4"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S41-gridline-overlay.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
