# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S11-identity-split-and-negative-constraints.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S11-identity-split-and-negative-constraints"
story_id: "ST-03"          # Start warm without inheriting the last site's identity
epic_id: "E2"            # Step 0 warm start
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "System/identity split, negative constraints and mined-source pre-fill"
objective: |
  Carry forward what is reusable and deliberately exclude what is identity, injecting prior identities as negative constraints.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S11-1"
  - "[ ] SL-S11-2"
  - "[ ] Artifacts: `identity-split.ts`, the session record of carried vs withheld, the negative-constraint block."
  - "[ ] Validation: a grep over the carried session shows **zero** excluded-set items; the negative-constraint block contains the prior hues; pre-filled answers are marked and overridable."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S11-1, SL-S11-2]`, `verification_method: grep-assert`."

verification_method: "grep-assert"

dependencies:
  slices:
    - slice_id: "S10-warm-start-and-asset-library-detection"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/warmstart.ts` (extend), `scripts/lib/identity-split.ts` (new), `session.json` (write), `00-interview/answers.json` (pre-fill only, with `source: pre-filled`)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "S"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-022", "FR-023", "FR-024"]
  cqs: ["CQ16"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S11-identity-split-and-negative-constraints.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
