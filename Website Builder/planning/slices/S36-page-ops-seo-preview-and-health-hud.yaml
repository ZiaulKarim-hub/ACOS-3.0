# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S36-page-ops-seo-preview-and-health-hud.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S36-page-ops-seo-preview-and-health-hud"
story_id: "ST-11"          # A safety net that makes experimenting cheap
epic_id: "E8"            # Editor shell and core operations
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Page-scoped ops (Branch A+), per-page SEO, preview mode and the Design Health HUD"
objective: |
  Ship page-scoped operations and per-page metadata now so multi-page is a later feature rather than a later migration, plus one honest health surface.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A37"
  - "[ ] \"§12.17-A97\""
  - "[ ] SL-S36-1"
  - "[ ] SL-S36-2"
  - "[ ] Artifacts: the page-scoped op envelope, the page ops, the SEO field set, preview mode, the health pill."
  - "[ ] Validation: `pages[]` has length one and every op is page-scoped; a `/systemLock` patch is rejected 400; the four write paths complete inside the allowlist; the pill shows a real observer-derived LCP, not a computed guess; a grep finds zero toast calls on the Tier-2 path."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A37, \"§12.17-A97\", SL-S36-1, SL-S36-2]`, `verification_method: exit-code` (A37: `grep-assert`; SL-S36-2: `manual-observation`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S31-typed-ops-autosave-history-undo"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/ops/page-ops.ts`, `scripts/lib/seo-fields.ts`, `app/editor/preview-mode.ts`, `app/editor/health-pill.ts`, `04-site/site.json` (through the op path only)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "L"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-091", "FR-092", "FR-093", "FR-094"]
  cqs: ["CQ11"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S36-page-ops-seo-preview-and-health-hud.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
