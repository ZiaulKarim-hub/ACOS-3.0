# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S23-versioned-coherence-lint-set.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S23-versioned-coherence-lint-set"
story_id: "ST-07"          # Coherence by computation, enforced by lints
epic_id: "E6"            # Token compiler and coherence lints
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "The versioned coherence-lint set, run at ingest and at LOCK"
objective: |
  Replace an unstable lint count with a versioned set whose required members are named, including logical-properties-only.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A17"
  - "[ ] SL-S23-1"
  - "[ ] SL-S23-2"
  - "[ ] Artifacts: registry, the named lints, the report shape, both call sites."
  - "[ ] Validation: a border-only fixture referencing a shadow token fails; a physical-direction fixture fails at ingest **and** at lock; the report names the set version."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A17, SL-S23-1, SL-S23-2]`, `verification_method: exit-code` (SL-S23-1/2: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S21-token-compiler-dtcg-and-forge"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/lints/**`, `scripts/lib/lint-registry.ts`, the import path (S19) and the lock path (S68) as call sites only."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-063"]
  cqs: ["CQ5"]
  evidence_refs: ["EL-087"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S23-versioned-coherence-lint-set.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
