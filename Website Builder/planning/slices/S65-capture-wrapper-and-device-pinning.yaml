# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S65-capture-wrapper-and-device-pinning.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S65-capture-wrapper-and-device-pinning"
story_id: "ST-21"          # Gates that measure, and captures that are valid evidence
epic_id: "E17"            # Quality gates and capture
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Capture wrapper with the inherited wait recipe and device-height pinning"
objective: |
  Produce captures that are valid evidence, especially for viewport-height layouts, with zero npm dependencies.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S65-1"
  - "[ ] SL-S65-2"
  - "[ ] SL-S65-3"
  - "[ ] SL-S65-4"
  - "[ ] Artifacts: `capture.ts`, `wait-recipe.ts`, the capture manifest with per-shot `{breakpoint, scheme, motion, devicePinned, measuredFrameHeight}`, one full matrix run."
  - "[ ] Validation: `npm ls` / lockfile hash unchanged; a deliberately mis-sized frame fails the height assertion; a lazy-image fixture captures non-blank."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S65-1, SL-S65-2, SL-S65-3, SL-S65-4]`, `verification_method: exit-code` (SL-S65-2/4: `grep-assert`; SL-S65-3: `recompute`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S63-gates-ts-verdicts-and-tiers"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/capture.ts`, `scripts/lib/wait-recipe.ts`, `scripts/capture-matrix.ts`, `07-lock/screenshots/` (write)."

files_prohibited:
  - "**No `.py` file anywhere.** No package added to the live dependency tree by this slice."

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-235", "FR-236"]
  cqs: ["CQ17"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S65-capture-wrapper-and-device-pinning.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
