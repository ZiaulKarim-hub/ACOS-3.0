# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S70-publish-automated-with-runbook-fallback.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S70-publish-automated-with-runbook-fallback"
story_id: "ST-24"          # Published, with the licence question answered
epic_id: "E15"            # Step 8 publish and evidence
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Automated publish with an honest runbook fallback"
objective: |
  Commit automated deploy as the v1 behaviour and record plainly when the fallback means the site is locked but not published.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S70-1"
  - "[ ] SL-S70-2"
  - "[ ] SL-S70-3"
  - "[ ] Artifacts: `publish.ts`, the runbook template, `publish-record.json` from both paths, two recorded transcripts (automated and fallback)."
  - "[ ] Validation: with a valid credential the deploy runs with no interactive prompt and records the URL; with the credential removed the run emits a runbook and records `live: false`; a grep of the bundle and logs for credential material returns zero."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S70-1, SL-S70-2, SL-S70-3]`, `verification_method: exit-code` (SL-S70-2: `grep-assert`; SL-S70-3: `manual-observation`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S69-lock-snapshots-manifest-and-unlock"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/publish.ts`, `scripts/lib/publish-target.ts`, `scripts/lib/runbook.ts`, `evidence/publish-record.json` (write), `07-lock/runbook.md` (write)."

files_prohibited:
  - "**No credential is ever written into the session tree, the evidence bundle or the git repository** — the credential lives wherever the deploy tool already keeps it, and this slice only detects presence and validity."

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-210", "FR-211"]
  cqs: []
  evidence_refs: ["EL-059"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S70-publish-automated-with-runbook-fallback.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
