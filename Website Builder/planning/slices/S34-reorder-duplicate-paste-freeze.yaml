# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S34-reorder-duplicate-paste-freeze.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S34-reorder-duplicate-paste-freeze"
story_id: "ST-10"          # Edits that are typed, atomic and undoable as one thought
epic_id: "E8"            # Editor shell and core operations
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Section reorder, duplicate, copy/paste with overrides, and element freeze"
objective: |
  Give the structural edits that precede the canvas, without losing breakpoint overrides or overloading the word lock.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S34-1"
  - "[ ] SL-S34-2"
  - "[ ] SL-S34-3"
  - "[ ] Artifacts: the structure ops, the fragment extractor, the clipboard binding, the freeze check, the string-grep transcript."
  - "[ ] Validation: a fragment with `md` and `sm` overrides and a free-mode entry pastes byte-identical modulo ids; a frozen node refuses all five verbs with a reason; the user-visible string grep shows zero element-protection uses of \"Lock\"; each of these is one undo entry."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S34-1, SL-S34-2, SL-S34-3]`, `verification_method: hash-compare` (SL-S34-2: `grep-assert`; SL-S34-3: `exit-code`)."

verification_method: "hash-compare"

dependencies:
  slices:
    - slice_id: "S31-typed-ops-autosave-history-undo"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/ops/structure-ops.ts`, `scripts/lib/fragment.ts`, `app/editor/clipboard.ts`, `app/editor/freeze.ts`, the user-visible string table."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-089", "FR-090"]
  cqs: []
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S34-reorder-duplicate-paste-freeze.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
