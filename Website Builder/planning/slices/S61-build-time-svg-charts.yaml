# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S61-build-time-svg-charts.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S61-build-time-svg-charts"
story_id: "ST-20"          # A narrow custom-component path that stays on-system
epic_id: "E13"            # Step 6 custom components
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Build-time SVG charts with dataviz sub-tokens and two shipped data states"
objective: |
  Make charts read as one system with the site, colourblind-safe in both schemes, and ship no chart runtime.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] SL-S61-1"
  - "[ ] SL-S61-2"
  - "[ ] SL-S61-3"
  - "[ ] SL-S61-4"
  - "[ ] Artifacts: the mark emitters, the chrome kit, the ramp derivation and its validation record, the two shipped states, the editor-only previews."
  - "[ ] Validation: published output contains SVG and no chart script; the ramp validation is recomputed in both schemes; a fifth mark type is rejected; loading/error strings are absent from published output."
  - "[ ] Demo-able increment: paste a small table of numbers into a chart node and see a token-styled chart render statically on the page."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [SL-S61-1, SL-S61-2, SL-S61-3, SL-S61-4]`, `verification_method: grep-assert` (SL-S61-2: `recompute`, SL-S61-4: `manual-observation`)."

verification_method: "grep-assert"

dependencies:
  slices:
    - slice_id: "S60-registry-table-embed-form"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/charts/{marks,chrome,ramps,states,index}.ts`, `scripts/lib/dataviz-tokens.ts`, `06-custom/charts/**` (write), the chart node's renderer branch."
  - "TypeScript on Bun."

files_prohibited:
  - "**No `.py` file anywhere in the skill tree.**"

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-192", "FR-193"]
  cqs: ["CQ1"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S61-build-time-svg-charts.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
