# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S13-interview-engine-tiers-waves-branching.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S13-interview-engine-tiers-waves-branching"
story_id: "ST-04"          # An interview that ends with a concept, not a questionnaire
epic_id: "E3"            # Step 1 interview (90 questions)
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Interview engine: tiers, waves, real branch map, policy questions, instrumentation"
objective: |
  Deliver the interview in hard-gated waves with honest tier semantics, a branch map that matches the bank, and elapsed-time instrumentation from day one.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A3"
  - "[ ] SL-S13-1"
  - "[ ] SL-S13-2"
  - "[ ] SL-S13-3"
  - "[ ] Artifacts: engine, branch map, `answers.json` with source and override fields, selftest assertions for the branch map and the tighten-only rule."
  - "[ ] Validation: the fast-mode fixture records the measured Tier-1 count (expected 45–55) rather than asserting the older ≤45 figure; the tighten-only assertion fails if a loosening path is introduced."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A3, SL-S13-1, SL-S13-2, SL-S13-3]`, `verification_method: exit-code`."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S12-interview-bank-90-questions"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/steps/step1.ts`, `scripts/lib/interview-engine.ts`, `scripts/lib/branch-map.ts`, `00-interview/answers.json` (write), `scripts/selftest.ts` (extend)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "L"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-031", "FR-032", "FR-034", "FR-035", "FR-036", "FR-037", "FR-039"]
  cqs: ["CQ1", "CQ11"]
  evidence_refs: ["EL-060"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S13-interview-engine-tiers-waves-branching.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
