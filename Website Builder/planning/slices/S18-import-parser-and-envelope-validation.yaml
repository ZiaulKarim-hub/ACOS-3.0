# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S18-import-parser-and-envelope-validation.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S18-import-parser-and-envelope-validation"
story_id: "ST-06"          # An ingest that refuses rather than half-applies
epic_id: "E5"            # Step 3 ingest and Local Regeneration Mode
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Tolerant FILE-block parser with envelope validation and no partial writes"
objective: |
  Ingest a complete chunk in one paste, and fail a truncated chunk loudly without writing a partial system.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A7"
  - "[ ] A8"
  - "[ ] SL-S18-1"
  - "[ ] Artifacts: parser, validation, staging/promotion, both fixtures (complete and truncated)."
  - "[ ] Validation: a directory hash of the system tree before and after the truncated fixture is **identical**."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A7, A8, SL-S18-1]`, `verification_method: exit-code` (SL-S18-1: `hash-compare`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S17-stage-b-envelope-and-chunking"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/steps/step3.ts`, `scripts/lib/import-parser.ts`, `scripts/lib/envelope.ts` (reuse), `.wb/tmp/import/**` (staging), `02-system/**` (promote only)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-050", "FR-051"]
  cqs: ["CQ13"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S18-import-parser-and-envelope-validation.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
