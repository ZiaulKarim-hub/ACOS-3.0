# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S38-reconciliation-409-lock-and-tab-claim.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S38-reconciliation-409-lock-and-tab-claim"
story_id: "ST-12"          # Two writers, no lost work, and a localhost that is not a doorway
epic_id: "E16"            # Security, concurrency and lifecycle
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Hash-journal reconciliation, 409 concurrency, process lock and tab claim"
objective: |
  Make silent work loss impossible in either direction, with reconciliation as the authoritative mechanism rather than the guard hook.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A82"
  - "[ ] \"§12.17-A95\""
  - "[ ] SL-S38-1"
  - "[ ] SL-S38-2"
  - "[ ] Artifacts: journal writer, reconciler, conflict-copy path, 409 save path, lock, tab claim."
  - "[ ] Validation: a heredoc write to a doc-owned file while the lock is held is detected before the next save, the divergent version exists under `.wb/conflicts/<iso>/`, and **both** versions are byte-recoverable; a stale save returns 409; a second tab is read-only."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A82, \"§12.17-A95\", SL-S38-1, SL-S38-2]`, `verification_method: exit-code` (§12.17-A95: `hash-compare`; SL-S38-1/2: `manual-observation`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S31-typed-ops-autosave-history-undo"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S37-eight-control-security-posture"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/reconcile.ts`, `scripts/lib/hash-journal.ts`, `scripts/lib/editor-lock.ts`, `scripts/lib/tab-claim.ts`, the save path and SSE handler from S31/S29."
  - "Runtime-only: `.wb/doc-hashes.json`, `.wb/editor.lock`, `.wb/conflicts/**`."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-222", "FR-225", "FR-226"]
  cqs: ["CQ7"]
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S38-reconciliation-409-lock-and-tab-claim.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
