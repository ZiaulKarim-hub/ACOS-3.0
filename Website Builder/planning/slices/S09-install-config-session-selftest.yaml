# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S09-install-config-session-selftest.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S09-install-config-session-selftest"
story_id: "ST-02"          # A TypeScript spine that installs once and resumes from disk
epic_id: "E1"            # Skill scaffold and the TypeScript spine
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Symlink installer, project config, session tree, nested git repo, selftest harness"
objective: |
  Make installation drift-proof and every session resumable, versioned and self-testable from the first run.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A88"
  - "[ ] A83"
  - "[ ] A85"
  - "[ ] SL-S09-1"
  - "[ ] Artifacts: installer, session bootstrap, config template plus snapshot, selftest harness."
  - "[ ] Validation: `ls -la` of the global skills directory shows an arrow; the site tree reports itself as a git repository; the sessions path is ignored by the framework repo; `bun selftest.ts` exits 0 with 100% of assertions passing."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A88, A83, A85, SL-S09-1]`, `verification_method: exit-code` (A88: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S07-skill-router-and-confirmation-gate"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`install.sh`, `scripts/selftest.ts`, `scripts/lib/session.ts`, `.acos/config/website-builder.yaml` (template), the framework `.gitignore`."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-013", "FR-014", "FR-015", "FR-016"]
  cqs: []
  evidence_refs: []
  source_task_file: "planning/preeng/001-website-builder/tasks/S09-install-config-session-selftest.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
