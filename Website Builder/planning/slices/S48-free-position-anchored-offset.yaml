# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S48-free-position-anchored-offset.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S48-free-position-anchored-offset"
story_id: "ST-15"          # Responsive authoring where nothing is invisible
epic_id: "E9"            # The canvas (DECISION-1 option B)
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Free-position escape hatch: anchored offset, authored flow fallback, demotion and lock gate"
objective: |
  Ship the escape hatch narrowly: parent or grid-cell anchors only, with reserved space, an authored fallback, a visible counter and a hard lock gate.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A43"
  - "[ ] A44"
  - "[ ] A45"
  - "[ ] A48"
  - "[ ] SL-S48-1"
  - "[ ] SL-S48-2"
  - "[ ] SL-S48-3"
  - "[ ] Artifacts: free-position module, fallback writer and Navigator editing surface, the counter, the A44 gate function, schema and op validators."
  - "[ ] Validation: a sibling anchor value is rejected by the **schema**, not by the UI; every free drop writes a `flowFallback` and a reserved `min-block-size`; demotion fires at ≤390px and not at 479px; the third free block in a section returns 422; a forced pinned-container placement without confirmation returns 422; an overflow-producing fixture fails the gate; the locked export greps clean of positioning script."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A43, A44, A45, A48, SL-S48-1, SL-S48-2, SL-S48-3]`, `verification_method: exit-code` (A45: `recompute`; A48: `manual-observation`; SL-S48-1/3: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S46-override-cascade-and-precommit-chip"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/canvas/free-position.ts`, `scripts/lib/canvas/flow-fallback.ts`, `scripts/lib/gates/free-position-gate.ts`, the `set-free-position` / `clear-free-position` op validators (S31), the Navigator from S30 (fallback editing surface)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "L"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-116", "FR-117"]
  cqs: ["CQ2", "CQ4"]
  evidence_refs: ["EL-072"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S48-free-position-anchored-offset.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
