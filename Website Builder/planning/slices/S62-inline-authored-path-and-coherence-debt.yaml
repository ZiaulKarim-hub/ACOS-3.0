# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S62-inline-authored-path-and-coherence-debt.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S62-inline-authored-path-and-coherence-debt"
story_id: "ST-20"          # A narrow custom-component path that stays on-system
epic_id: "E13"            # Step 6 custom components
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "Inline-authored components, coherence debt, the signature-moment rule and third-party marks"
objective: |
  Allow bespoke work through one audited door, record what it costs in coherence, and never redraw a third-party mark.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A75"
  - "[ ] SL-S62-1"
  - "[ ] SL-S62-2"
  - "[ ] SL-S62-3"
  - "[ ] Artifacts: the inline path, the ledger and its writer, the signature-moment lint, the `[3P]` refusal, the two grep assertions."
  - "[ ] Validation: an accepted off-system value appears in the ledger with a reason; a second signature moment is flagged; a redraw attempt on a `[3P]` mark is refused; no `Task(` spawn and no custom-code-block container exist."
  - "[ ] Demo-able increment: author one bespoke component inline, accept one off-system value, and read the resulting debt entry in the editor."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A75, SL-S62-1, SL-S62-2, SL-S62-3]`, `verification_method: exit-code` (A75: `manual-observation`, SL-S62-1: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S23-versioned-coherence-lint-set"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S60-registry-table-embed-form"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/inline-authored.ts`, `scripts/lib/coherence-ledger.ts`, `scripts/lib/lints/signature-moment.ts`, `scripts/lib/third-party-marks.ts`, `04-site/coherence-ledger.json` (write), `06-custom/**`."
  - "TypeScript on Bun."

files_prohibited:
  - "**No `.py` file anywhere in the skill tree.**"

flow: "slice-execution-flow"

effort_estimate: "M"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-195", "FR-196", "FR-197"]
  cqs: ["CQ1"]
  evidence_refs: ["EL-084"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S62-inline-authored-path-and-coherence-debt.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
