# Generated by _bridge-to-slices.ts from preeng feature 001-website-builder.
# Source of truth for this slice: planning/preeng/001-website-builder/tasks/S67-eight-purity-gates.md
# Preeng ids are carried verbatim — do not renumber.

slice_id: "S67-eight-purity-gates"
story_id: "ST-22"          # An export that proves it is clean
epic_id: "E14"            # Step 7 LOCK
vision_id: "VISION-WEBSITE-BUILDER"
created_date: "2026-07-26"
created_by: acos-preeng-classic-bridge
status: "planned"

title: "The eight purity gates, with isolated two-build comparison"
objective: |
  Prove the published tree is clean rather than claim it, and run the two-build comparison without touching the live dependency tree.

# Ids come from the task file's own slice.yaml mapping line; the statements
# come from its Definition of Done. Nothing here is invented.
acceptance_criteria:
  - "[ ] A51"
  - "[ ] A52"
  - "[ ] A53"
  - "[ ] A57"
  - "[ ] \"§12.17-A93\""
  - "[ ] \"§12.17-A94\""
  - "[ ] SL-S67-1"
  - "[ ] SL-S67-2"
  - "[ ] Artifacts: eight gate modules, `gate-report.json` with eight rows, both build manifests, the screenshot pair and its diff, the before/after hashes of the live dependency surface."
  - "[ ] Validation: eight rows present (five is a fail of SL-S67-1); gate 6 output is a node list; a seeded design-time origin inside `srcset` is caught by gate 7; the live lockfile hash is unchanged."
  - "[ ] `slice.yaml` mapping — `acceptance_criteria: [A51, A52, A53, A57, \"§12.17-A93\", \"§12.17-A94\", SL-S67-1, SL-S67-2]`, `verification_method: exit-code` (A51/A53/§12.17-A94: `hash-compare`; A52: `screenshot-diff`; §12.17-A93: `grep-assert`)."

verification_method: "exit-code"

dependencies:
  slices:
    - slice_id: "S66-lock-render-scrub-and-absence-mechanisms"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S05-byte-reproducibility-spike"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"
    - slice_id: "S65-capture-wrapper-and-device-pinning"
      what_it_provides: "# TODO: confirm — see the task file's In scope"
      status: "planned"

files_allowed:
  - "`scripts/lib/gates/purity/*.ts`, `scripts/lib/gate-report.ts`, `.wb/tmp/gate2/` (scratch, git-ignored), `07-lock/{gate-report.json, manifest-a.json, manifest-b.json}` (write)."
  - "**Gate 2 must never edit the live `package.json`, the live lockfile or the live dependency tree** — hash all three before and after and assert unchanged; a design server running in another terminal must be unaffected (§12.17-A94)."

files_prohibited:
  []

flow: "slice-execution-flow"

effort_estimate: "L"

assigned_to: null
assigned_date: null
handoff_path: null

# Traceability — every id below resolves into the preeng artifact set.
traceability:
  type: "build"
  moscow: "MUST"
  fr_ids: ["FR-202", "FR-203"]
  cqs: ["CQ9"]
  evidence_refs: ["EL-022", "EL-088", "EL-085"]
  source_task_file: "planning/preeng/001-website-builder/tasks/S67-eight-purity-gates.md"

notes: |
  Read the source task file before starting — it carries the PM / Dev / QA
  contract in full, including the seven-part evidence bundle and the zero-trust
  QA steps this skeleton only summarises.
  Role charters: planning/preeng/001-website-builder/agent_instructions/{pm,dev,qa}.md
  BUILD GATE: §18 admits no v1 build slice until its sign-off rows are resolved.
  Four remain unsigned — 4(c) rich-text-is-v2, 4(d) one-direction-only, 4(f)
  editor-lacks-zoom-pan-rulers-multi-select, 6 charts-partial.
