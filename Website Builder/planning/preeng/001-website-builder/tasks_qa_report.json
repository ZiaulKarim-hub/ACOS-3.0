{
  "qa_status": "APPROVED",
  "issues": [
    "NON-BLOCKING (id-format drift, traceability): eleven acceptance-criterion ids are spelled `section 12.17-A9n` in stories.json and `§12.17-A9n` in the corresponding task file (S19 A101; S24 A92 and A98; S26 A91; S36 A97; S37 A100; S38 A95; S39 A96; S59 A99; S67 A93 and A94). Every one resolves after normalising `section ` to `§`; none is missing and none is wrong. A byte-exact traceability join between stories.json and tasks/*.md fails on all eleven, which is exactly the class of breakage the id-collision register was opened to prevent. Recommend one spelling.",
    "NON-BLOCKING (header/JSON disagreement): S72-demo-4-locked-published-evidence carries `| Requirements | FR-200, FR-216 |` in its header table while its stories.json `fr_ids` is empty. Empty `fr_ids` is the deliberate demo-slice convention (S28, S40 and S50 all carry none, and the 188-FR partition depends on demos claiming zero). FR-200 is owned by S66 and FR-216 by S71, so no requirement is orphaned or double-claimed — but S72's header reads as a third claim and must be re-worded to `via S66, S71` or emptied.",
    "NON-BLOCKING (header/JSON disagreement): S70 and S71 carry `| Phase / Demo | Phase 5 / Demo 4 (feeds) |` while their stories.json `demo` field is null. The parenthetical `(feeds)` disambiguates for a human reader, but a machine count of demo-bearing slices returns 6 from the task files and 4 from stories.json. stories.json is the record; the task headers should read `feeds Demo 4 (S72)`.",
    "NON-BLOCKING (Lean-Context-Engineering structural gap): three of 78 task files omit an explicit `**Steps.**` label — S43 (its nine numbered branches sit under `**In scope — the nine branches, each separately tested.**`), S50 (its five numbered demo steps sit under `**In scope.**`), and S48 (no numbered step list at all; its execution sequence must be inferred from the In-scope paragraph and the Definition of Done). S48 is the weakest of the three: it is the free-position escape hatch, the slice with the highest residual risk in the canvas epic, and it is the one slice whose Dev has no ordered procedure to execute. All three still carry Objective, In scope, Out of scope, Allowed files and Definition of Done.",
    "NON-BLOCKING (prioritisation is effectively flat): 77 of 78 slices are MoSCoW MUST and exactly one (S49, the canvas tail: zoom, pan, rulers, multi-select) is COULD. No SHOULD and no WON'T tier is used anywhere. The consequence is that the slice layer offers the schedule exactly one trade-back-out lever, which contradicts the plan's own framing that the canvas tail is merely 'the first candidate to trade back out'. This is honest — DECISION-1 option B did make almost everything mandatory — but it means scope relief cannot be found in the backlog, only in the sign-off table.",
    "NON-BLOCKING (dependency-graph gap): S04, the O4 topology spike and its ADR, has ZERO dependents. The editor shell S29 depends on S25 and S08 only. Both plan.md and the task prose state the binding rule — until the O4 ADR lands, only the topology-independent invariants I1-I6 may be built — but that rule is nowhere expressed in `depends_on`, so a mechanical scheduler is free to build the entire editor before the topology is chosen. S01 (Gate 16-A) by contrast is correctly wired: it is a transitive prerequisite of S08 and therefore of everything server-dependent. Recommend adding S04 to S29's `depends_on`.",
    "NON-BLOCKING (inferred path, recorded not fixed): `coherence-ledger.json` is written by S51 and S62, but data-model.md §3.21 names the `CoherenceLedger` entity without giving it a path and the §1 file map does not list it. The `04-site/` prefix used upstream is an inference `[I]`. Either the data model gains the path or the two slices must stop asserting one.",
    "NON-BLOCKING (source contradiction carried forward, NOT fixed here): plan.md §3.2's demo table places all four demos at slice numbers stories.json does not use — Demo 1 at S30 vs S28, Demo 2 at S41 vs S40, Demo 3 at S51 vs S50, Demo 4 at S73 vs S72. The task files follow stories.json throughout. plan.md owns the edit; this command did not modify it. The carried defect list named only Demo 2 and Demo 3; the disagreement is in fact four-for-four.",
    "NON-BLOCKING (source id collision, wider than previously recorded): A91-A97 are each defined THREE times in the source PRD — §7 (line 1221ff, forced-colors render, text-wrap balance, pick extensions, direction-hash, editor-renders-zero-controls), §12.17 (line 2861ff) and §18 (line 3980ff) — and A98-A101 twice (§12.17 and §18). Prior QA recorded a two-section collision. Every A91+ citation in stories.json and in the task files is section-qualified, so nothing downstream is ambiguous, but the renumbering pass the register calls for must reconcile three sets, not two.",
    "NON-BLOCKING (source count defect): §12.5's amendment note announces 'new criteria A91-A99 below are appended' while §12.17's own table runs A91 through A101 — the announcing sentence understates its own table by two rows. Downstream citations follow the table, not the sentence.",
    "NON-BLOCKING (namespace overload): the bare token `A<n>` denotes three different things in the source PRD — §5's interview-bank question ids (A1-A5 observed), §8's accessibility requirement rows (A1-A6 observed) and §19's acceptance criteria (A1-A90). Downstream, an unqualified `A<n>` always means §19, but no artifact states that rule, so a reader arriving at a task file from §5 or §8 can mis-resolve a citation. §19 remains the count of record at 90 criteria `[V - read in full]`; the '96 criteria' figure remains unsupported.",
    "NON-BLOCKING (out-of-range criterion citations): plan.md prose cites A102 and A112, which exist in none of the three A91+ sets — all three stop at A101. Neither appears in stories.json or in any task file, so no slice is built on them, but they are unresolvable ids in a planning artifact and must be renumbered or removed with the rest of the A91+ pass.",
    "NON-BLOCKING (evidence-ledger coverage): 22 of 88 ledger entries are cited by a slice's `evidence_refs` or named in a task file; 66 are not. Every cited ref resolves (zero unresolved). The uncited majority is concentrated in the research-only entries (market history, framework rejections, licence texts) that inform the spec rather than any single slice, so this is a coverage observation rather than a defect — but it means the ledger cannot be used as a completeness check on the backlog.",
    "NON-BLOCKING (requirement-id density): the FR register runs FR-001 to FR-253 with 188 ids allocated and 65 numbers unallocated. This is block allocation by epic, not loss: every allocated id is claimed by exactly one slice and every id cited in a task file is defined. An id-density check run in ignorance will read 65 missing requirements.",
    "NON-BLOCKING (citation style, upstream): plan.md cites only four FR ids and tech_prd.md and data-model.md cite none — all three reference requirements in prose instead of by id. Traceability from spec.md to the backlog is therefore carried entirely by stories.json and the task files. It holds (188/188 both directions), but it has no second, independent path.",
    "NON-BLOCKING (carried source defects re-verified as still open, none fixed by this command): FR-106 labels the three legal drop rejections `§11.2.1 AC7-AC9` while SL-S43-1 enumerates nine branches with the rejections as the ninth (S43 records the divergence as an Assumption and follows the nine-branch enumeration); FR-228 names the inbox `commands.jsonl` while the data model makes `.wb/inbox.jsonl` canonical with `commands.jsonl` as an alias; success criterion S2 is named a 'one-paste protocol' while budgeting three pastes per chunk as a retry allowance; lock-time check 11a requires a skip-link component that the component inventory does not contain (one row, one variant, owed); A66 omits INP and A67 states a flat 2MB against NFR-04/§13.4 gate 20, the canonical threshold, so a §19 edit is owed; and the component inventory stands at 216 rows / 1,228 variants of record `[V §8.2]` against an 87/674 v1 cut candidate (DECISIONS item 2, UNSIGNED) and a stale ~50/~430 sizing that §18 and §13 were built on.",
    "BUILD-START GATE (not a task-file defect; recorded because it governs when any of these 78 slices may begin): §18's sign-off table still has FOUR unsigned rows — 4(c) rich-text-is-v2, 4(d) one-direction-only / no cross-direction swaps, 4(f) the editor still lacks zoom, pan, rulers and multi-select, and 6 charts-partial. DECISION 1 (option B) resolved rows 4(a), 4(b) and consequentially 7. §18's own precondition is that nothing in v1 may be built until every sign-off row is resolved. The backlog is therefore complete and unstartable at the same time.",
    "EVIDENCE LIMITATION (not a defect): no code exists yet. Every acceptance criterion was verified for id resolution, for the presence of a `verification_method` from the seven-value vocabulary actually used (exit-code 118, grep-assert 73, manual-observation 54, recompute 29, hash-compare 19, probe 9, screenshot-diff 4), and for citation inside its own task file. None was verified for executability, and no gate was run.",
    "EVIDENCE LIMITATION (not a defect): the 78 task files were judged MECHANICALLY on structure, header-to-JSON agreement, id resolution, dependency topology, learnings-section content length and file termination, plus a full read of three files (S01, S43, S48 in part). Prose quality across the remaining 75 is inferred from that sample and from the mechanical signals, not read line by line. Every file terminates inside its QA Learnings block; none is truncated.",
    "OPEN, NO KNOWN MITIGATION (carried, not solved by any slice): motion feel cannot be judged while editing because the editor runtime fights the site runtime (S54 records the limit rather than closing it); sibling-anchored free positioning is UNPROTOTYPED and S48 states the limit instead of shipping it; no consulted source establishes bundler byte-reproducibility, so S05's spike may force the D3-weakening normalised-comparison fallback that itself needs sign-off; and the generation-channel output ceiling is unknown, so S17's chunking is computed from measured artifact sizes at runtime rather than from any published figure."
  ],
  "notes": "Preconditions verified before any judgement: plan.md (48,992 bytes) and tech_prd.md (67,259 bytes) both exist at the absolute feature directory, and planning_qa_report.json carries qa_status APPROVED, so the planning-QA error gate did not fire and /preeng.tasks was entitled to run. All three required outputs exist on disk: stories.json (152,839 bytes), 78 files under tasks/ (394,515 bytes total, mean 5,058, min 3,183 S22, max 7,361 S67) and this report. stories.json parses and is schema-conformant against the minimal deterministic schema adopted in plan.md section 7. SLICE-TO-TASK COVERAGE: 78 slices, 78 task files, zero missing, zero extra, and every slice's task_file field equals tasks/<id>.md. STRUCTURE: all 78 files carry PM, Dev, QA, Dev Learnings and QA Learnings; all 78 carry the five header rows; all 78 name a slice.yaml acceptance_criteria + verification_method mapping; all 78 name a rejection condition in the QA section; all 78 carry a seven-part evidence bundle; zero learnings sections are under 40 characters; zero files are truncated. REQUIREMENTS: 188 FR ids defined as table rows in spec.md section 4.1; 188 claimed across the 74 non-demo slices; zero defined-but-unclaimed; zero claimed-but-undefined; zero claimed by more than one slice; zero task files omitting a declared FR. The partition is exact. ACCEPTANCE CRITERIA: A1-A90 are all cited, zero uncited, zero cited outside the source set; 180 slice-local SL-<slice>-<n> criteria, all namespaced to their own slice, all carrying a statement, zero duplicated; 11 section-qualified A91+ ids, all resolving into the section 12.17 table. DOMAIN: 18 competency questions of record, 18 cited, zero unresolved; 88 ledger entries, every cited ref resolves, every lattice_node_ids reference resolves, 334 nodes / 546 edges with zero dangling endpoints and zero out-of-vocabulary types, relations or tiers. DEPENDENCIES: 120 edges, zero referencing a non-existent slice, zero cycles, zero pointing at an equal-or-later slice number, zero isolated slices; four roots (S01, S02, S03, S06) and a maximum transitive prerequisite depth of 42. DEMOS: four demo slices, S28 / S40 / S50 / S72, and for every one of them zero transitive prerequisites are ordered after the demo, so all four are reachable in the stated order. LANGUAGE RULE: every mention of Python across the 78 files is either a prohibition (`No .py file may be created anywhere in the skill tree`), the sign-off-gated F4 launcher rung, or the in-estate server.py being ported away; zero task file proposes new Python. EFFORT POSTURE: size_class is ordinal and is tagged `[I]` in all 78 header rows, with S01 additionally spelling out `[I - ordinal size class, explicitly non-temporal]`; no task file publishes a day figure, so no schedule figure is asserted at slice level. VERDICT: the issues above are contradictions inherited from the source PRD, id-spelling drift between two artifacts that resolves under normalisation, three structural omissions in three files, and one missing dependency edge. None of them invalidates an emitted artifact and none of them is a fabrication, a scope breach or a missing deliverable, so the verdict is APPROVED with every issue carried forward. The single most consequential line in this report is not a task defect at all: four §18 sign-off rows remain unsigned and §18's own precondition bars the build from starting until they are resolved."
}
